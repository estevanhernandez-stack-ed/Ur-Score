# Ur Score backlog: the nice-to-haves from the score book reviews

Every Minor ("nice to have") finding from the code reviews of stage 1 (v0.2.0) and stage 2 (v0.3.0), de-duplicated and checked against `feat/boards` at 7223b9b on 2026-09-15. Listed: items a review marked Minor, re-review "out of scope" notes, pre-flight notes, and work an implementer flagged as left undone. Important and blocking findings were fixed in their own rounds and aren't repeated here. The remediation plan is `docs/plans/2026-09-15-backlog-remediation.md`.

Line format: status — what it is — where — source. IDs (S1-6.8 and so on) are for cross-reference only.

## Counts

- **OPEN: 15** (5 you'd notice, 10 code, tests, performance or docs only), including V3-S.9 and V3-S.20 found since v0.3.0 shipped, the loadout work (V3-S.23, V3-S.24) asked for on 2026-09-20, the five-dimension review of 0.5.0 on 2026-09-20 now closed in full (V3-S.25 to V3-S.31 and V3-S.33), V3-S.36, the question the threat-alerts branch's final fix wave fenced but did not answer, and V3-S.40, which the fix for V3-S.38 turned from a guess into evidence
- **FIXED: 197**
- **GONE: 25**
- Total: 231 distinct items, counted as rows on 2026-09-20 (duplicates merged; every source is named on the line). The figures before that (90 open, 119 fixed, 7 gone) undercounted, because rows that put a note in brackets inside the status marker were not matched by a pattern expecting the bare word. Counting on the marker word alone reconciles the held-apart 0.3.2 section exactly to the 52/37/2 its own header claims, which is what says the count is right. **A row's status is the FIRST `**STATUS` in bold on its line, and a row must carry only one** — found 2026-09-21 when V3-S.39 was re-opened and its text still narrated a `**FIXED 2026-09-21**` inside the same row, which made a greedy match read the row as FIXED and put the totals out by exactly one. Every other multi-mark row repeats the SAME word, so no earlier count was affected; the method was simply ambiguous and had not been tested. Use a first-match count (`re.findall(...)[0]`), not a shell `grep -oE` with `.*?`, which ERE does not honour and which silently takes the LAST mark on the line.
- The recount that replaced them (96 open, 123 fixed, 8 gone) then overcounted by exactly one in every category, because it read this Counts block's own three summary bullets as backlog rows: they carry `**OPEN:**`, `**FIXED:**` and `**GONE:**` in the same shape a row does. Corrected on 2026-09-20 by counting only rows BELOW this block and above the held-apart 0.3.2 section, which still reconciles that section to its own 52/37/2. A count of this file is only believable if it states its bounds, so: rows matching `^- ` with a `**STATUS` marker, from "## Open, and you'd notice" up to "# 0.3.2".
- Rows recounted on 2026-09-17 after Wave 3 and the owner-selected bounded follow-up pair, S2-6.4 and S2-P.14; Wave 1 corrected an existing one-row undercount of FIXED items and included V3-S.20. The two summary lists below cover the main section only; desktop smoke remains pending where noted.
- Follow-up verification: 1,275 tests passed and the Release warnings-as-errors build passed with isolated outputs under obj/new-board-naming-validation. One earlier full-suite run stalled and was cancelled without an assertion failure; its retry (1,262 tests) and the final run completed in about 26 seconds with a 60-second hang timeout. The stall reproduced once on 2026-09-21 and was then diagnosed from a blame sequence file left over from the original 2026-09-17 run: six STA-thread tests stuck on a Join with no timeout. V3-S.38 carries it. Default-output WPF build and coordinated desktop smoke remain unresolved.
- S1-F.1 verification, 2026-09-17: 46 focused and 1,281 full tests passed; Release warnings-as-errors build passed via obj/claim-diagnostics-validation. VS Code test runner failed at build; isolated CLI validation succeeded. Last-read claim explanations implemented; Diagnostics desktop wrapping/clipboard smoke remains pending. Counts updated for this item.
- Desktop smoke, 2026-09-17, with RoRoRo 1.29 running and the owner watching: all ten walks against the f895491 Release build, 173 steps passed, 0 failed. The new `walk-visible-fixes.ps1` walks S2-F.8, S2-P.18/S2-P.3 (including a real drag by the grip), V3-S.13 (both tooltips, screenshots checked), S2-8.2 (the pop-out's own close returns it; WM_CLOSE and taskkill keep it; a UI Automation Close returns it), S2-8.3, S2-FR.1, S2-FR.2, AC-6.11, AC-2.2 and AC-3.4. Still unwalked, because they need a live battle read: AC-B2.18 (the SENT dot's name) and S1-F.1 (the Diagnostics claim line). Increased-DPI appearance was not walked (the rig runs at 100%). Both privacy checks on the real data folder: 0 problems.
- Note: the parked lists in the stage 2 plan's execution record (plan:5343-5349) match the OPEN items marked "parked" here.
- **0.3.2 (the Alerts page and avatars), added 2026-09-15:** 91 more distinct items in their own section at the end — **52 OPEN, 37 FIXED, 2 GONE** — from the thirteen reviews of `feat/alerts-card`. They are held apart from the counts and the two summary lists above. That branch has since merged (#9), and they haven't been folded in yet.
- Recounted 2026-09-20, task 8 of the threat-alerts plan: V3-S.32 closed **FIXED** (the two `clan.standing.threat-*` numbers, the soonest-by-time rule, the managed label, and the write-then-report ordering with no-label-no-send), and V3-S.35 opened for the operability gap its own review found and deliberately left unfixed — a suppressed threat number leaves no trace. One row out of OPEN, one row in: OPEN stays 92 (still 6 you'd notice — V3-S.32 out, V3-S.35 in), FIXED 126 → 127, GONE unchanged at 8, Total 226 → 227. Counted as rows matching `^- ` with a `**STATUS` marker, from "## Open, and you'd notice" up to "# 0.3.2" (the Counts block's own three summary bullets excluded, per the correction above); the held-apart 0.3.2 section still reconciles to its own 52 OPEN, 37 FIXED, 2 GONE, and the "Open, and you'd notice" summary list names exactly the six rows marked "you'd notice" and OPEN.
- Recounted 2026-09-20, the final fix wave of the threat-alerts branch: V3-S.36 opened for the question that wave only half closes — whether more than one clans list should be allowed to send field metrics at all, the metric ids being fixed and shared. One row in, none out: OPEN 92 → 93 (you'd notice 6 → 7, V3-S.36 in), FIXED unchanged at 127, GONE unchanged at 8, Total 227 → 228. Recounted as ROWS, never adjusted by arithmetic, on the bounds this block has stated twice now: rows matching `^- ` with a `**STATUS` marker, from "## Open, and you'd notice" up to "# 0.3.2", this block's own three summary bullets excluded. The held-apart 0.3.2 section still reconciles to its own 52 OPEN, 37 FIXED, 2 GONE, and the "Open, and you'd notice" summary list names exactly the seven rows marked "you'd notice" and OPEN.

- Recounted 2026-09-21, the flakes-and-gaps sweep: V3-S.34 (the half-empty race band), V3-S.35 (a held-back threat number leaving no trace), V3-S.38 (a Join that could not fail) and V3-S.39 (the CS5001 build flake) all closed **FIXED**, and V3-S.40 opened for what fixing V3-S.38 exposed — six STA tests blocking together, which is contention on something the process shares rather than one bad test. Four rows out of OPEN, one in: OPEN 96 → 93 (you'd notice 7 → 5, V3-S.34 and V3-S.35 out), FIXED 127 → 131, GONE unchanged at 8, Total 231 → 232. Counted as ROWS, never adjusted by arithmetic, on the bounds this block has now stated three times: rows matching `^- ` with a `**STATUS` marker, from "## Open, and you'd notice" up to "# 0.3.2", this block's own three summary bullets excluded. The held-apart 0.3.2 section still reconciles to its own 52 OPEN, 37 FIXED, 2 GONE, and the "Open, and you'd notice" summary list names exactly the five rows marked "you'd notice" and OPEN.

- Recounted 2026-09-21, second half of the flakes-and-gaps sweep: S1-5.4 (the writer loop's backstop), S2-P.11 (the boards.json fence) and S1-12.11 (four clauses, three as tests and one as a measurement) closed **FIXED**. Three rows out of OPEN, none in: OPEN 93 → 90 (you'd notice unchanged at 5), FIXED 131 → 134, GONE unchanged at 8, Total unchanged at 232. Two rows were re-read without closing and say so on their own lines rather than quietly: S2-FR.3 is mostly closed already and the one hole left needs a live walk, and S2-P.16 is a real defect whose fix changes automation ids the smoke walks hard-code, which is the owner's call rather than a sweep's. Counted as ROWS on the bounds this block states: rows matching `^- ` with a `**STATUS` marker, from "## Open, and you'd notice" up to "# 0.3.2", this block's own three summary bullets excluded. The held-apart 0.3.2 section still reconciles to its own 52 OPEN, 37 FIXED, 2 GONE.

- Recounted 2026-09-21, after `rororoblox-77`'s host-side observation on the V3-S.35 fix: V3-S.41 opened for the half of it that is ours — a held-back number's trace stops at this machine's screen, and three states (down, holding back, nothing crossed) are indistinguishable to RoRoRo because its `AlertKind` has no kind for a metric that stopped arriving. One row in, none out: OPEN 90 → 91 (you'd notice 5 → 6), FIXED unchanged at 134, GONE unchanged at 8, Total 232 → 233. Counted as ROWS on the bounds this block states. The held-apart 0.3.2 section still reconciles to its own 52 OPEN, 37 FIXED, 2 GONE, and the "Open, and you'd notice" summary list names exactly the six rows marked "you'd notice" and OPEN.

- Recounted 2026-09-21, the recipe-update fix: V3-S.42 opened and closed **FIXED** in one go (an installed recipe could never be updated, so every user stayed on whatever they first imported), and **V3-S.39 RE-OPENED** — the CS5001 flake came back with the rsp in force and zero MSBuild nodes alive, so closing it on one clean afternoon was too strong. One row in, one row back out of FIXED: OPEN 91 → 92 (you'd notice unchanged at 6; V3-S.39 is a test-gap row), FIXED unchanged at 134, GONE unchanged at 8, Total 233 → 234. **The counting method itself was corrected in the same pass** and the bullet above now states it: a row's status is the FIRST bold marker on its line and a row carries only one. The shell count used until now took the LAST, which no earlier count was affected by — every other multi-mark row repeats the same word — but which would have been wrong here by exactly one. The held-apart 0.3.2 section still reconciles to its own 52 OPEN, 37 FIXED, 2 GONE, and the "Open, and you'd notice" summary list names exactly the six rows carrying both marks.

- Recounted 2026-09-21, the tidiness batch: S1-7.1, S1-7.2, S1-8.1, S1-8.6 and S1-9.5 closed **FIXED**. Five rows out of OPEN, none in: OPEN 92 → 87 (you'd notice unchanged at 6), FIXED 134 → 139, GONE unchanged at 8, Total unchanged at 234. **Two of the five had their diagnosis slightly wrong on the row, and both rows now say so:** S1-7.1 called a fully qualified name unnecessary when `Source` binds to a NAMESPACE in that file and dropping it does not compile (the fix is the alias the UI files already use), and S1-8.6 called an unlocked reference read a staleness problem when a reference assignment is already atomic — what was missing was visibility across cores, which is what volatile buys. Counted as ROWS by first bold marker, one marker per row. The held-apart 0.3.2 section still reconciles to its own 52 OPEN, 37 FIXED, 2 GONE.

- Recounted 2026-09-21, after the V3-S.42 walk: V3-S.43 opened and closed **FIXED** in one go — the walk itself seeded a folder that could report to RoRoRo, which is a process defect the walk caused rather than found. One row in, none out of OPEN: OPEN unchanged at 87, FIXED 139 → 140, GONE unchanged at 8, Total 234 → 235. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-21, the tidiness-and-edges sweep: S1-6.7, S1-13.13, S2-2.2 and S2-4.1 closed **FIXED**; S2-2.1 closed **GONE** because it is not a defect — the alias it calls redundant is load-bearing, checked by deleting it and watching CS0118; and V3-S.44 opened for the cause, a namespace that shadows a type and has now produced three wrong rows. Five rows out of OPEN, one in: OPEN 87 → 83 (you'd notice unchanged at 6), FIXED 140 → 144, GONE 8 → 9, Total 235 → 236. **Two of the four fixes needed a new test because the existing one could not fail:** S1-6.7's timestamp was asserted only as "not in the past" against the real clock, and S1-13.13 had no test at all. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-21, the behaviour-edges sweep: V3-S.39 closed **FIXED** — root-caused at last, and it was never the entry point going missing but a failed cleanup of the WPF temp project leaving the NEXT build to report CS5001 — and S1-6.10 closed **FIXED** as far as detection can take it. Two rows out of OPEN, none in: OPEN 83 → 81 (you'd notice unchanged at 6), FIXED 144 → 146, GONE unchanged at 9, Total unchanged at 236. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-21, the second edges sweep: S1-9.2 (a later final corrects an earlier one) and S1-2.2 (a fractional rank is no rank) closed **FIXED**; S1-9.4 and S1-14.15 closed **GONE**, both checked rather than reasoned — the earliest final is the right time for placing a battle, and `File.Exists` does not answer false for a denied file on this platform, tested with a real ACL rule; and **V3-S.39 re-opened a second time**, because CS5001 came back once with the cleanup target in place and no MSB3061 in the full log, so the race has a second face the mitigation does not reach. Four rows out of OPEN, one back in: OPEN 81 → 78 (you'd notice unchanged at 6), FIXED 146 → 147, GONE 9 → 11, Total unchanged at 236. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-21, the third edges sweep: S1-3.2 (re-adding switches back on, now on purpose), S1-4.2 (a cancelled wait no longer spaces the next request), S1-F.4 (the recipe text file is written atomically) and S1-F.10 (skipped lines are counted and said) closed **FIXED**. Four rows out of OPEN, none in: OPEN 78 → 74 (you'd notice unchanged at 6), FIXED 147 → 151, GONE unchanged at 11, Total unchanged at 236. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-21, the namespace rename: V3-S.44 closed **FIXED** on the owner's go-ahead. One row out of OPEN: OPEN 74 → 73 (you'd notice unchanged at 6), FIXED 151 → 152, GONE unchanged at 11, Total unchanged at 236. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-21, Sweep A of the remaining-backlog plan, on fifteen owner rulings given in one message: S2-9.1, S1-1.3, S1-5.2, S2-1.2, S2-2.3, V3-S.9 (closed with the rulings that went the other way recorded) and V3-S.37 (now `docs/testing.md`) closed **FIXED**; S2-P.4, S2-P.7, S2-P.19, S2-8.5 (a historical plan is a record, not a spec), S1-2.3 (merged history stays), S1-6.5 (a read is one observation) and S2-P.9 (the kept copy is the user's own text) closed **GONE**. Fourteen rows out of OPEN, none in: OPEN 73 → 59 (you'd notice 6 → 5, V3-S.9 out), FIXED 152 → 159, GONE 11 → 18, Total unchanged at 236. Counted as ROWS by first bold marker, one marker per row — and the count corrected an arithmetic slip in the first draft of this bullet, which had thirteen. The "Open, and you'd notice" list names exactly the five rows carrying both marks.

- Recounted 2026-09-21, the pop-out id scheme: S2-P.16 closed **FIXED** on the owner's scheme. One row out of OPEN: OPEN 59 → 58 (you'd notice unchanged at 5), FIXED 159 → 160, GONE unchanged at 18, Total unchanged at 236. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-21, Sweep B1 (the read lifecycle in `SourceHost`): S1-8.2, S1-8.3 and S1-8.4 closed **FIXED**, one answer to one question — what a watch does with a change that lands mid-read — written once. Three rows out of OPEN, none in: OPEN 58 → 55 (you'd notice unchanged at 5), FIXED 160 → 163, GONE unchanged at 18, Total unchanged at 236. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-21, Sweep B2 (`AppServices` and the account list): V3-S.28, S1-14.7, S1-14.8, S1-14.13 and S2-5.10 closed **FIXED**. Five rows out of OPEN, none in: OPEN 55 → 50 (you'd notice unchanged at 5), FIXED 163 → 168, GONE unchanged at 18, Total unchanged at 236. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-21, Sweep B3 (`BoardsFile` and the book): S1-5.1, S1-11.2, S1-12.6 and S1-2.1 closed **FIXED**, S2-1.1 closed **GONE**. Five rows out of OPEN, none in: OPEN 50 → 45 (you'd notice unchanged at 5), FIXED 168 → 172, GONE 18 → 19, Total unchanged at 236. Sweep B is complete. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-22, Sweep C1 (the core): S1-6.4, S1-6.6, S1-8.5, S1-F.9 and S1-14.16 closed **FIXED**; S1-6.11 and S1-4.3 closed **GONE** (measured and bounded respectively). Seven rows out of OPEN, none in: OPEN 45 → 38 (you'd notice unchanged at 5), FIXED 172 → 177, GONE 19 → 21, Total unchanged at 236. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-22, Sweep C2 (the UI and the composition root): S1-14.14, S1-10.2, S1-10.1, S1-11.1, S1-14.6, S1-12.9 and S1-14.12 closed **FIXED**; S1-14.17 closed **GONE**. Eight rows out of OPEN, none in: OPEN 38 → 30 (you'd notice unchanged at 5), FIXED 177 → 184, GONE 21 → 22, Total unchanged at 236. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-22, Sweep C3 (the board): S1-13.10, S2-6.7 and S2-P.13 closed **FIXED**; S2-4.7 closed **GONE** (counted the callers). Four rows out of OPEN, none in: OPEN 30 → 26 (you'd notice unchanged at 5), FIXED 184 → 187, GONE 22 → 23, Total unchanged at 236. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-22, Sweep C4 (`PanelModels.cs` split): S1-13.15 closed **FIXED**. One row out of OPEN, none in: OPEN 26 → 25 (you'd notice unchanged at 5), FIXED 187 → 188, GONE unchanged at 23, Total unchanged at 236. Sweep C's four clusters are complete; S1-F.11 (the rename before going public) is the one row folded into C still open. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-22, Sweep C5 (the rename before going public): S1-F.11 closed **FIXED**, with the docs half left as the owner's question. One row out of OPEN, none in: OPEN 25 → 24 (you'd notice unchanged at 5), FIXED 188 → 189, GONE unchanged at 23, Total unchanged at 236. Sweep C is complete. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-22, Sweep D (performance, measured): S1-F.3 and S1-F.2 closed **FIXED** with a number before and after; S2-5.1 and S1-12.5 closed **GONE** by measurement. V3-S.45 opened for what the measurement found. Four rows out of OPEN, one in: OPEN 24 → 21 (you'd notice unchanged at 5), FIXED 189 → 191, GONE 23 → 25, Total 236 → 237. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-22, Sweep E1 (timers): S1-8.7 and S1-5.6 closed **FIXED**. Two rows out of OPEN, none in: OPEN 21 → 19 (you'd notice unchanged at 5), FIXED 191 → 193, GONE unchanged at 25, Total unchanged at 237. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-22, Sweep E2 (the lists, and the stall): S1-L.2, S1-L.4 and V3-S.40 closed **FIXED**. Three rows out of OPEN, none in: OPEN 19 → 16 (you'd notice unchanged at 5), FIXED 193 → 196, GONE unchanged at 25, Total unchanged at 237. Counted as ROWS by first bold marker, one marker per row.

- Recounted 2026-09-22, Sweep E3 (the startup seam): S1-14.9 closed **FIXED**. One row out of OPEN, none in: OPEN 16 → 15 (you'd notice unchanged at 5), FIXED 196 → 197, GONE unchanged at 25, Total unchanged at 237. Sweep E is complete. Counted as ROWS by first bold marker, one marker per row.

## Open, and you'd notice

- V3-S.20 Clan membership comes only from battle contributions, so accounts cannot be placed between battles (roster design cycle).
- V3-S.23 Loadout testing, not built: mark a window as running a loadout, and say which one actually earned more per hour.
- V3-S.24 Event loadout testing as its own session, not built: events reward different enchants, books and potions, so each is tested on its own.
- V3-S.41 A held-back number leaves a trace on this machine's screen only, and the phone is the premise.
- V3-S.36 The clan numbers describe ONE of your clans, the best placed. With alts in two clans you cannot get alerts for both, because each number has one fixed id.

## Open, code tidiness or tests only

- **Test gaps:** V3-S.39 (the CS5001 build flake, re-opened a second time), S1-15.4, S1-L.1, S1-L.3, S2-8.6, S2-FR.3
- **Behaviour edges nobody is likely to see:** S1-L.5
- **Performance:** V3-S.45 (the reader's memory, found by Sweep D's measurement)
- **Code tidiness:** S2-FR.4 (S1-13.13 was listed here after it closed on 2026-09-21; dropped 2026-09-22)
- **Docs, plan text, process:** S1-16.1

---

# Stage 1 (v0.2.0)

## Task 1: recipe format additions
- S1-1.1 **FIXED** — RecipeParserTests now rejects literal player IDs at the start, middle, and end of period history paths, including leading zeros and oversized numbers. Named fields and known placeholders remain accepted. Nine regression cases; production parsing unchanged. Original finding: T1 review.
- S1-1.2 **FIXED** — RecipeFormatTests now covers empty, space-only, and tab/newline labels falling back to "Items", plus an explicit plural overriding an empty label. Four added cases; production behavior unchanged. Original finding: T1 review.
- S1-1.3 **FIXED** 2026-09-21 — code tidiness: the parser repeats the "is this a group list" rule with no comment saying why it can't reuse it — src/Recipes/RecipeParser.cs:77 — T1 review The parser's copy of the rule now says why it is a copy: the `Recipe` does not exist yet at that point, and the check decides whether a recipe with no `metricId` is a problem or a group list, which has to be settled before one can be built.

## Task 2: engine periods and past
- S1-2.1 **FIXED** 2026-09-21 — code tidiness / test gap: a read that stops on its last step still carries a period, contrary to what the report said; no test — src/Recipes/RecipeEngine.cs:171, 182 — T2 review A stop on the last step keeps the period the earlier steps established, on both shapes of recipe. The per-account path always did; the list path dropped it, so the same failure named the battle on the board's top line for one recipe and not the other. Nothing records a stop — `Record` is never reached for one — so a period on a stop is information for that line and not a decision. **The test needed the clan-battle fixture:** `tests/Fixtures/petsim99-top-clans.recipe.json` is an older copy with no period declared, so a recipe read through it has none to carry whatever the code does, which cost twenty minutes of certainty that the fix was not running before the fixture was read. Watched red under the old return.
- S1-2.2 **FIXED** 2026-09-21 — a fractional rank in a top list is cut to a whole number instead of treated as unreadable (real data is whole numbers) — src/Recipes/RecipeEngine.cs — T2 review. A rank is a whole number from 1 up, or it is no rank: 2.7 used to become second place with nothing saying the number was invented, and 0 and -3 went through untouched. The group is still read; only its rank is left out, the way an unreadable stat is. Watched red (2, 0, -3) then green.
- S1-2.3 **GONE** 2026-09-21 — process only: several stage 1 commits carry a "Claude Sonnet 5" or "Claude Haiku 4.5" co-author line instead of the mandated Opus 5; merged history, nothing to do short of a rewrite — git history (e.g. 52faa24, dd9e406) — T2, T3, T5, T9, T12, T16 reviews Owner's ruling 2026-09-21: merged history is not rewritten for a co-author line.

## Task 3: sources
- S1-3.1 **FIXED** — SourcesTests and ClansModelTests now cover Watch directly to Main, retaining source identity and inputs, demoting only the same recipe's old main, moving the row from watching to owned, and preserving input records. Setup re-enables a picked source; the lower-level role change preserves its enabled state. Four added cases; production unchanged. Original finding: T3 review, T11 review.
- S1-3.2 **FIXED** 2026-09-21 — re-adding a clan you had switched off silently switches it back on; undocumented and untested — src/Core/Sources.cs — T3 review. The behaviour is unchanged and now deliberate: adding is an affirmative act, and somebody who switched a clan off and then adds it again has said in the clearest way the app offers that they want it read. Leaving it off would make the Add button look broken for that one clan. The id is kept, because the score book's lines are filed under it. Written on the method and pinned by a test, which fails when the switch-on is removed.
- S1-3.3 **FIXED** — a locked or permission-denied sources/accounts file could crash loading instead of reading as empty — Task 4 fix round (src/Core/Sources.cs:67, src/Core/SharedAccounts.cs:30) — T3 review, T4 review
- S1-3.4 **GONE** — implementer reports with wrong test counts, line numbers or claims (the reports were deleted; the code was right) — n/a — T3, T4, T8, T11 reviews

## Task 4: shared accounts, claims, spacing
- S1-4.1 **FIXED** — a gated SharedAccountsTests regression proves another host completes while one request is held in flight and a second same-host request remains queued. It verifies serialization and drains requests after releasing the gate, without depending on elapsed-time ordering. Existing interval-spacing coverage remains; production unchanged. Original finding: T4 review.
- S1-4.2 **FIXED** 2026-09-21, for the half that was wrong — after a failed or cancelled request, the next one to that site still waits the full 2 s (slower recovery, never too fast) — src/Recipes/SpacedTransport.cs — T4 review. Only a request that actually LEFT now stamps the host's spacing clock. One cancelled while still waiting its turn never reached anybody's server, and counting it made the next caller wait a full spacing for nothing. A request that left and then FAILED still counts, on purpose: it did reach the server, and the spacing exists to protect the server rather than to reward a good answer — so that half of the row is working as intended and stays. Timing-based test with the numbers chosen for room (spacing 400 ms, cancel at 200; old code cannot start the next before 600, new starts at 400, assertion accepts up to 520); the mutation measured 604 ms, which is the old code's answer to the millisecond.
- S1-4.3 **GONE** 2026-09-22 — code tidiness: the claims table and per-site lanes never forget old entries (negligible at real scale) — src/Core/AccountClaims.cs:12; src/Recipes/SpacedTransport.cs:13 — T4 review The row says negligible and the bound says why: both tables are keyed by things that do not grow with time. The claims table's key is (recipe slug, one of YOUR user ids) — the product of installed recipes and your RoRoRo accounts, tens of entries, each a string and a timestamp — and a removed account or recipe leaves one such entry behind for the life of the process. The transport's lanes are one per distinct host across every recipe's URLs; the shipped recipes use two. Neither is a leak; both are a set whose size is set by what is configured. Nothing to forget that would not be a timer to forget it with.

## Task 5: score book writer
- S1-5.1 **FIXED** 2026-09-21 — the "dropped lines" count can be off by one in a rare race (the count only; no data lost) — src/Book/ScoreBook.cs:84 — T5 review The drain's "can never be written" branch counts a drop only if THAT branch removed the node. An append's overflow on another thread could take the same node out first — already counted there — and the branch then counted it again with nothing left to remove. **The race has no seam and the row says so:** it needs the writer thread paused between taking a node and its catch, and the mutation that counts regardless passes the test, which pins only the ordinary case (one unwritable line is one drop). Right by inspection — count only what you removed — and no more is claimed.
- S1-5.2 **FIXED** 2026-09-21 — code tidiness: `Written`'s comment says "on the writer's thread", untrue in test mode — src/Book/ScoreBook.cs:10 — T5 review The comment now says which thread in which mode — the writer's when the book runs one, the caller's under `background: false` — and that the line is on disk before it fires either way.
- S1-5.3 **FIXED** — strengthened `RemovingARecipeLeavesItsBook` at the RecipeStore removal boundary used by AppServices: confirms the installed recipe and state actually disappear, uses the parsed recipe's slug and text hash, and verifies read/final history across two months plus archived recipe text remain byte-for-byte unchanged and readable. The old test already called Remove but only checked one surviving line. Production unchanged; live UI removal not exercised. Original finding: T5 review.
- S1-5.4 **FIXED** 2026-09-21 — test gap: the writer loop's last-resort catch has no test — src/Book/ScoreBook.cs — T5 re-review. **Why it had none, which is the interesting part:** it cannot be reached through `Append`. `Drain` already contains every failure it knows how to handle — a locked file, a denied handle, a line that can never serialize, a subscriber that throws — so no input makes it throw, and a test driving the public API can only ever prove the paths that ARE handled. The loop turn is now `ScoreBook.Turn(Action)`, internal to the tests, and what is pinned is not the trigger (by definition the one nobody thought of) but the promise: the loop gets another turn. Two turns in the test, deliberately, because a backstop that swallowed the exception and then stopped looping would pass a test that took only one. Mutation-tested by removing the catch.
- S1-5.5 **FIXED** — a wrongly typed time (`{"t":123}`) might break reading the book: the final review tested it and it is skipped safely, and the related nested-null hole was fixed — fix wave a830a42..a8161dc (`BookJson` completeness check in src/Book/BookLine.cs) — T5 re-review, final review Minor 7
- S1-5.6 **FIXED** 2026-09-22 — test gap / slow suite: the drop-limit test takes about 24 s because every line is flushed to disk (a durability choice) — tests/ScoreBookTests.cs:138; src/Book/ScoreBook.cs:207 — T5 re-review, T5 report 24 s, and none of it where the row thought: the appends were fast, and the time was the `Flush()` at the end syncing five thousand lines to disk one by one — the durability the app is right to keep. The limit is now a constructor parameter, `DefaultMaxPending` (5,000) unless a test says otherwise, and the test says fifty: the rule under test is "the oldest reading goes first and a final never goes", which the number changes nothing about. 388 ms for the whole book class. **The suite went from 26 s to 4 s** — that one test was most of the wall time.

## Task 6: lines and watch recording
- S1-6.1 **FIXED** — RecipeWatchTests now runs the first-report recipe/policy-switch scenario with and without a book. With recording attached, exactly one completed old-recipe reading retains its slug, text hash, archived text, source inputs and both account values; only the first report is sent, and remembered account rows stay cleared. Existing change-during-read coverage remains. Production unchanged. Original finding: T6 review.
- S1-6.2 **FIXED** — RecipeWatch suppresses account mapping for group-list recipes regardless of source role. RecipeWatchBookTests now covers Watch, Mine, and Main: the engine is called with no account IDs, groups remain visible, no lines or recipe text are recorded, and no reports are sent. Owned-role cases reproduced the ID leak before the guard changed. Original finding: T6 review.
- S1-6.3 **FIXED** — RecipeWatchBookTests uses a manual clock to check one second before, exactly at, and one second after twice the recipe's effective interval. Both account recording and reporting are blocked before expiry and allowed at/after it; the previous source cannot immediately reclaim the account from its new owner. Three added cases; production unchanged. Original finding: T6 review.
- S1-6.4 **FIXED** 2026-09-22 — code tidiness: the book line records the source's inputs while the read used the watch's own copy; if they ever differ a line is filed under the wrong inputs — src/Book/LineBuilder.cs:48 — T6 review `ReadContext` carries the inputs the READ was made with, and the line is filed under those; it took the source's, and the two are separate fields updated by separate calls (`UpdateSource`, then `UpdateRecipe`) that the composition root makes one after the other, so a cycle starting between them read the old clan and wrote the new one on its line. **The first draft of the test passed before the fix and was thrown out:** it swapped the source MID-read, and a cycle takes its source and inputs together at the top, so a mid-read swap never reaches the line at all. The window is between the two update calls, which is what the test now sets up — source updated, inputs not yet; watched red ("Other Clan") then green ("K0i2").
- S1-6.5 **GONE** 2026-09-21 — switching a clan to Watch during a read lets the rest of that read send (documented as "applies next read") — src/Core/RecipeWatch.cs:206 — T6 review Owner's ruling 2026-09-21: a read is one observation, and "applies next read" is the right rule — it is documented and it holds. Changing a clan's role mid-read does not reach into the read already in flight.
- S1-6.6 **FIXED** 2026-09-22 — code tidiness: the lock comment says every access is locked, but `Recipe` is read without it — src/Core/RecipeWatch.cs:95-97, 122 — T6 review The comment is made true rather than softened. `Recipe` was read with no lock and, it turned out, by nothing at all — dropped, and the build agreed. `Policy` takes the lock like `Source` does, and the one unlocked read of the policy inside the cycle (`policy.SentStats.Count == 0`) now reads the list it took under the lock a few lines down, moved up. The comment names every field the lock covers, policy included, and says why a lock rule with unwritten exceptions is not a rule. No test: a stale reference read cannot be provoked on demand, and the change is to which lines hold a lock.
- S1-6.7 **FIXED** 2026-09-21 — code tidiness, and it was more than that: report timestamps use the system clock, not the injected one — src/Core/RecipeWatch.cs — T6 review. One `Now` property, and every timestamp in the class reads it. The account-stat path took `DateTimeOffset.UtcNow` directly while the field numbers and the book lines already took the injected provider, so ONE read produced two kinds of time. That is worse than either alone: RoRoRo keys a rate window on the observation time, so two halves of one reading disagreeing about when it happened is not untidiness, it is a reading nobody can reason about afterwards. **The existing test did not discriminate** — `ReportsOnlyYourOwnAccountsRawAndInUtc` asserted only `ObservedAt >= before` against the real clock, which passes either way (V3-S.37 again). The new one injects an instant in 2031, far from any coincidence, and mutation-testing by putting `UtcNow` back fails it.
- S1-6.8 **FIXED** — you'd notice: Setup › Score book gave the wrong "not recording" reason when another clan source had claimed your account ("None of your accounts were in this read"). A read whose accounts another source of the recipe claimed first now says "Your accounts in this read are kept by another source of this recipe, which read them first."; a read that really had none of them still says so. Naming the source that keeps them is S1-F.1 — src/Core/RecipeWatch.cs (`Record`, `NotRecordingKeptElsewhere`) — T6 review, fixed 2026-09-16
- S1-6.9 **FIXED** — you'd notice: "#rank of N" counted players with no value for that stat, so it disagreed with Promotion check's count. N is now how many players the rank was counted among, the ones with a value, the same field Promotion check ranks against: on My accounts and Account card ("#1 of 3" where four were read and one had no points), and in `--try`'s "rank 1 of 2". The score book keeps that field per stat, as `ranked` beside `of` (which stays the row count, spec §5.2), so a remembered place and the first read of the same clan give the same N. A line kept before this has no field, and its place shows alone ("#7"), because its `of` counted players with no value too — src/Book/Ranking.cs (`Competition`, whose count is N); src/Book/LineBuilder.cs (`Accounts`); src/Book/BookLine.cs (`BookAccount.Ranked`); src/Book/Remembered.cs; src/Core/RecipeWatch.cs (`RankInGroup`); src/Board/PanelModels.cs (`InGroup`); src/Cli/TryCommand.cs — T6 review, T13 review, fixed 2026-09-16
- S1-6.10 **FIXED** 2026-09-21, as far as it can be — privacy edge: a headline number is only compared with the ids in this read's rows, so an id-like number for someone outside them could be kept — src/Book/LineBuilder.cs — T6 review. `OtherIds` now covers every id the READING mentions: the rows, the accounts the source said it could not show (`Unavailable`), and the ones a stat missed for (`CellMisses`). Two of those three were known to the read and never looked at. Mutation-tested by narrowing it back.
  **The limit is now written where the code is, because it cannot be closed by detection and somebody will otherwise assume it was.** A stranger's id can only be recognised by having SEEN it: a Roblox user id and a clan's battle points are both plain integers in the billions, so any rule based on the shape or size of the number would throw away real points. A recipe whose headline path points straight at, say, a clan leader's id would still keep that number, because the reading never mentions the leader anywhere else. **The control for that case is consent, not detection** — the import screen lists every headline a recipe will keep before it is installed (`ImportText`) — and it is a real control rather than an airtight one, because it lists the recipe author's LABELS and not the paths behind them. Checked: the three shipped recipes declare two headlines between them, `clan-place` and `clan-points`, and neither is an id. So this is a third-party recipe risk of the same family as V3-S.25, and the same answer applies — what a recipe is trusted to declare should be visible before it is trusted.
- S1-6.11 **GONE** 2026-09-22 — code tidiness: the recipe hash is recomputed every read — src/Core/RecipeWatch.cs:428 — T6 review Measured rather than cached: `BookFiles.Hash` is SHA-256 of the recipe text, 1,290 bytes for the shipped clan-battle recipe, and 10,000 of them took 59 ms in a loop — six microseconds each, including the loop — once per read at intervals of a minute or more. A cache would be a field to keep in step with the text for a saving nobody could measure. `ContextFor`'s comment now says the hash is computed each time on purpose, with the number.

## Task 7: finals and backfill
- S1-7.1 **FIXED** 2026-09-21 — code tidiness: an unnecessary fully qualified name, with a wrong reason given in the report — src/Book/Finals.cs — T7 review. **And the row was half wrong too, which is the useful part.** The name was not unnecessary: `Source` alone binds to the `Labs626.UrScore.Source` NAMESPACE in this file, not the type, so dropping the qualification does not compile — found by dropping it and watching it fail, not by reading. What was actually wrong is that the codebase already has an idiom for this (`using Source = Labs626.UrScore.Core.Source;`, as the UI files use) and these two sites spelled the full name instead. Now aliased, with the collision stated where the alias is, so the next reader does not try the same edit.
- S1-7.2 **FIXED** 2026-09-21 — code tidiness: the "period has ended" test is written twice — src/Book/Finals.cs — T7 review. One `FinalsPlanner.EndHasPassed`, used by both readers. They were deciding the same thing — whether a period is still running — and two copies of a comparison are two chances to disagree about the boundary, which here would mean a final written a read early or a read late. The inclusive end (a period ending exactly now has ended) is now stated once rather than implied twice.

## Task 8: SourceHost
- S1-8.1 **FIXED** 2026-09-21 — code tidiness: cancellation sources are never disposed and pile up slightly with each Start/Stop — src/Core/SourceHost.cs — T8 review. Each Start made one run source and one LINKED source per entry, and each Stop dropped all of them; a session that started and stopped repeatedly accumulated them along with the registrations each linked source holds on both parents. `Stop` now disposes the run source and each loop disposes its own linked source in a `finally`, whichever way it leaves. **Two things done carefully rather than quickly.** `Cancel` moved OUTSIDE the lock — it runs every linked source's callbacks on the calling thread, and doing that under the gate makes "could a callback ever want the gate?" a question somebody has to keep re-answering. And a removed entry's own source is cancelled but NOT disposed there, because its loop may be between the cancel and its finally. `StartAndStopManyTimesReleasesItsCancellationSourcesAndStillReads` runs ten cycles and reads afterwards; mutation-tested by disposing in the wrong order, which throws `ObjectDisposedException` rather than leaking quietly.
- S1-8.2 **FIXED** 2026-09-21 — on exit, a read in flight isn't waited for, so at most one read's lines can be lost — src/Core/SourceHost.cs:99-106 — T8 review, T14 review (exit race), stage 1 final review triage Exit now stops the loops first, so no new read starts, then gives reads already in flight `SourceHost.ExitGrace` (three seconds) to land before cancelling them. That needed the loop to run under TWO tokens — one that ends the loop and cuts its wait short, one that cancels the read — because with one there was no way to say "no more reads, but finish the one you are on". Stop still cancels both at once, as it always did; exit is the path that waits, and only for a read a moment's patience can keep. Two tests: a read that lands inside the grace is in the book, and a read that never lands is cut off at the grace so exit cannot hang on a dead network. The first fails by name when exit cancels reads at once again.
- S1-8.3 **FIXED** 2026-09-21 — Test now keeps reading, recording and sending a clan you removed mid-read; only its snapshot is hidden — src/Core/SourceHost.cs:91-97 — T8 review Two halves. Test now ran each read under the caller's token alone, so a removal cancelled the loop's reads (always linked to the entry's own token) but not Test now's — it hid the snapshot and the read went on to record and send. Test now's reads now link the entry's token too. And nothing after the fetch checked the token at all, so a read whose fetch had RETURNED by the time of the removal recorded and sent regardless; there is now one check after the fetch, the last point a read can be stopped before it has any effect. Both tested through a gated engine that blocks until told and honours cancellation, which the suite's stub could not do; both fail by name under the mutation that puts each back.
- S1-8.4 **FIXED** 2026-09-21 — switching a clan off and straight back on during a read can briefly run two watches for it — src/Core/SourceHost.cs:45-48 — T8 review Bounded rather than eliminated, and the bound is now pinned. Off and straight back on during a read: the old watch's read is cancelled by the removal and the new one starts its own, so for a moment there are two — one draining, one starting. What the row feared was two watches READING: a burst against the server and a doubled line in the book. The test drives exactly that sequence through the gated engine and asserts two starts, one cancellation, one finish and one book line. A third start or a second line is the defect, and neither happens.
- S1-8.5 **FIXED** 2026-09-22 — code tidiness: creating a watch runs inside the lock, so a slow or failing factory blocks the UI or half-applies — src/Core/SourceHost.cs:61 — T8 review `Apply` builds every new watch first, with no lock held, and applies the whole set after: removals, source updates and adds in one locked pass. So a factory that takes its time holds up only the Apply that called it — `WatchFor` and the loops filing snapshots answer meanwhile — and a factory that throws has applied NOTHING: the sources it would have dropped are still there, the watches it had built are not added, and the caller's exception means "not applied" rather than "partly". An entry appearing under a built id between the two locks (a concurrent Apply, which the class never promised to serve) keeps the one that is there. Two tests: the throw on the third of three sources leaves the first's watch in place and adds neither of the others; a factory blocked on a gate is asked `WatchFor` from another thread and must answer within half a second. Both watched red — the second timed out — then green; the Start/Stop and lifecycle tests all still pass.
- S1-8.6 **FIXED** 2026-09-21 — code tidiness: a source reference is read without the lock (possibly stale, never corrupt) — src/Core/SourceHost.cs — T8 review. `Entry.Source` is now a volatile read and write. The row's own parenthesis had the diagnosis slightly off: a reference assignment is already atomic, so the value was never going to be corrupt and no lock was needed for that — what was missing is the guarantee that a loop on another core SEES a new source rather than a cached old one, which the memory model does not give for a plain field. Volatile buys exactly that and nothing more. The staleness that remains is deliberate and one read long: a read already in flight finishes against the source it started with.
- S1-8.7 **FIXED** 2026-09-22 — test gap: no tests for Stop then Start, timer-triggered reads, the interval clamp, Dispose or the interval-lookup guard; the removal-during-read test never hits the real race window — tests/SourceHostTests.cs — T8 review, T8 re-review. **Four of the six closed.** Stop-then-Start (an entry has to survive its loop being cancelled, or a source that went quiet after a Stop/Start would look exactly like one never switched on), the interval clamp in BOTH directions (a clamp written as Min passes a test that only checks the low side), the interval-lookup guard, and Dispose including twice. The clamp and the guard moved into `SourceHost.DelaySeconds` to be reachable at all — the floor is a minute, so a test driving the real loop would have to sit through one, and a rule nobody can afford to test is one that quietly stops holding. A fifth was found and closed while in there: **Start twice** was untested, and without its guard each Start adds another loop per entry, so a source read once a minute gets read twice, then three times, against somebody else's server. All mutation-tested (clamp → Min, guard removed, Start guard removed). **The last two, closed with a clock the tests can move.** `SourceHost` takes a `TimeProvider` and waits between reads with `Task.Delay(interval, time, token)`; `ManualTime` in the tests now has timers of its own, fired in due order as `Advance` moves the clock through them, with a continuation running right there inside the call. Three loop-level tests: the second read comes at 180 s and not at 179, and carries the timer trigger; a lookup asking for five seconds gets no read at five and one at the floor — the clamp WIRED to the wait, not only present in `DelaySeconds`; a source removed between reads never reads again when the interval passes. Two mutations, both caught by name: the wait back on the wall clock (all three fail, "the loop never reached its wait"), and the clamp as Min (the floor test fails). The removal-during-read half was covered by B1's gated-engine tests; this is the between-reads half. Every future timer in the app can be tested the same way without waiting one out.
  **Two left, and the reason each is left rather than quietly dropped.** (1) TIMER-TRIGGERED READS need the loop's `Task.Delay` driven by a clock a test controls. `Task.Delay` has a `TimeProvider` overload, but the suite's `ManualTime` overrides only `GetUtcNow` — not `CreateTimer` — so it falls through to the real system timer and controls nothing. The route is concrete: give `SourceHost` an optional `TimeProvider` (one production call site, `AppServices.cs:132`), use the `Task.Delay(delay, timeProvider, token)` overload, and give the test double a real `CreateTimer`. That last part is the actual work. (2) The REMOVAL-DURING-READ race wants a read suspended at a chosen point while `Apply` removes the source, which needs a seam the engine stub does not have.
- S1-8.8 **FIXED** — the manual-read SourceHostTests case now requires exactly one book line per source, verifies each source's inputs, read kind, manual trigger and account value, and requires both snapshots to report Recorded. Empty or duplicated output can no longer pass the assertions. Production unchanged. Original finding: T8 review.

## Task 9: reader and records
- S1-9.1 **FIXED** — two direct ScoreBookReaderTests cover headline-series source/period/headline filtering, chronological order, source-level freshness and offset, exclusion of finals and expired readings, missing-data results, and duplicate handling with source timestamps and the exact 60-second boundary. Production unchanged. Original finding: T9 review.
- S1-9.2 **FIXED** 2026-09-21 — with two final lines for one account in one battle, the first wins, so a later correction is ignored — src/Book/ScoreBookReader.cs — T9 review. The latest final by T now wins for an account's numbers and for the headline, because a later final is a correction and the point of a correction is to replace what it corrects. By T and not by file order, deliberately: a "Bring in stats" merge appends the other machine's lines after this one's whatever their times, so position in the file says nothing about which is newer. **The first draft of the test passed before the fix** — two lines, correction first, and "first wins" picked it by coincidence (V3-S.37 rule 2). It now has three lines for the same battle in an order where each of the three candidate rules gives a different number, 100, 175 and 250, and only latest-by-time gives 250. Watched red, then green.
- S1-9.3 **FIXED** — you'd notice (not reachable): Records merged several sources' readings into one series, so "biggest day" and "fastest week" could run from one source's number to another's. A duplicate is now what ruling R4 says inside one source (same value and same as-of, or no as-of and under 60 s apart), which the reader already collapses; two sources' readings are never merged, because two clans' points for one account are different numbers and nothing can prove a reading through one source is the same observation as another's. `Records.For` takes one source, and the Records panel shows the best of each source's own records (a max, so nothing counts twice). Every caller already passed one source (plan P4), so no board showed a merged record — src/Book/Records.cs (`For`); src/Board/PanelModels.cs (`RecordsPanel`, `AccountCard`) — T9 review, fixed 2026-09-16
- S1-9.4 **GONE** 2026-09-21 — not a defect: a past battle's time is its earliest final line, not its latest — src/Book/ScoreBookReader.cs — T9 review. Earliest is RIGHT, and the reason is what the time is for. `FinalEntry.T` places the battle in the Past battles list, newest first. A final written at the end of a battle is the closest thing to when it ended; a correction written five days later is not. Under "latest", a corrected battle would jump to the top of the list as though it had just finished. The existing test `FinalsMergeSupplementaryLinesAndListNewestFirst` already pinned this ordering (A's finals at -20 and -1 days, B's at -5; order B then A), which is the evidence. So an entry now deliberately carries two clocks — its place from the earliest final, its content from the latest (S1-9.2) — and the code says why.
- S1-9.5 **FIXED** 2026-09-21 — code tidiness: fully qualified `Source.KeyOf` instead of the alias used elsewhere — src/Book/ScoreBookReader.cs — T9 review. This row had it right where S1-7.1 did not: the fix is the alias, not simply deleting the qualification, because `Source` binds to a namespace here. Both files now carry `using Source = Labs626.UrScore.Core.Source;` with the reason beside it.
- S1-9.6 **FIXED** — loading the book was quadratic, which would keep Start disabled for seconds — final fix wave (Load no longer prunes per line, src/Book/ScoreBookReader.cs) — T9 re-review, stage 1 final review Important 1

## Task 10: searchable Stats table
- S1-10.1 **FIXED** 2026-09-22 — code tidiness: the same `Show` helper now exists in 7 files — src/UI/Controls/StatsTable.xaml.cs:181 (and ImportWindow, Accounts, Clans, Recipes, ScoreBook, Stats pages) — T10 review Eight by now, not seven (`AlertsPage` had grown one). One `TextLines.ShowLine(TextBlock, string)` in `src/UI/`, taken by each file with `using static`, so a call site still reads as a bare call and the rule — an empty line takes no space — lives once. Renamed from `Show` because `ImportWindow` is a `Window`, and a window's own `Show()` hides a static import of that name; the build said so and the doc comment records it. 58 call sites, mechanical, the build and the suite as proof.
- S1-10.2 **FIXED** 2026-09-22 — code tidiness: a refused Send tick redraws the table up to three times — src/UI/Controls/StatsTable.xaml.cs:162, 167 — T10 review One redraw, not three. A refused Send tick redrew once for the tick, once when the undo's own change re-entered the handler, and once more from the undo's explicit `Refresh()`; the handler now returns at once while reverting and returns after posting the undo, so the undo's redraw is the one. Nobody could see three, but each raised `Changed`, which the host enables its Save button from. `StatsTableTests` builds the control, gives it 300 accounts so one sent stat is past the ceiling, ticks Send, pumps the dispatcher and counts: watched red at 3, green at 1. **Building the control needed the application's resources, which turned up a test-infrastructure fact worth its own line, on `UiThread`:** WPF permits one `Application` per process, ever, and the flag its constructor checks is never cleared — the suite had exactly one App-building test and could not have learned this until a second class needed one. The second class took the test host down with `Cannot create more than one System.Windows.Application instance`, and the run then printed "Passed!" with six tests fewer and "Test Run Aborted" three lines later. `UiThread.RunInApp` now builds the one application on its own STA thread with a live dispatcher, on first use, and invokes every caller onto it; `LegacyRecipeUpdateTests` goes through it too. Three consecutive full runs at 1,517, exit code checked.
- S1-10.3 **FIXED** — StatsTableModelTests cover saved counters when the recipe has no counters and when the saved name is rejected: one no-longer-offered row, unticked Show/Send, preserved label and pinned metric choice, no rule line or sent stat. Production unchanged. Original finding: T10 review.

## Task 11: Setup window and Clans
- S1-11.1 **FIXED** 2026-09-22 — code tidiness: clan search picks and name loading are fire-and-forget with no outer catch — src/UI/Setup/ClansPage.xaml.cs:33-39 — T11 review The four discarded tasks on the page — three picks and the name list — go through `Unawaited.TrailFailures(task, addTrail, what)`: a task nobody awaits, with its failure's TYPE sent to the trail and nothing more, and a cancellation saying nothing. A discarded task that faults raises nothing and reaches no handler, unlike an `async void` handler's failure, which the Dispatcher's net catches; so a bug past `PickAsync`'s own catches simply did not happen and left no sign. Three tests on the helper: a faulted task with a key in its message leaves `CLAN PICK: InvalidOperationException` and no key; an async method's fault is named by its cause and not its `AggregateException` wrapper; a cancelled or finished task leaves no line.
- S1-11.2 **FIXED** 2026-09-21 — the "Top of the battle" switch uses the first group-list recipe installed, not the page's own (matters only with two clan-type recipes) — src/UI/Setup/ClansModel.cs:149-150 — T11 review `GroupListSource` takes the page's own recipe slug and answers that recipe's source, or null when the page is not a list. It used to take the first installed group list's source whichever page asked — the same thing with one list installed, and the wrong list the moment there are two. The test installs two distinct lists with two distinct sources and asks for the second, so "first" and "the page's own" give different answers; watched red under the old rule.
- S1-11.3 (merged into S1-3.1)
- S1-11.4 **FIXED (80262f7, 0.3.3)** — the question now uses the shared themed ConfirmWindow (ClansModel.AddQuestion). Original finding: you'd notice: the "add a 6th source?" confirmation is a stock Windows message box, not themed — src/UI/Setup/ClansPage.xaml.cs:288-289 — T11 review

## Task 12: remaining Setup pages
- S1-12.1 **GONE** — the moved import flow dropped typed inputs: Task 14 removed the import screen's inputs — n/a — T12 review, T12 report
- S1-12.2 **FIXED** — two quick Send ticks could lose one — stage 1 fix wave item 9 (src/UI/Setup/AccountsPage.xaml.cs, change computed inside the deferred save) — T12 review, final review triage
- S1-12.3 **FIXED** — the Alerts card could describe a watch-only recipe as sending — Task 14 fix round (src/Core/ReportPolicies.cs, shared by AppServices and AlertsModel) — T12 review, T14 review, T14 report
- S1-12.4 **FIXED** — you'd notice: messages vanished on the next refresh: "Could not ask RoRoRo" (Your accounts), "Could not open the folder" (Score book), and the import result when Setup went on to Clans. Each is now said where no redraw writes it. Your accounts works its message line out from what the page keeps on every redraw, and says "Something went wrong while Ur Score was getting your accounts. Diagnostics has the details." (the old words named a cause the ask can't have: it never fails for RoRoRo's sake). Open folder has a line of its own, "Ur Score couldn't open the folder. Its path is above, so you can open it yourself.", cleared by the next click that works. The import's result goes with it to the Clans page, above everything else there. The board's empty-state Import recipe… had the same fault on the detail line and keeps its note until the next import or press — src/UI/Setup/AccountsPage.xaml.cs, AccountsModel.cs (`MessageLine`); ScoreBookPage.xaml(.cs), ScoreBookModel.cs (`FolderNotOpened`); RecipesPage.xaml.cs; SetupWindow.xaml.cs (`ShowPage` note); ClansPage.xaml(.cs) (`ImportedLine`); src/UI/BoardWindow.xaml.cs — T12 review, fixed 2026-09-16
- S1-12.5 **GONE** 2026-09-22 — performance: disk reads on the UI thread on every refresh (RoRoRo's rules file, the accounts cache time) — src/UI/Setup/AlertsPage.xaml.cs `Refresh` (:51, now skipped while an editor is open); ScoreBookPage.xaml.cs:36; AccountsPage.xaml.cs:35 — T12 review Measured: `RulesFile.Read` on a rules file of a dozen rules is 136 µs warm (the real file is 185 bytes), `AccountsCache.SavedAt` is 79 µs, and `Refresh` runs at most once per batch of changes — a read landing every three minutes per source, or a click. A tenth of a millisecond on the UI thread every few minutes is not a stall, and the alternative is a cache that has to notice RoRoRo rewriting the file. Real-time scanning of a file another process writes can add a few milliseconds to the open; still nothing.
- S1-12.6 **FIXED** 2026-09-21 — the Stats page asks the source for counter names again on each recipe switch or save while none are saved (spec says once) — src/UI/Setup/StatsPage.xaml.cs:92 — T12 review The unprompted read of counter names happens once per recipe per session, tracked on the page, and not again on each switch back or save while the names are still unsaved. A source that answered with none was being asked again on every visit for the same answer. The button reads again on purpose; only the read nobody pressed for is held. Page code-behind, so pinned by reading rather than by a test; the one-line guard is the whole change.
- S1-12.7 **FIXED** — you'd notice: an account found only in a watched clan read "Not in a watched clan", and the noun came from the first recipe only. Setup › Your accounts now says "Only in clans you're watching" for it, and otherwise says how much has been read ("No clans read yet", "Not found in the clans read so far") until every source has been read this session; when the recipes name their groups differently the word is "source". The words and the decision are My accounts' own (S1-13.4) — src/UI/Setup/AccountsModel.cs (`FoundIn`, `Words`); src/Board/PanelText.cs (`NotFound`, `OnlyWatched`) — T12 review, fixed 2026-09-16
- S1-12.8 **FIXED** — you'd notice: Diagnostics could still say "Reporting to RoRoRo." after you pressed Stop. Stopped, a source's state is its last read's: "Stopped. Last read: Reported to RoRoRo.", and the other states that claimed something was happening now (RoRoRo not running, nothing to read right now, reading with no stat to send) are worded as what happened — src/UI/Setup/DiagnosticsModel.cs (`Sources`, `LastReadText`) — T12 review, fixed 2026-09-16
- S1-12.9 **FIXED** 2026-09-22 — code tidiness: an unknown Setup page id shows Diagnostics, and the Stats page's recipe parameter is never passed — src/UI/SetupWindow.xaml.cs:87, 91 — T12 review The page switch is total: `Diagnostics` has its own arm and the default throws `ArgumentOutOfRangeException` naming the id, so a page added to `SetupPages.For` and forgotten here fails at first use instead of quietly showing Diagnostics. The Stats page's `recipeSlug` parameter, which nothing passed, is gone; the page's slug comes from its own dropdown as it always did in practice. **No test could be red for this one and the row says so:** every id the nav can select comes from `SetupPages.For`, and every one of those already had an arm, so the default was unreachable by construction — the change is to what happens when that stops being true.
- S1-12.10 **FIXED** — a source comment named a real account and a game — stage 1 fix wave item 6 (src/UI/Setup/AccountsModel.cs) — T12 review, final review triage
- S1-12.11 **FIXED** 2026-09-21 — test gap: Alerts tests leave temp folders behind; no tests for listed-time preference, id-0 misses, or watch-source inputs in the copy text — T12 review. All four closed, three as tests and one as a measurement. (1) **Temp folders:** already fixed, and checked rather than assumed — `%TEMP%` held 974 `urscore-*` folders before a full run and 974 after, and `AlertsModelTests` no longer touches temp at all. What remains is HISTORY: 968 of those 974 are `urscore-alerts-*` dated 14 and 15 September, from before the cleanup landed, 480 of them still holding a `file.json` fixture. Harmless and not the app's, but they are the leak this row named, still on disk; deleting them is the owner's call and nothing here has. (2) **id-0 misses:** id 0 is RoRoRo's "no Roblox id known", so it is the ABSENCE of an id and several accounts carry it at once — matching on it puts one person's name against another's miss. The fixture holds TWO id-0 accounts, because with one, dropping the filter names that one account and the test still looks reasonable. (3) **Watch-source inputs** are in the copy text like any other source's, which is how a reader sees which clan a watch is pointed at. (4) **Listed-time preference:** a cached list keeps RoRoRo's own last listing time rather than the file's, which is a different fact wearing the same clothes — the file says when WE wrote the cache, later, and reading it as a listing time overstates freshness on the one screen whose job is to say how stale the list is. The fallback was covered; the preference was not. All three mutation-tested. — tests/DiagnosticsModelTests.cs, tests/SharedAccountsTests.cs
- S1-12.12 **FIXED** — you'd notice (rare): a reload that threw after a good import said "Could not save that recipe" about a recipe that was saved. Saving and loading now fail on their own words: "{name} was saved, but Ur Score couldn't load it. Restart Ur Score to load it.", with the exception's type in the trail — src/UI/Setup/ImportFlow.cs (`SaveAndLoad`) — T12 review, fixed 2026-09-16

## Task 13: the ten panels
- S1-13.1 **FIXED** — stale panels had no "Choose another" button — stage 2 Task 6, 028e24d (`ChooseAnotherButton`) — T13 review
- S1-13.2 **FIXED** — Records showed "+-1.5K" for a fall — stage 1 fix wave item 4 (`PanelText.Signed`) — T13 review, final review Minor 5
- S1-13.3 **FIXED** — Ctrl+C on the live leaderboard copied other players' names — stage 1 fix wave item 5 (src/UI/Panels/LiveLeaderboardPanel.xaml, `ClipboardCopyMode="None"`) — T13 review, final review Minor 6
- S1-13.4 **FIXED** — you'd notice: before the first read, My accounts listed everyone under "Not in a watched clan", and so were accounts seen only by a watched clan. Each leftover account is now headed by what is known of it: "No clans read yet", "Not found in the clans read so far", "Only in clans you're watching", "Not matched by RoRoRo yet", and "Not in a watched clan" only once every clan of the recipe has been read this session (a remembered reading never counts: the book keeps only the accounts it kept) — src/Board/PanelModels.cs (`MyAccounts`, `Leftovers`); src/Board/PanelText.cs (`NotFound`) — T13 review, fixed 2026-09-16
- S1-13.5 **FIXED** — you'd notice: the gap to the group above didn't show when ranks were tied (12, 12, 14). The group above is now the nearest one ranked higher, and the gap shows when the list holds every group ranked between: 14th measures to 12th, each 12th to 11th, and 12, 12, 15 or 12, 14 still shows none — src/Board/PanelModels.cs (`Gap`) — T13 review, fixed 2026-09-16
- S1-13.6 **FIXED** — you'd notice: Race said "This panel's clan was removed." when the recipe simply had no summed total, silently dropped removed clans, and didn't enforce 2 to 5. It now says "This panel's recipe has no total to race."; draws the lines it still has with "One of this race's clans was removed." (or "2 of this race's clans were removed."); draws the first 5 of more with "Only the first 5 clans are drawn."; and says "A race needs at least 2 clans." beside a single line, and in place of the chart when it names none. Every line gone is still "This panel's clan was removed." — src/Board/PanelModels.cs (`Race`); src/Board/PanelText.cs — T13 review, fixed 2026-09-16
- S1-13.7 **FIXED** — you'd notice: Profile stat quietly fell back to another source when its own was removed, and its row note said "can't read" instead of the real reason. Falling back was wrong for a pinned source: one that is gone is now stale ("This panel's source was removed.") and nothing else is drawn under its settings; only an unpinned panel reads the recipe's first source (D17), which is what it was asked for. A row carries the read's own miss, and a source that is off says "{name} is switched off, so it isn't read." — src/Board/PanelModels.cs (`ProfileStat`) — T13 review, fixed 2026-09-16
- S1-13.8 **FIXED** — you'd notice: an Account card whose chosen account had no reading said "No reading of your accounts yet." A card pinned or picked to one account now names it and gives the read's own reason when there is one, else "No reading of {name} yet."; a pinned account RoRoRo isn't listing says "RoRoRo isn't listing this panel's account right now."; "No reading of your accounts yet." is left to a card that chose none — src/Board/PanelModels.cs (`AccountCard`) — T13 review, fixed 2026-09-16
- S1-13.9 **FIXED** — code tidiness: Past periods regrouped finals the reader had already grouped; the regroup is gone and the panel now orders the reader's entries itself — src/Board/PanelModels.cs (`PastPeriods`) — T13 review, fixed 2026-09-16
- S1-13.10 **FIXED** 2026-09-22 — code tidiness: your account-id set is rebuilt on every access — src/Board/PanelModels.cs:47 — T13 review One set per board. `LiveBoard.MyUserIds` built a fresh hash set on every access, and two of its ten call sites sit inside per-row lambdas — "is this row one of mine?" — so a fifty-row leaderboard built fifty sets to draw itself. Cached against the account list it was built from rather than in a plain field, because a record's `with` copies every field and a copy given other accounts must answer for those; the benign race (two threads build the same set, one wins) is stated on the property. Test pins the identity (`Assert.Same` on two reads, watched red) and the `with` hazard (a copy with one account answers with one id while the original still answers with four).
- S1-13.11 **FIXED** — you'd notice: a chart whose values were all equal drew a flat line at the bottom of a made-up axis, with no tests for equal or negative values. Equal values now draw through the middle with one grid line naming the value. A chart drawn from zero keeps zero on its axis at either end, so all-negative values sit below a zero line; equal values from zero still have their real zero-to-value range. Tests cover equal, single, all-zero, negative and across-zero values — src/Board/ChartGeometry.cs (`Layout`) — T13 review, fixed 2026-09-16
- S1-13.12 **FIXED** — Standing panel tests now assert literal change/period text, direction, gap text/fill (including tied ranks), and overdue state instead of computing expectations with production helpers. Renamed `withGap` to `withMissingRank`: it correctly excludes a nonadjacent rank; the positive gap case was already present. Production unchanged. Original finding: T13 review.
- S1-13.13 **FIXED** 2026-09-21 — code tidiness: an unknown panel type silently becomes a Live leaderboard — src/UI/Panels/PanelViews.cs, tests/PanelViewsTests.cs — T13 review. `LiveLeaderboard` is now its own arm and the default throws. It cannot be reached from disk (`BoardsFile` rejects an unknown name with `Enum.IsDefined`), so the only route is a new enum member nobody wired up — which used to draw a leaderboard on somebody's board: a wrong panel that looks like a working one. `PanelViewsTests` walks every enum value so the throw lands in the suite instead. **It asks WHICH exception rather than constructing the panels, and that is the only reading available:** building a real panel runs its XAML, which wants `App.xaml`'s brushes, and a unit test has no `Application` to have loaded them, so every MAPPED type throws `XamlParseException` here and only an unmapped one reaches the default. The two are never confusable. What it does not check, and the test says so rather than leaving it to be assumed: that two types do not map to the same view, which needs the constructed controls and so belongs to a smoke walk. Both mutation-tested — restoring the fallback fails the throw test, deleting a mapped arm fails the walk and names it ("no view is built for: Pace").
- S1-13.14 **FIXED** — you'd notice: Standing's change was always cyan, even when the clan went down. It is cyan only for a rise, magenta for a fall, and the text colour for no change or none known, from the same movement its words come from. The words carry the sign ("+1M in 30m", "-1M in 30m"), so colour is never the only signal — src/UI/Panels/StandingPanel.xaml; src/Book/Records.cs (`Direction`); src/Board/PanelModels.cs (`StandingModel.ChangeDirection`) — T13 review, fixed 2026-09-16
- S1-13.15 **FIXED** 2026-09-22 — code tidiness: PanelModels.cs is 761 lines of records and builders; worth splitting — src/Board/PanelModels.cs — T13 review 1,390 lines by the time it was done, not 761. Split by panel type into twelve `partial class` files — `PanelModels.Race.cs`, `PanelModels.Top.cs` and so on, the shape `BoardWindow` already takes — each holding its panel's records and builder, with the shared helpers and `LiveBoard` in `PanelModels.cs`; the largest is now 279 lines. Every private helper went with its only caller and the ones used by several stayed shared, by a count of call sites rather than a guess. Mechanical: a script cut the file by member and asserted every member was assigned exactly once, and a check afterwards found every non-boilerplate line of the original in exactly one new file, once. No caller changed — the class name, every signature and every namespace are as they were — and the suite is the proof, 1,523 green.
- S1-13.16 **FIXED** — Profile stat tests assert literal "no earlier read" in both windows with zero or one reading; UTC-5 and UTC+9 cases distinguish local midnight from UTC and verify exact-midnight inclusion and the rolling seven-day boundary. FixedTime accepts an optional zone, retaining UTC by default. Production unchanged; DST transitions are not covered by these fixed-offset cases. Original finding: T13 re-review.

## Task 14: board window and AppServices
- S1-14.1 **FIXED** — you'd notice: when a recipe update dropped its icon, the old icon stayed on the window. A source keeps a picture only while its recipe names an icon: every change to sources or recipes forgets the rest, in memory and in the saved `source-icons.json`, and the window, a Standing panel and a recipe's row each check the recipe as well, so an icon the recipe dropped is never drawn. Not while `sources.json` couldn't be read, when there are no sources to judge by — src/Composition/AppServices.cs (`ApplySources`), src/Source/SourceIcons.cs (`Keep`), src/Board/IconChoice.cs (`SourcesWithIcons`), src/Board/PanelModels.cs (`LiveBoard.IconFor`) — T14 review, fixed 2026-09-16
- S1-14.2 **FIXED** — you'd notice (rare): a score book that failed to load left Start and Test now off for the session, with an exception's own message as the only reason and no retry. The state line says "Your score book could not be read.", the detail line says why in plain words (another program has a book file open, Windows didn't let Ur Score read its folder, Windows' own sentence for another disk fault, else "Something unexpected went wrong. Setup › Diagnostics has the details."), and every board shows "Your score book couldn't be read" / "Start and Test now stay off until Ur Score can read it. Why it couldn't is on the line above." with **Try again**, which reads the book again and then does what opening does. The exception goes to the trail — src/UI/BoardWindow.xaml.cs (`OpenOnTheBookAsync`, `ReadBookAsync`); src/UI/BoardText.cs (`BookUnread`, `EmptyFor`, `EmptyState`) — T14 review, final review triage, fixed 2026-09-16
- S1-14.3 **FIXED** — you'd notice: Start could wait up to 20 s for RoRoRo's accounts with nothing on screen saying so. The state line says "Starting. Asking RoRoRo for your accounts…" for as long as it waits. It is worked out on every redraw from what the window is doing, so the 20 s clock can't put "Not started." back mid-wait; Test now's "Reading every source once…" had the same fault and is fixed the same way, and so is "Something unexpected went wrong.", which the press's own redraw used to wipe at once — src/UI/BoardText.cs (`BoardActivity`, `StateLine`); src/UI/BoardWindow.xaml.cs — T14 review, fixed 2026-09-16
- S1-14.4 **FIXED** — Start/Stop presses were silently ignored during Test now — live-walk fix (src/UI/BoardButtons.cs, `BoardButtons.For` + `ApplyButtons`) — T14 review
- S1-14.5 **FIXED** — you'd notice: while stopped, the top line only ever said "Not started." or "Stopped.", so a Test now result never showed there. After a read you ask for while stopped (Test now, or Setup reading a source once) it says what that read found: the source in trouble by name ("Not started. Last read of K0i2: Could not reach the data."), else what every source found ("Stopped. Last read: Reported to RoRoRo."), else how many answered ("Last read: 2 sources answered."). Pressing Stop still says "Stopped." alone — src/UI/BoardText.cs (`LastReadNews`); src/Composition/AppServices.cs (`AskedReadAt`, `StoppedAt`); src/UI/Setup/DiagnosticsModel.cs (`PastStateText`) — T14 review, fixed 2026-09-16
- S1-14.6 **FIXED** 2026-09-22 — code tidiness: async paths with no catch (empty-state import, name resolving, icon fetch) — src/UI/BoardWindow.xaml.cs:554-584, 165; src/Composition/AppServices.cs:631 — T14 review The two discarded tasks that had no catch of their own — name resolving on the board window, the icon apply in the composition root — take the same `Unawaited.TrailFailures` as S1-11.1. The empty-state import the row also named is an `async void` handler, and those already reach the Dispatcher's net (`App.OnUnhandled`, type to the trail) — the hole was only ever the `_ = SomethingAsync()` shape. The remaining discarded tasks (`OpenOnLastNumbersAsync`, `FollowThemeAsync`, `AskForAvatarsAsync`, the pages' own) each carry a catch-all inside and were left as they are.
- S1-14.7 **FIXED** 2026-09-21 — an icon fetch that throws is never retried until the icon text changes — src/Composition/AppServices.cs:695 — T14 review A fetch that THROWS is asked again on the next read; a fetch that answers "no picture" is not, and that distinction is the whole fix. The two were treated alike, so a network blip on a session's first read cost a source its picture until its icon text changed — which for a clan is never. "No picture" is an answer and retrying it every read would be a burst for nothing; an exception is not an answer, and the text is still unknown. The same rule the cancellation branch already applied, now applied to the failure it was written next to. The ask is forgotten at the end of the attempt, after the cache fallback has been applied, which is why it is not in the catch. Test covers both halves with the "no picture" case as the control; watched red.
- S1-14.8 **FIXED** 2026-09-21 — changing only which stats you track releases a held "key rejected / sign in" stop, costing one extra rejected request — src/Composition/AppServices.cs:539-542 — T14 review One line. `UpdateRecipe` cleared the held stop on every call; it now clears it only for a change the site could answer differently to — another recipe, other inputs, new recipe text — and not for a change to which stats are tracked, which the site never sees. Watched red (engine called twice) then green (once).
- S1-14.9 **FIXED** 2026-09-22 — test gap: AppServices and broader App startup lack dedicated coverage; the --try/single-instance boundary now has a subprocess test under S1-15.2, but service composition and normal UI startup remain uncovered — src/Composition/AppServices.cs; src/App.xaml.cs — T14 review, T14 report, T14 re-review 2 **The seam, then the tests.** `AppPaths` is one record naming every file's place under one root, and the nine `DefaultPath`s that each worked the root out for themselves now read `AppPaths.Default` (a test pins that they agree). `AppServices` has a second constructor with its seams open — the paths, an `IHostClient`, an `IRecipeTransport`, a `TimeProvider`, the rules path — and the app's own constructor is that one with the real things filled in. `AppCompositionTests` then composes the whole app against a folder of its own: an empty folder gives nothing installed, the starters following, a book that loads empty, and not one file outside the folder; a recipe and a source on disk compose to a watch, and a Test-now read goes the whole way through the real engine, policy, book and reader — fake transport answering the recipe's two steps, stub host listing one account — to a line on disk with your account in it and the stranger's out, a snapshot in `Latest`, a starter board naming the source, and nothing reported. A wrong path in the composition (sources read from the boards file) fails it by name. **Two things stated plainly:** a test that leaves the host or transport null has built the app for real, indistinguishable from the installed plugin to RoRoRo (V3-S.43), so none does and the file says so at the top; and the reader's word from the book goes through the window's dispatcher, which a test does not pump, so that last hop is watched by the walks and not by this. Normal UI startup (`App.OnStartup`, the window) stays with the smoke walks, where it belongs.
- S1-14.10 **FIXED** — you'd notice: a cancelled import left the Recipes status line blank, because the wait for RoRoRo wrote over it. A cancel now leaves both of the page's lines as they were before the import started — src/UI/Setup/ImportFlow.cs (`LinesAfter`); src/UI/Setup/RecipesPage.xaml.cs — T14 review, T14 report, fixed 2026-09-16
- S1-14.11 **GONE** — panels in one row keep their own height instead of stretching to the row, so bottoms are ragged — src/UI/Controls/PanelGrid.cs:65-66 — T14 review, T14 report. **GONE (superseded, recorded 2026-09-20)** — this was built. D7 of the default-views plan arranged a one-row panel at its row's full height, naming this row as its source. Task 8 then reversed it on the evidence this project trusts: screenshots beside the mock showed the first row lopsided and the race below the fold, so panels went to natural heights, top-aligned, and `BoardLayout.ArrangedHeight` was written to hold that. The design doc carries a "Changed during execution (2026-09-15)" banner saying so. Building this row would undo a screenshot-backed decision, so it is closed rather than left for someone reading the row alone to pick up a third time. Owner confirmed, 2026-09-20.
- S1-14.12 **FIXED** 2026-09-22 — code tidiness: the icon client and the closing token are never disposed — src/Composition/AppServices.cs:451-467 — T14 review `IconClient` owns an `HttpClient` and is now `IDisposable`, disposed from `AppServices.Dispose` beside the two HTTP clients that class builds itself. `_closing` is cancelled there and deliberately NOT disposed, with the reason on the line: its token has been handed to work that may still be unwinding when the window closes, and `.Token` on a disposed source throws, so a cancelled-but-undisposed source is the safer end for a token that lives as long as the process. Half the row done and half ruled against, each with its why.
- S1-14.13 **FIXED** 2026-09-21 — a newly listed account's policy refresh can land after a fresher one, briefly allowing a just-excluded stat to your local RoRoRo (self-corrects) — src/Composition/AppServices.cs:551 — T14 re-review 1, final review triage One gate, `_refreshGate`, held while a refresh reads what is installed and hands each watch its policy, and while a load replaces what is installed. The race was between RoRoRo listing accounts on a fetching thread — which refreshes every policy from there, deliberately, so the new account is sent on that very read (F8) — and the user unticking a stat on the UI thread, which refreshes from a newer state. The background one could read the old state, be overtaken, and land its old policy on top of the new; a stat the user had just excluded went out once more. Under one gate the two cannot interleave. **Not covered by a test, and said so:** `AppServices` builds from the real data paths and has no seam a test can drive two interleaved refreshes through; S1-14.9 in Sweep E is the row for that seam, and this is the first case to cover once it exists. The suite passes twice over with the lock in place, which says it deadlocks nothing that runs today and nothing more.
- S1-14.14 **FIXED** 2026-09-22 — privacy tidiness: some trail lines still carry full exception text or messages instead of the type — src/UI/BoardWindow.xaml.cs:112, 614; src/Composition/AppServices.cs:664, 683 — T14 re-review 1, stage 1 final fix report The four lines that still wrote more than the type — `EXCEPTION: {ex}` and `BOOK NOT LOADED: {ex}` in the board window, `COUNTER NAMES NOT SAVED` and `BUDGET NOT CHECKED` with `{ex.Message}` in the composition root — now write `ex.GetType().Name` like the other twenty. And a fence, `TrailFenceTests`, so a fifth is visible: it scans every `AddTrail` call in `src/` for `{ex}`, `{ex.Message}` or `{ex.ToString()}`, with a theory pinning that the pattern sees those three shapes and lets a type name and a `Redactor.Redact` screen line through. Watched red on exactly the four.
- S1-14.15 **GONE** 2026-09-21 — not a defect on this platform, checked rather than reasoned: a sources.json you're denied read access to looks "missing", so start could migrate and save over it — src/Core/Sources.cs — T14 re-review 1, final review triage. The fear was that `File.Exists` answers false for a denied file, and false is the one answer that does damage — start reads it as "migrate and save" and would write over the user's clans. **Tested with a real ACL deny rule on the file for the current account, first `Read` and then `FullControl`: `File.Exists` answered TRUE both times** on .NET 10 / Windows 11, so the old code reached `ReadAllText`, caught `UnauthorizedAccessException`, and reported exists-but-unreadable, on which start holds and says so. The scenario could not happen. The loader was rewritten anyway to learn existence from the open itself rather than from `File.Exists` — not-found means missing, anything else that stops the read means unreadable — because that shape cannot be fooled by any platform's answer to `File.Exists`, nor by the file vanishing between an exists check and the read. `AFileYouAreDeniedIsThereButUnreadableNotMissing` pins the answer that matters, with the deny put back in a `finally`.
- S1-14.16 **FIXED** 2026-09-22 — code tidiness: a dead `_services is null` check in the startup guard — src/App.xaml.cs:76 — T14 re-review 2 The two fields became one. `_started` (a bool set after `Show`) and `_services` (set before it) guarded the net with `!_started || _services is null`, and the second half could never be true once the first was false — a check that reads as a case and is not one. `_shown` now holds the services of a board that is SHOWN and is null until then, so the guard is one pattern match and the compiler sees the proof itself. `_services` stays for exit, which disposes a build that never got as far as showing. WPF application class, so pinned by reading.
- S1-14.17 **GONE** 2026-09-22 — code tidiness: an unreachable catch around the accounts refresh — src/UI/Setup/AccountsPage.xaml.cs:56 — T14 report The catch is reachable and is the right shape. `RefreshAccountsAsync` catches RoRoRo's failures itself and answers with the saved list, so nothing RoRoRo does reaches the page's catch — which is what the comment beside it already said. What does reach it is a bug in what Ur Score does with the answer afterwards (`RefreshPolicies`, `AskForAvatars`), and for a task the constructor starts and does not await, a catch that puts the type on the trail and "accounts not updated" on the page is exactly the net S1-11.1 and S1-14.6 add everywhere else. Removing it would open the hole those two rows close.

## Task 15: try-out command
- S1-15.1 **FIXED** — the TryCommandTests JSON test now runs with and without --account, excludes all three foreign IDs from current and past periods, verifies period/count output, and requires either an empty account list or exactly the selected account with its value and rank. Without --account, that ID is absent too. Production unchanged. Original finding: T15 review.
- S1-15.2 **FIXED** — TryCommandTests launches the built application with an existing single-instance mutex and malformed --try arguments, requiring CLI usage and exit code 5 rather than the duplicate-instance exit code 0. The subprocess has a timeout and cleanup; no recipe/network read or normal UI startup is needed. Production unchanged. Original finding: T15 review.
- S1-15.3 **GONE** — the `--try` KeyStore isn't disposed: KeyStore holds nothing disposable — src/Recipes/KeyStore.cs:29 — T15 review
- S1-15.4 **OPEN** — test gap: `--try` was only run live against an idle clan, never a real battle read — src/Cli/TryCommand.cs — T15 report

## Task 16: smoke scripts
- (co-author line merged into S1-2.3)
- S1-16.1 **OPEN** — if a smoke walk is killed mid-run, the next run doesn't notice your data still sitting in a `.smoke-backup-*` folder (the README says to rename it back) — tools/smoke/uia.ps1:231 — T16 review

## Stage 1 final review, re-review and fix report
- S1-F.1 **FIXED** — Diagnostics and copied diagnostics name each own account skipped in the last read and the source that held its claim. The owner is captured atomically at rejection; wording is historical, not a claim of present ownership, membership or successful sending. Expiry/transfer and reporting policy are unchanged. Misleading no-account wording is corrected for conflicts; foreign and removed-account evidence is filtered. Roster grouping remains V3-S.20; live ownership queries are not part of this bounded fix. 46 focused/1,281 full tests and Release warnings-as-errors build pass; desktop wrapping/clipboard smoke pending — src/Core/AccountClaims.cs; src/Core/RecipeWatch.cs; src/UI/Setup/DiagnosticsModel.cs; src/UI/Setup/DiagnosticsPage.xaml — final review Minor 8, fixed 2026-09-17
- S1-F.2 **FIXED** 2026-09-22 — performance: startup reads the whole book twice (finals index, then the reader) — src/Composition/AppServices.cs:263-264 — final review Minor 9, parked One pass. `ScoreBookReader.Load` takes the `FinalsIndex` and feeds it from the same walk of the files; `AppServices` builds the index and hands it over instead of loading it first on its own. On the season's book (S1-F.3's instrument): 2,246 ms for the two passes before, 1,540 ms for the one pass after, both cold. Not the half one might expect, because the second pass used to run on a warmed JIT and the reader's pass is the heavier one (it keeps what it reads; the index keeps a few hundred tuples). Test loads a book with a final in it through the one call and checks the index and the reader both answer for it.
- S1-F.3 **FIXED** 2026-09-22 — performance: each chart scans every kept reading of every clan, and five weeks of book may hold 100-170 MB — src/Book/ScoreBookReader.cs:104-125 — final review Minor 10, parked Measured, then fixed, then measured. The instrument is `BookGenerator` + `BookBenchmarks` (run by `tools/bench/book-bench.ps1`): a synthetic book of a season's shape and size — five clan sources and a clans list, five weeks, a reading every three minutes, 100,846 lines, 60.5 MB — written the way the app writes one, no player in it. BEFORE: one board's worth of chart queries (30 series, 43,230 points) took 68 ms, because every query scanned every kept reading of every source and then sorted what matched. AFTER: 11 ms. The reader keeps readings by source id, each list in time order, inserted in place when a line arrives out of order (a merge, or `Apply`, which promises no order) and appended otherwise; a query reads its own source's list and sorts nothing. Three existing tests catch a plain append in place of the ordered insert, by name. A five-week book was not needed to get here — a book of that shape was, and it takes four seconds to make.
- S1-F.4 **FIXED** 2026-09-21 — the recipe text file isn't written atomically, and a torn write is never repaired — src/Book/ScoreBook.cs — final review Minor 11, parked. Through a temp file and one move now, like `sources.json` and `boards.json`. The second half of the row is why the first mattered: the file's name carries the recipe's hash, so once a torn one exists it is never rewritten, and a half-file sits beside a whole book forever. The existing recipe-file test also asserts no `.tmp` is left behind.
- S1-F.5 **FIXED** — you'd notice: the SENT dot meant "sent at some point this session", not "sent this read". It now means that account's number for that stat went to RoRoRo in its source's last read: a read with RoRoRo closed, the account's Send off, a stopped read, or numbers from the score book show no dot. The panel's legend says "● sent to RoRoRo in the last read" — src/Core/RecipeWatch.cs (`RecipeSnapshot.SentThisRead`); src/Board/PanelModels.cs (`MyAccounts`, `SentLegend`) — final review Minor 12, fixed 2026-09-16
- S1-F.6 **FIXED (7ee4afd)** — you'd notice: a read that stops (no battle, an error) blanked the panels until the next good read. A stopped read carries no rows, headline or groups, so it replaces nothing: every panel that draws your own numbers or a source's headline keeps what the score book kept from the last good read, marked remembered, and the state line says how old. Verified 2026-09-16 with an idle read (`RememberedTests.AReadThatStopsForNoBattleKeepsTheNumbersTheLastGoodReadKept`, which fails with 7ee4afd's `SnapshotOf` reverted). Still empty after a stopped read, by design: Live leaderboard, Top and Promotion check, which show other members the book never keeps (A40); since 2026-09-16 they say "The last read brought nothing back." (Promotion check: "The last read of {name} brought nothing back.") instead of nothing or "Waiting for the first read." — src/Board/PanelModels.cs (`LiveBoard.SnapshotOf`; `Top`, `LiveLeaderboard`, `PromotionCheck`); src/Board/PanelText.cs (`NothingBack`) — final review Minor 13
- S1-F.7 **FIXED** — a stale "Task 15 adds the --try branch HERE" comment — stage 1 fix wave item 7 (src/App.xaml.cs) — final review Minor 14
- S1-F.8 **FIXED** — the import screen's "Kept in your score book" list now includes the period identity, declared start/end times, and source freshness with optional stale status alongside headline items. Recipes without headlines still disclose their metadata; group lists still keep nothing. ImportTextTests covers optional fields and both list/per-account freshness. Original finding: final review Minor 15.
- S1-F.9 **FIXED** 2026-09-22 — code tidiness: the account-list fallback catches every exception, so a real bug would look like "RoRoRo not answering" — src/Core/SharedAccounts.cs:92 — final re-review The fallback catches the transport's failures and no other: `RpcException` from the gRPC call and `IOException` from the pipe. It caught everything, so a bug in the client — an `InvalidOperationException`, say — was answered with the saved accounts and a cached-list note and looked exactly like RoRoRo being closed. Anything else now leaves as the exception it is, lists nothing, raises no `Listed`, and reaches the read's own "last read failed" snapshot, where its type is named. Test throws an `InvalidOperationException` from the listing and expects it back with `Last` still null; the transport case beside it is the control. Watched red (no exception thrown) then green.
- S1-F.10 **FIXED** 2026-09-21 — a book line that still fails to load is skipped with no trail note — src/Book/ScoreBookReader.cs, src/Book/Finals.cs, src/Composition/AppServices.cs — final re-review, parked. Both loaders count what they skip, `ScoreBookReader.Load` returns it and `FinalsIndex.Skipped` carries it, and the composition root adds one trail line when the count is above zero — a count and nothing else, since a corrupt line's text could hold anything and Diagnostics is copied and pasted. **Worth knowing before anyone waits for that line to appear:** it will be rare by construction. Everything that reaches those catches has already passed `BookJson.TryParse`, and both `Add`s are defensive (`long.TryParse`, null checks), so the catches are backstops for a failure nobody has produced — a line that throws in `Add` could not be built through the public path for the test. Counting a backstop is still right, because a silent backstop is the same fault V3-S.35 fixed; only the clean case (zero skipped from both loaders) is pinned, and the test says why.
- S1-F.11 **FIXED** 2026-09-22 — before the repo goes public: tests use real-looking account names (estehernandez, ItsJustEste...) and a possibly real Roblox id, test comments say "Pet Sim", and src comments use "CCGP" and "ps99.diamonds" as examples — tests/BoardFixtures.cs; tests/ScoreBookTests.cs:21; tests/RecipeEngineTests.cs:563; src/Board/PanelModels.cs:61; src/Recipes/RecipeStore.cs:9 — stage 1 final fix report The part that identifies a person is renamed; the part that is game data is not, by the owner's 2026-09-20 ruling, and the line between them is drawn here. RENAMED, across eleven test files: the four account display names, which were the owner's own Roblox handles, are now `BirchMain`, `AshAlt`, `DuneAlt` and `CedarLoose` — chosen to keep the case-insensitive order the old names had, since three tests assert rows sorted by name — and the ten-digit user id in the score-book and `--try` tests, which was plausibly real, is `101` like the other fixtures. LEFT ALONE, and why: clan names (`CCGP`, `K0i2`, `NovaForge`) are game data the owner ruled may be kept; the game's name is in the shipped recipes' own file names; `ps99.diamonds` is a metric id, not a person. **Still the owner's call:** seven historical documents under `docs/` (the 2026-09-12 plan, the score-book design and stage plans, the alerts card, the remediation plan) carry the same four handles. Yesterday's ruling was that plan documents are records and are not rewritten; a privacy rename before going public is a different question, so it is asked rather than answered. Suite green at 1,523 without a body changed.

## Stage 1 live walk (fix review and report)
- S1-L.1 **OPEN** — test gap: the starter-board walk throws away whether Test now ever went disabled, so a broken button could still pass step 2c — tools/smoke/walk-starter-board.ps1:78 — live-fix review
- S1-L.2 **FIXED** 2026-09-22 — test gap: the RowList automation test was never run against the original bug's shape, so it may not catch the same bug by another route — tests/RowListAutomationTests.cs:66-67 — live-fix review Run against the bug's own shape at last: the same rows in a stock `ItemsControl`, in the same test file. What this WPF gives for one is not quite what the fence's first sentence said — the row's buttons ARE in the control view, nested under an item peer named by the record's `ToString` — and the RowList test discriminates on exactly that: it wants the buttons as direct children with no item peer between and no record name anywhere, and against the stock control all three of those assertions fail. So the test can catch the bug coming back by another route, which was the question. The fence's wording is corrected to what was seen.
- S1-L.3 **OPEN** — test gap: walk-score-book still sleeps a fixed 20 s after Test now and can race a slow read — tools/smoke/walk-score-book.ps1:21 — live-fix report
- S1-L.4 **FIXED** 2026-09-22 — test gap: ListBox lists (Setup nav, search matches, stats rows) aren't covered by the RowList fence — tests/RowListFenceTests.cs — live-fix report Probed first, and the probe found a real thing: a `ListBox` item is in the control view with its controls — it is not an `ItemsControl` in that respect — but it is NAMED by its record's `ToString` unless the container style names it, so a screen reader reading the Stats table said "Labs626.UrScore.UI.StatRow" for every row. Three of the four ListBoxes now name their items in the container style (the stats row by its label, Setup's page list by its title, the search matches by their text; the board tabs already did), and the fence has a second rule: every `<ListBox>` in `src/UI` sets `AutomationProperties.Name` in its item container style, inline or by a keyed style in the same file. Watched red on exactly the three. `ListBoxAutomationTests` pins both halves in the automation tree: unnamed reads as the record, named reads as the binding.
- S1-L.5 **OPEN** — accessibility: lists expose their buttons and text directly, which strict UI Automation checkers may flag (Narrator reads them fine) — src/UI/Controls/RowList.cs — live-fix report

---

# Stage 2 (v0.3.0)

## Pre-flight scan (NOTE rows)
- S2-P.1 **FIXED** — pop-out titles took the first recipe's words, not the panel's — e942e1d (`PanelGallery.TitleOf`) — pre-flight P15, final review Minor 1
- S2-P.2 **FIXED** — moving a borderless pop-out through UI Automation was unverified — walk-pop-outs passed 14/14 live (plan execution record) — pre-flight P25
- S2-P.3 **FIXED** — the grip is now a non-focusable Label with an explicit DragHandle automation id. DragHandleTests loads the actual XAML and verifies its LabelAutomationPeer id/name, preserved mouse-handler wiring, and themed geometry. Desktop dragging remains a smoke check. Original finding: pre-flight P27, T7 review.
- S2-P.4 **GONE** 2026-09-21 — plan text: Task 3 Step 2 expects CS0117 for a missing instance member; it is CS1061 — docs/plans/2026-09-14-score-book-stage-2.md:1268 — pre-flight T3 row Owner's ruling 2026-09-21: the stage 2 plan is a record of what was planned, not a living spec, and correcting its text now would rewrite what the record says was believed at the time. The code and tests are the truth; the plan stays as written.
- S2-P.5 **FIXED** — the gallery's Race card counts only enabled sources, matching the form's defaults. It still needs at least two sources of one recipe with a total to race. Tests cover both clans off, either clan off, and both on, including the refusal text — src/Board/PanelGallery.cs (`CanAdd`), tests/PanelGalleryTests.cs (`RaceNeedsTwoEnabledClans`) — pre-flight T4 row, fixed 2026-09-16
- S2-P.6 **FIXED** — the race checkbox problem line could lag one click — 028e24d (`OnRaceChanged` copies the tick before checking) — pre-flight T6 row
- S2-P.7 **GONE** 2026-09-21 — plan text: Task 9's Consumes list omits `Get-SetupWindow` and `Get-UrProcessId` — docs/plans/2026-09-14-score-book-stage-2.md:4914 — pre-flight T9 row Owner's ruling 2026-09-21: the stage 2 plan is a record of what was planned, not a living spec, and correcting its text now would rewrite what the record says was believed at the time. The code and tests are the truth; the plan stays as written.
- S2-P.8 **FIXED** — `StarterBoard.Key` and `AnchorSourceId` were read only by tests — a3c5296 — pre-flight R11, final review Minor 5
- S2-P.9 **GONE** 2026-09-21 — privacy edge: an unreadable boards.json is copied verbatim, so a hand-typed stranger's id would sit in the kept copy (it's your own text) — src/Board/BoardsFile.cs:133-134 — pre-flight D8 Owner's ruling 2026-09-21: the kept copy is the user's own typed text, kept precisely so they can recover it, and scrubbing it would defeat that. A stranger's id would have to have been typed by hand into a file the app never edits by hand.
- S2-P.10 **FIXED** — saving boards re-derived your ids instead of sharing the panels' helper — 0e21742 (`PanelModels.UserIdsOf`) — pre-flight D9, final review Minor 8
- S2-P.11 **FIXED** 2026-09-21 — test gap: no fence test that only AppServices writes boards.json, always through Sanitize — tests/BoardsFileFenceTests.cs — pre-flight D10, final review recommendation 2. R17 keeps an account id only when it is one of yours and `BoardDefs.Sanitize` is the whole of that rule, which is worth something only while there is one writer: a second one added later fails no test and does not look wrong in review. The fence asserts the ARGUMENT and not merely both names in the same file — `AppServices` mentions `Sanitize` AND calls `Save`, so a "contains both" test would still pass with the unsanitized boards written, which is the exact regression it exists to catch (V3-S.37 rule 2). Mutation-tested by adding a second writer in `Following`. Says plainly what it buys, as `ReportPolicy` does: it catches an accident, not a determined author, because reflection defeats a source scan.
- S2-P.12 **FIXED** — a literal "5" in the Race card — src/Board/PanelGallery.cs:46 uses `PanelModels.MaxRace` — pre-flight D11
- S2-P.13 **FIXED** 2026-09-22 — code tidiness: the MainClan/AltClan/Rival fixtures are redefined in 5 test classes — tests/BoardTextTests.cs:11 (and 4 more) — pre-flight D12 Eight classes by now, not five. `MainClan`, `AltClan`, `Rival` and `SecondAltClan` live once in `BoardFixtures`, which every one of those classes already took with `using static`, so removing the private copies changed no test body. `RaceBoardTests` keeps its own `Rival` (a different clan, with a comment saying why) and is left alone. Turned up because the new S1-13.10 test wanted `MainClan` in a class that had never declared one.
- S2-P.14 **FIXED** — moved AddBoardWindow's unchanged naming decision to BoardEdits.NewBoardName; the window forwards the typed, suggested and starter names. Thirteen cases cover blank/null text, empty-board fallback, untouched starter suggestions, ordinal comparison, custom-name trimming and the existing 40-character limit. Pop-out and form fallbacks were already tested. No UI or persistence behavior change; live dialog smoke not performed. Original finding: pre-flight D13.
- S2-P.15 **GONE** — ⋯ changes to a popped-out panel in edit mode wouldn't show until Done: a popped-out slot now ignores every tool but Remove — src/UI/BoardWindow.PopOuts.cs:112-120 — pre-flight D14
- S2-P.16 **FIXED** 2026-09-21 — test gap: pop-outs from two different boards can share one automation id — src/UI/BoardWindow.PopOuts.cs — pre-flight D15. **Confirmed 2026-09-21 and it is a defect, not only a test gap.** `BoardEdits.AutomationIds` numbers panels within ONE board, so the first Standing panel on any board is `StandingPanel1`; `AutomationIdIn` then names a pop-out WINDOW from that. Two boards each with a Standing panel, both popped out, give two live top-level windows carrying the same id — which breaks `Get-PopOutFor` in every walk that uses it, and is wrong for a screen reader besides. **The cost the row does not mention, and why it stopped here:** every scheme that makes the id unique changes the ids the smoke walks hard-code (`StandingPanel1`, `RecordsPanel1`, and the `...PoppedOut` slots derived from them), so the fix lands in `tools/smoke/` as well and every walk needs re-running. That is the owner's call rather than a sweep's. **FIXED 2026-09-21 on the owner's scheme** ("yes, with the scheme"): the slot id on the BOARD is unchanged, since a board shows one board at a time, and a panel inside its own pop-out window is named `<boardId>/<slotId>` — `b-starter-battle/StandingPanel1` — so two boards' first Standing panels differ the moment both are popped out. The window keeps its own id, `PopOutWindow`, so nothing that lists pop-outs changes. `BoardEdits.PopOutAutomationId` holds the rule beside `AutomationIdOf` where it is pure and tested with the exact collision (same type, same position, two boards); the mutation that drops the board fails it. The walks match by suffix through one shared `Find-PopOutPanel` / `Get-PopOutFor` in `uia-board.ps1`, replacing the two private copies `walk-pop-outs` and `walk-visible-fixes` each carried. **Not yet walked** — the walk re-run is Sweep F's, with everything else that needs a launch.
- S2-P.17 **FIXED** — duplicating a long board name lost " copy" — 0013952 — pre-flight D16, final review Minor 6
- S2-P.18 **FIXED** — six drawn Ellipses replace the Braille glyph in PanelFrame.xaml, using the live muted brush inside a named Label with a minimum 24-pixel hit target. DragHandleTests verifies geometry, palette updates, automation identity, tooltip, and no new keyboard stop. Mouse handler and move buttons are retained; desktop drag/DPI smoke remains pending. Original finding: pre-flight D17.
- S2-P.19 **GONE** 2026-09-21 — plan text: the interface contract omits `BoardText.EmptyFor` and `PanelForms.GroupWords` — docs/plans/2026-09-14-score-book-stage-2.md:95-240 — pre-flight D18 Owner's ruling 2026-09-21: the stage 2 plan is a record of what was planned, not a living spec, and correcting its text now would rewrite what the record says was believed at the time. The code and tests are the truth; the plan stays as written.

## Task 1: board definitions and boards.json
- S2-1.1 **GONE** 2026-09-21 — if boards.json is locked when you change a board, the save throws while reading the old file (you get the "not saved" message; nothing is lost) — src/Board/BoardsFile.cs:129 — T1 review Owner's ruling by way of Sweep A's S2-1.2, ratified here: a locked file cannot be written whichever step notices, the read of the old file or the write of the new; the user is shown `BoardText.BoardsNotSaved`, the trail records the exception's type, nothing is lost, and the next change tries again. `Save`'s comment now names both throws. There is nothing to fix that would not be a retry loop against a lock somebody else holds.
- S2-1.2 **FIXED** 2026-09-21 — code tidiness: Save's comment doesn't say it can also throw while reading the old file — src/Board/BoardsFile.cs:51 — T1 review `Save`'s comment names both throws: the folder that can't be written, and the old file that can't be read to decide whether to keep it. Same "not saved" outcome, nothing lost.
- S2-1.3 **GONE** — two near-identical board key builders: `StarterBoard.Key` was removed — a3c5296 — T1 review

## Task 2: board edits
- S2-2.1 **GONE** 2026-09-21 — not a defect: the `using Source = ...` alias is NOT redundant — src/Board/BoardEdits.cs — T2 review. Checked by deleting it and building: `Source` alone binds to the `Labs626.UrScore.Source` NAMESPACE, which shadows the Core type everywhere outside Core, and the build fails with CS0118. The alias now carries that reason in the file. **This is the third row to make the same wrong call** — S1-7.1 called the same thing an unnecessary qualification and S1-9.5 half-called it — which is what turned it from a papercut into V3-S.44.
- S2-2.2 **FIXED** 2026-09-21 — code tidiness: two identical search loops for boards and panels — src/Board/BoardEdits.cs — T2 review. One `IndexOfId<T>`. Both named wrappers stay, because `IndexOf(boards, boardId)` reads better at a call site than the generic does, but only one of them knows how to search.
- S2-2.3 **FIXED** 2026-09-21 — code tidiness: doc comments on some BoardEdits methods and not others — src/Board/BoardEdits.cs — T2 review Every public method on `BoardEdits` now has a one-line summary; the eight that had none were `NewBoardName`, `Add`, `Replace`, `CanDuplicate`, `AddPanel`, `RemovePanel`, `PopOut`, `Return` and `Find`.

## Task 3: layout
- No minor findings.

## Task 4: forms and gallery
- S2-4.1 **FIXED** 2026-09-21 — code tidiness: the "★ name" label is still hand-built in My accounts and in Setup › Your accounts instead of the shared helper — src/Board/PanelText.cs, src/Board/PanelModels.cs, src/UI/Setup/AccountsModel.cs — T4 review, T4 report
- S2-4.2 **FIXED** — the form retyped the board's "was removed" wording — 2e18235 — T4 review
- S2-4.3 **FIXED** — the group-recipe selector was written twice — 2e18235 (`PanelText.GroupRecipe`) — T4 review
- S2-4.4 **FIXED** — a Profile stat whose only source is off looked stale — 2e18235 — T4 review
- S2-4.5 **FIXED** — a saved panel whose recipe is missing uses "source"/"sources" in settings labels and validation, including when rebuilding removed selections loses the recipe ID. Blank new forms retain the first installed recipe's wording, and selecting an installed recipe takes precedence. PanelFormsTests covers these cases. Original finding: T4 review, T4 re-review.
- S2-4.6 **FIXED** — Promotion check gallery wording and CanAdd now share the same qualifying-recipe lookup: an owned origin, another source of its recipe, and a ticked stat, with the main's recipe preferred when eligible. Existing disabled-card wording is retained when none qualify. PanelGalleryTests covers a main without a partner, competing eligible recipes, unticked stats, and watched-only origins. Original finding: T4 re-review.
- S2-4.7 **GONE** 2026-09-22 — code tidiness: the gallery recomputes a card's defaults on every title call — src/Board/PanelGallery.cs:23, 72 — T4 re-review Counted the callers rather than caching. `PanelGallery.Title(type, live)` works the card's recipe out each time, defaults included, and it is called in exactly two places: once when Add panel opens its form, and once per card when the gallery opens. Nothing calls it on a render or a tick; `TitleOf(panel, live)`, which pop-outs and the ⋯ form use on every redraw, does not go through the card recipe at all. A cache for a click is a cache for nothing, and the doc comment on `Title` now says where it is called so the next review does not file this again.

## Task 5: saved boards and tabs
- S2-5.1 **GONE** 2026-09-22 — performance: while following the starter, every read of the board list (each render, the 20 s clock) rebuilds the starter — src/Composition/AppServices.cs:214-215 — T5 review Minor 1, T5 report, final review Minor 9, parked Measured: one read of `Boards` with the starter rebuilt — three recipes, five sources, two starters — is 35 µs. There are 20 reads of it across the window's code, so a redraw that hit every one would cost under a millisecond, once per twenty-second tick. Nothing relies on the rebuilt boards' identity either: panels are rebuilt on a change of `BoardDefs.Key`, not of reference. A cache keyed on the installed and source lists would be correct and would save nothing a person could notice.
- S2-5.2 **FIXED** — the tab menu's hover and disabled colours might not match the theme — 9755c07 (themed menu, src/App.xaml:346-393) — T5 review Minor 2, final review parked item E
- S2-5.3 **FIXED** — many tabs squeezed + Board and the period line — 10eb325/da74cb2 (tabs scroll, capped width) — T5 review
- S2-5.4 **FIXED** — keyboard focus lost after a rename — 10eb325/da74cb2 — T5 review
- S2-5.5 **FIXED** — a stale board list was saved after a prompt — 10eb325/da74cb2 — T5 review
- S2-5.6 **FIXED** — starter buttons' accessible names dropped the panel count — 10eb325/da74cb2 — T5 review
- S2-5.7 **FIXED** — the add-board name box had no length limit — 10eb325/da74cb2 — T5 review
- S2-5.8 **FIXED** — SaveBoards' comment missed the empty-list rule — 10eb325/da74cb2 — T5 review
- S2-5.9 **FIXED** — the board period anchor now uses installed recipes: an enabled explicit non-list source, then an enabled non-list source of a panel's recipe, then the main, then a panel's group list. Recipe-only My accounts and Records panels no longer lose to a Top panel. BoardEditsTests covers ordering, disabled/uninstalled recipe fallbacks, and the current Battle starter with no own source. Current clan starters without sources already show an empty state. Original finding: T5 review, T5 report.
- S2-5.10 **FIXED** 2026-09-21 — retrying a failed save after a failed load can make a second unreadable-file copy — src/Composition/AppServices.cs:236-237 — T5 re-review `KeepUnreadable` does not make a copy whose contents already sit beside the file: it reads the existing `boards.unreadable-*.json` copies newest first and returns the match. `Save` copies the old file and THEN writes, so a write that threw left the old file where it was, still unreadable, and the retry arrived at the copy step again and made a second copy of the same bytes under a new stamp. Test fails the write by putting a folder where the temp file goes, moves the clock a minute so a second copy WOULD get a distinct name, retries, and asserts one copy; watched red without the fix.
- S2-5.11 **FIXED** — the "wasn't saved" note hid the RoRoRo-down and budget text indefinitely — da74cb2 — T5 report

## Task 6: panel tools, settings, gallery
- S2-6.1 **FIXED** — an Account card pinned to an unlisted account opened blank and silently saved as "Your top account" — d42c838 — T6 review, final review parked
- S2-6.2 **FIXED** — adding a Profile stat pinned a source the form never showed — d42c838 (a leftover corner is S2-FR.2) — T6 review, final review parked
- S2-6.3 **FIXED** — a circular comment in `RenderEmpty` — 5bd1fab — T6 review
- S2-6.4 **FIXED** — BoardEditsTests now covers null-to-empty and empty-to-null race lists at SetSettings: both return the original board/settings and keep the same render key. Changing the recipe still creates a changed board without mutating the original. Production behavior unchanged. Original finding: T6 review.
- S2-6.5 **FIXED** — a long race list could push Save off screen — 5bd1fab — T6 review, final review parked
- S2-6.6 **FIXED** — keyboard focus dropped after ⋯ Save or Add panel — fa169ac, 2dc6858 — T6 review, final review Minor 2
- S2-6.7 **FIXED** 2026-09-22 — code tidiness: settings are applied by panel id without checking the panel's type (not reachable today) — src/UI/BoardWindow.PanelSettings.cs:42 — T6 review `BoardEdits.SetSettings` takes the panel type the settings were built for and throws, naming both types, when the panel under that id is another; the one caller passes `def.Type`. Nothing reaches it today — the ⋯ form is modal and a panel's type never changes under it — so this is the invariant stated where it is relied on, and a throw is how a future caller finds out. Test drives it directly with Race settings on a Standing panel; the mutation that drops the guard fails it by name.

## Task 7: edit mode
- S2-7.1 **FIXED** — Done's "compare with the board as editing began" rule had no test — fa169ac (`BoardEdits.Finish`) — T7 review
- S2-7.2 **FIXED** — an empty board in edit mode told you to use Edit board — fa169ac — T7 review
- S2-7.3 **FIXED** — Done could undo a pop-out change, and tools could move a popped-out panel — 4383253 (`CarryPopOuts`, Remove-only slots) — T7 review
- S2-7.4 **FIXED** — a non-IO save failure on Done was silent — fa169ac — T7 review

## Task 8: pop-outs
- S2-8.1 **FIXED** — a pop-out's automation id went stale after panels were renumbered — cd40bd0 — T8 review
- S2-8.2 **FIXED** — explicit Return and native system-close commands are distinguished from direct external closes. PopOutLifecycle preserves placement and suppresses reopening after external close; main shutdown is marked before draft-saving redraws. Latest external-close positions remain eligible for saving. Deterministic tests cover both shutdown orders, repeated close, return and restart eligibility; hidden test-owned WPF windows verify native message-hook ordering. Actual application close-all/restart and saved-file smoke remains pending. Original finding: T8 review, T8 report.
- S2-8.3 **FIXED** — panel creation, board-grid visibility and pop-out synchronization now wait for ReaderLoaded. Existing Try again recovery remains; saved placement is not cleared. BookLoader extracts the existing shared-task retry policy for injected failure/success tests, and source-fence tests check production readiness gates. A late successful load cannot resume startup after shutdown begins. Full application failure/retry and exactly-once window restoration smoke remains pending. Original finding: T8 review, T8 report; current code already had a failure view but could open blank pop-outs before loading.
- S2-8.4 **FIXED** — a pop-out could maximize or snap to half the screen — cd40bd0 — T8 review
- S2-8.5 **GONE** 2026-09-21 — plan text: the automation id table doesn't list the slot's Remove button — docs/plans/2026-09-14-score-book-stage-2.md:132 — T8 review Owner's ruling 2026-09-21: the stage 2 plan is a record of what was planned, not a living spec, and correcting its text now would rewrite what the record says was believed at the time. The code and tests are the truth; the plan stays as written.
- S2-8.6 **OPEN** — test gap: pop-out placement on mixed-DPI monitors is unverified (the app isn't per-monitor DPI aware) — src/UI/BoardWindow.PopOuts.cs:212 — T8 review, T8 re-review, final review parked

## Task 9: stage 2 smoke walks
- S2-9.1 **FIXED** 2026-09-21 — docs: the two new walk rows in the smoke README don't say they end with the privacy check — tools/smoke/README.md:24-25 — T9 review Both rows now end "ends with `check-boards-privacy.ps1`", which is what both walks do at their last line.
- S2-9.2 **GONE** — README row placement and wording weren't disclosed in the report (report deleted; the placement is fine) — n/a — T9 review
- S2-9.3 **FIXED** — a `$board` parameter shadowed the walk's `$board` window — tools/smoke/walk-board-editing.ps1:14 now `$index` (final fix wave) — T9 review
- S2-9.4 **FIXED** — walk-pop-outs step 3 showed a bare FAIL when no battle was running — 73e911a (`Skip` with a reason; the leftover is S2-FR.3) — T9 review

## Stage 2 final review
- (Minor 1 is S2-P.1; Minor 2 is S2-6.6; Minor 5 is S2-P.8; Minor 6 is S2-P.17; Minor 9 is S2-5.1)
- S2-F.3 **FIXED** — holding Enter on Remove could delete panel after panel — 1313b28 — final review Minor 3
- S2-F.4 **FIXED** — an empty first-run starter could be frozen into boards.json by a change elsewhere — c41345e — final review Minor 4
- S2-F.5 **FIXED** — one wrongly typed JSON field made the whole boards.json unreadable — 5d0dd79 — final review Minor 7
- S2-F.6 **FIXED** — small duplications (your ids, group words, panel automation id) — 0e21742 — final review Minor 8
- S2-F.7 **FIXED** — README now explains starter tabs, tab actions, gallery/settings, draft editing, current flow-layout limitations, pop-outs, boards.json, and the accepted empty-starter duplication edge. CHANGELOG restores 0.3.0/0.3.1 summaries, places older recipe notes under 0.2.0, and keeps current Wave 1 changes under Unreleased. Existing recent documentation was retained. Original finding: final review recommendation 4.

## Stage 2 final re-review
- S2-F.8 **FIXED** — owner approved disabling Duplicate only for empty source-following starters on 2026-09-17, superseding the earlier accepted F4 behavior. BoardEdits and BoardButtons share eligibility; a rejected duplicate returns the original list, so the caller skips saving. Eight cases cover empty/populated following starters and user boards, retaining Rename and the tab menu. Live tab/menu smoke pending. Original finding: final re-review ("not counted").
- S2-FR.1 **FIXED** — Past periods offers an explicit last stat choice, "Don't show your best account", using the existing null-stat saved format. A removed stat stays held until explicitly replaced; headline/history-only settings survive reopening even after stats are ticked. New panels still default to the first ticked stat. PanelFormsTests and BoardsFileTests pass; coordinated Cancel/Remove/reopen desktop smoke pending. Original finding: final re-review Minor 1.
- S2-FR.2 **FIXED** — Profile stat settings now explain that the selected off source isn't read, without blocking Save, switching the source, or enabling reads. PanelFormsTests covers enabled, mixed, all-off and saved-off selections; the existing off label and earlier board explanation remain. Coordinated desktop warning/change-selection smoke pending. Original finding: final re-review Minor 2, T4 re-review; board half previously fixed by 17dbf9c.
- S2-FR.3 **OPEN**, mostly closed — test gap: walk-pop-outs step 3 skips instead of failing when reads are broken — tools/smoke/walk-pop-outs.ps1:48-57 — final re-review Minor 3, parked. **Narrowed by 73e911a and re-read 2026-09-21**, so nobody re-derives it: the skip is now conditional on `-not $battleRuns`, so a broken read WHILE a battle runs fails the step, and a read broken so badly that no period line appears fails it too. Exactly one hole is left — a broken read that renders em-dashes with no period line is indistinguishable from "no battle is running" from the board alone, and skips. Closing it needs the walk to read the source's STATE rather than the panel's text, which means navigating to Setup mid-walk and back; that moves focus and could disturb the pop-out assertions either side of it. Worth doing, but it is a change to the walk's shape that cannot be checked without running it live against the app, so it wants an owner-watched walk rather than a quiet edit.
- S2-FR.4 **OPEN** — code tidiness: the app-wide menu item style handles flat items only (no submenus or check marks); the light-theme menu wasn't seen live — src/App.xaml:364-393 — final re-review Minor 4, parked

## Found in the v0.3.0 smoke (2026-09-15)

v0.3.0 installed from the GitHub release into RoRoRo 1.28 (Store) and walked: all seven smoke walks passed against the installed copy (92 checks), then the owner's own data folder was used with the clan battle recipe re-imported.

- V3-S.1 **FIXED** — an idle stop now carries its past periods: both idle paths (`idleWithout` and `absentMessage`) read `period.past` from the last step's response through the same `PastAt` the success path uses, the planner backfills finals from an idle reading, and the watch writes them. Every other stop still returns nothing. The empty Past battles panel also says what is true now ("Ur Score hasn't read this clan's finished battles yet…") instead of "No finished battles kept yet." — src/Recipes/RecipeEngine.cs (`WithPastAsync`), src/Book/Finals.cs, src/Core/RecipeWatch.cs (`Backfill`), src/Board/PanelModels.cs — v0.3.0 smoke, fixed 2026-09-16
- V3-S.2 **FIXED** — a single-stat legacy state with `metricIdOverride` and no `stats` field now supplies Show and Send ticks to the update review, under its old override or the installed recipe's metric name. It requires review even when contacts have not changed. Loading alone enables no reports and writes nothing; modern stats, including an empty or null `stats` field, are never overridden. Saving the reviewed update writes ordinary stat choices, with no new file format. Removed stats remain unticked and the review names the send that would stop. Verified with state, review and hidden WPF window tests; the live installed-plugin smoke remains to be run — src/Recipes/RecipeStore.cs (`LegacyStatChoices`, `ChoicesForUpdate`), src/Recipes/ImportReview.cs (`CompareToInstalled`), src/UI/ImportWindow.xaml.cs; tests/LegacyRecipeUpdateTests.cs, tests/RecipeStoreTests.cs — v0.3.0 smoke, fixed 2026-09-16
- V3-S.3 **FIXED** — the update's change list now says "Now tracks the current battle." and "Now reads past battles.", using the recipe's own period words. Current-period and history changes are compared separately: additions, removals, changed timing or period definitions, and changed history paths are listed; equal definitions say nothing. Removing history says the score book is kept. These changes alone do not request new consent; the existing contact and sending rules still decide that. Regression tests cover the reported update, history-only changes, removals, changed definitions, and season wording — src/Recipes/ImportReview.cs (`CompareToInstalled`), tests/ImportReviewTests.cs — v0.3.0 smoke, fixed 2026-09-16
- V3-S.4 **FIXED (d7102ae, 0.3.2) — Attribution deduplicates per sentence, so recipes sharing an opening sentence credit it once.** Original finding: you'd notice: the board footer repeats "Data from Big Games' public Pet Simulator 99 API." once per installed recipe when two recipes share the same credit — src/UI/BoardText.cs:78 — v0.3.0 smoke
- V3-S.5 **FIXED — uia.ps1 honours a UR_SCORE_EXE override; used to walk the installed 0.3.2 copy on 2026-09-16.** Original finding: test gap: the smoke scripts can only launch the build folder's exe (`uia.ps1` hardcodes `bin\Release`), so walking the installed plugin needs a patched copy of the scripts; add an `UR_SCORE_EXE` override — tools/smoke/uia.ps1:14 — v0.3.0 smoke
- V3-S.6 **FIXED** — you'd notice: the window, taskbar and board icon stayed Ur Score's own at every start until the first read (Start or Test now), although the clan's picture was already in `icon-cache`; the icon files and texts lived only in memory and were filled by reads. Each source's icon text is now kept beside the pictures in `icon-cache\source-icons.json` (a source id and its icon text, nothing else, and only once its picture was found). At start, before the window exists, each source's picture comes back from the cache alone, however old, with no request; a read later refreshes it as before, and a picture that can't be fetched again for the same icon text stays. The window, the taskbar, the top bar, each Standing panel and Setup › Recipes open on it. The first start after this change still waits for one read, because nothing had yet saved whose picture was whose — src/Source/SourceIcons.cs (`Restore`), src/Source/IconClient.cs (`CachedFile`), src/Composition/AppServices.cs (constructor), src/UI/BoardWindow.xaml.cs — owner's first look, 2026-09-15, fixed 2026-09-16
- V3-S.7 **FIXED** — you'd notice: the icon was kept per recipe, not per source, so every clan read on the clan battle recipe (main, a clan your accounts are in, a watched clan) replaced it, and the board showed whichever read last instead of the main clan's (this is how K0i2's icon appeared). A picture now belongs to a source (`SourceIcons`), and whose picture goes where is decided from the sources and recipes alone, never from which picture arrived first (`IconChoice`): the window shows the first switched-on main whose recipe names an icon, waits for that clan's picture rather than wearing another's, and with no main keeps Ur Score's own (the old "else the first source that has one" fallback is gone). The owner's feature rides on it: Clan standing shows the logo of the clan that panel is about, beside the place, so a panel pinned to a watched clan shows that clan's; a recipe's row in Setup shows its main clan's. Each picture is named for its clan ("CCGP clan icon"), the top bar's included, and each keeps its space while the picture isn't there (A26) — src/Source/SourceIcons.cs, src/Board/IconChoice.cs, src/Composition/AppServices.cs, src/UI/Panels/StandingPanel.xaml, src/UI/Setup/RecipesPage.xaml, src/UI/BoardWindow.xaml, src/UI/BoardWindow.xaml.cs — owner's first look, 2026-09-15, fixed 2026-09-16
- V3-S.8 **FIXED — ⋯ beside the board tabs opens the tab's own menu, so Rename…, Duplicate and Delete… are on screen; the right-click and Shift+F10 still open the same three.** Original finding: you'd notice: Rename…, Duplicate and Delete… for a board exist only on the tab's right-click menu (or Shift+F10); nothing on screen says they're there — src/UI/BoardWindow.xaml, src/UI/BoardMenu.cs, src/UI/BoardButtons.cs — owner's first look, 2026-09-15, fixed 2026-09-16
- V3-S.9 **FIXED** 2026-09-21 — you'd notice: editing a board is clumsy. Panels flow in reading order on a 12-column grid (R6), so resizing one reflows everything after it; there's no free placement, no drop marker while dragging (R9), and moving and sizing are separate small steps (Move earlier/later, a size menu, a Tall tick). Owner's goal (2026-09-15): a snap grid, where you drag a panel anywhere, drag a corner to resize, the other panels stay put, and a live outline shows where it lands. Its own spec and plan after the 2026-09-19 clan battle — src/Board/BoardLayout.cs, src/UI/BoardWindow.Editing.cs, docs/plans/2026-09-14-score-book-stage-2.md R5-R9 — owner's first look, 2026-09-15 **Closed with the owner's rulings recorded, because the row's stated goal was ruled against.** PR #13 (2026-09-21) landed what the board needed: drag a panel by its header with a live drop caret, haul an edge or corner to resize with a size preview, and the arrow buttons, size box and Tall tick retired. On the two design questions the row raised, the owner chose the other way both times — **push, not stay-put** (a dragged panel pushes the others along rather than landing on top of them) and **reflow as today plus drag, not free placement** — so the grid and the reading-order reflow stay by decision, not by omission. Header-only drag was also the owner's call. Ratified as closed 2026-09-21.
- V3-S.10 **FIXED — all seven stock message boxes are gone, and a fence over the whole of src keeps them gone.** A question opens Ur Score's own themed `ConfirmWindow`, one window for all three, its words decided in the pure `Confirm` and tested away from what draws them: delete a board (`BoardText.DeleteBoardQuestion`), remove a recipe (`RecipesModel.RemoveQuestion`) and add a clan past the limit (`ClansModel.AddQuestion`, which returns null when nothing needs asking; S1-11.4). Cancel is the default and the answer keyboard lands on, so Enter alone deletes nothing, and the button that acts is named for what it acts on. Something that has already gone wrong is not a question and opens nothing: a failed board save stays on the board's own detail line until a save works (BoardWindow.xaml.cs `SaveBoards`), and every refused import — unreadable file, invalid recipe, a name clash, a recipe that couldn't be saved — is said on Setup › Recipes' own `ImportProblemLine`, under the button that started it, the way Setup › Alerts says a failed write on the card. `MessageBoxFenceTests` sweeps all of src with an empty allow list, asserted as equality so it stays a ratchet. The smoke helpers learned the themed window in the same commit (owner's ruling, 2026-09-16: the scripts serve the app): `Close-MessageBox` and `Get-MessageBoxText` worked through Win32 — BM_CLICK to a control's window handle, and a `Static`'s text — which a WPF window has neither of, so they are replaced by `Wait-UrConfirm`, `Get-UrConfirmText` and `Invoke-UrConfirm` in uia.ps1 and `Get-ImportRefusal` in uia-import.ps1. walk-board-editing step 9 now proves the two things separately (Delete asks first, in Ur Score's own window, naming the board; then the board goes), and window-smoke step 2 reads the refusal where it is now said. The tooltips that shared this row move to V3-S.13. All eight Ur Score windows already follow the theme and Ur Score has no toasts; the recipe file picker (ImportFlow.cs:26) is Windows' own dialog and follows the Windows theme, which Ur Score can't restyle — owner's first look, 2026-09-15, fixed 2026-09-16
- V3-S.13 **FIXED** — App.xaml supplies an implicit ToolTip template for "Drag to move" and "Return to the board": live palette brushes, body font, compact border, and bounded wrapped text replace Windows chrome. ThemeFenceTests load the actual style on an STA thread for both labels and verify content, sizing, and brush replacement reaching the rendered text/border. Desktop hover smoke remains unverified. Original owner rule: every popup and toast is themed; carried out of V3-S.10.

- V3-S.11 **FIXED** — you'd notice: in Past battles, Total and Your best sat flush against each other, so a row read as one number (`32 32 · estehernandez` as 3232), and the account name lost characters early on a narrow panel. Every column past the first now opens with a 12 px gutter; the two number columns take what they need (64 px for an ordinal, 76 px for an abbreviated total) and the two columns that carry names share the rest, Your best on the larger share — src/UI/Panels/PastPeriodsPanel.xaml — owner's look at the backfilled record, 2026-09-16. **The widths on this line are superseded by V3-S.15**, which took Your best's larger share back out again: it had been bought from the battle-name column. The 12 px gutters, which are what actually separate the two numbers, stayed.
- V3-S.12 **GONE** — Past battles was reported as listing the record in the source's own order instead of newest first. It doesn't: the rows were already newest first, and reversing them would have been the defect. Checked against the live record (23 finished battles, `HalloweenBattle` first to `NinjaBattle2026` last), that source's published battle schedule (the two agree key for key, oldest first), and the reported screen order (Ninja 08-01, Lunar 07-04, Soccer 06-20, Backrooms 06-06, Angel 05-09 — strictly descending). What was true: nothing said so. The order was inherited from the reader through two stable sorts in two files while the panel's own line read as "sort by T", which a backfill ties across a whole record. The panel now orders explicitly by the two signals a final has (when the book learned it, then where the record keeps it) and two tests hold it — src/Board/PanelModels.cs, src/Book/ScoreBookReader.cs (`FinalEntry.Kept`), tests/PanelModelsTests.cs — owner's look at the backfilled record, 2026-09-16

- V3-S.14 **FIXED** — you'd notice: every pop-out opened at one size, 360 x 300, whatever panel it held. Past battles at that size clipped all four columns at once (`NinjaBattl…`, `Backroom…`, `32 · estehern…`, `31.2M · CECP…`) and showed 5 of 23 rows. A pop-out now opens at what the panel already has on the board it came from: the width its own span holds on that board's grid, through the same column maths `PanelGrid` arranges it with, and the height the panel asked for there — so a four-column table opens on its table, a short list of label/value pairs still opens small, a narrow board that widens every panel widens its pop-outs too, and there is no table of sizes per panel anywhere. It is only a starting size: the screen still caps it (`PopOutPlacement.Clamp`), the window is still freely resizable, and nothing new is remembered — where you leave it is saved as before, and a panel popped out again in the same session still reopens where it was last left, not at the size worked out here. The pop-out window also draws its panel at the board's 13 px rather than WPF's 12, which is what makes the height it was handed true — src/Board/PopOutPlacement.cs (`SizeFor`), src/Board/BoardLayout.cs (`CellWidth`), src/UI/BoardWindow.PopOuts.cs, src/UI/Boards/PanelPopOutWindow.xaml, tests/PopOutPlacementTests.cs — owner's look at the pop-outs, 2026-09-16, fixed 2026-09-16
- V3-S.15 **FIXED** — you'd notice: V3-S.11 bought Total and Your best their separation out of the battle-name column, which then trimmed (`LunarBattle20…`, `SoccerBattle2…`, `AngelBattle20…`) where full names had fitted. The gutters do all of the separating, so the fixed columns now cover only what they carry — 56 px for an ordinal up to "1,234th", 64 px for an abbreviated total up to "312.5M", each with its 12 px gutter and its own heading — and the two columns that carry names share the rest equally, because a row whose name is gone can't be read at all. `PastPeriodsColumnsTests` measures the real text against the widths the XAML declares, at a board tile and popped out, on a roomy board and a narrow one, and still holds the separation V3-S.11 won — src/UI/Panels/PastPeriodsPanel.xaml, tests/PastPeriodsColumnsTests.cs — owner's look at the pop-outs, 2026-09-16, fixed 2026-09-16
- V3-S.16 **FIXED** — you'd notice: all five ⧉ on a board answered to "Pop out" and all five ⋯ to "Panel settings", so a screen reader gave no way to tell which panel each belonged to and an automation caller had to reach past the name to the panel's id. Each is now named from the panel's own title — "Pop out Past battles", "Settings for Past battles" — as the popped-out slot's "Bring back {title}" and "Remove {title}" and the gallery's "Add {title}" already are, set in `PanelFrame.UpdateTools` beside every other tool state so a redrawn panel renames them with it. No walk changes: every walk reaches these by automation id (`PopOutButton`, `PanelSettingsButton`) — src/UI/BoardText.cs, src/UI/Panels/PanelFrame.xaml, src/UI/Panels/PanelFrame.xaml.cs, tests/PanelToolNamingTests.cs — owner's look at the pop-outs, 2026-09-16, fixed 2026-09-16
- V3-S.17 **FIXED** — a stale panel's button is now named "Choose another for {title}" in UI Automation, refreshed beside Pop out and Settings from the panel's own head. Its visible text and action are unchanged; a missing title keeps "Choose another". The naming tests cover distinct titles, trimmed and missing titles, and the frame/XAML wiring. Live Narrator testing remains unverified — src/UI/BoardText.cs (`ChooseAnotherName`), src/UI/Panels/PanelFrame.xaml(.cs), tests/PanelToolNamingTests.cs — noted while fixing V3-S.16, fixed 2026-09-16
- V3-S.18 **FIXED** — you'd notice: straight after a read that finished ("Reading 3 sources."), My accounts filed every account under "No clans read yet", and Setup › Your accounts said the same, of clans that had just been read. A clan between battles is read: its reading is idle, with no rows, because there is no member list to give. Both screens counted only readings with rows, so an idle one counted as never read. It is what the board shows most of the time, since the main clan sits between battles far longer than it is in one, and the true-reason wave (17dbf9c, S1-13.4, S1-12.7) had tested never-read and read-with-rows but not read-and-idle. The one decision both screens share, `PanelText.NotFound`, now also counts this session's idle readings (`PanelText.ReadIdle`: the idle state, asked of `LiveOf` or Setup's latest, never of a remembered reading) and, with no rows on hand anywhere, says "No clan is in a battle right now" once every clan was read so, or "No clan read so far is in a battle" while any is unread or its read failed, in the recipe's own words (`RecipeWords.Group` and `Period`, "an" before a vowel; Setup says "period" when its recipes' periods differ). Rows on hand still come first ("Not found in the clans read so far", remembered ones included), a failed read is still no read, and "Not in a watched clan" still waits for every clan to be read with rows — src/Board/PanelText.cs (`NotFound`, `ReadIdle`), src/Board/PanelModels.cs (`MyAccounts`), src/UI/Setup/AccountsModel.cs (`FoundIn`, `Words`), tests/PanelModelsTests.cs, tests/AccountsModelTests.cs, tests/PanelTextTests.cs — owner's own board, 2026-09-16, fixed 2026-09-16
- V3-S.19 **FIXED** — you'd notice: a clan added under "your accounts are in" wore the "yours" chip whatever its read said, so a Clan standing panel showed "yours" directly above "Your accounts 0 of 57" (owner's screenshot, K0i2). The chip now reflects the read in hand: a live read with members, none of them yours, wears "watching". With no members read (not yet read, or between battles) there is no evidence, so it keeps the role you chose — the roster change V3-S.20 settles that case properly — src/Board/PanelModels.cs LiveBoard.ChipRole — owner, 2026-09-16
- V3-S.20 **OPEN** — you'd notice: Ur Score works out which clan your accounts are in only from battle contributions, so between battles it can place none of them, although the clan API returns a full roster on every read, battle or not (`data.Members`: UserID, PermissionLevel, JoinTime). Measured 2026-09-16: CCGP's roster held 6 of the owner's 8 accounts while his board said "No clan is in a battle right now" for all 8, and K0i2's roster held none of them. Proposed, owner agreed to backlog it: take membership from the roster and use battle contributions only for points; derive the chip from the roster so "yours" means your accounts are on it; let My accounts and Setup > Your accounts group by roster between battles; and surface accounts on no added clan's roster (CElCPapa and ItsJustEstePapa were on neither), which is the promotion question the owner set out to answer. Privacy is unchanged: the roster carries other members' ids, which are compared against yours and dropped, never kept, logged or written — the same rule battle contributions follow today. Touches the clan recipe (does it read `Members`?), the engine's membership model, LiveBoard.ChipRole (V3-S.19) and both grouping screens — owner, 2026-09-16
- V3-S.21 **FIXED** — you'd notice: a score book that couldn't be read said so twice, "Your score book could not be read." on the state line and "Your score book couldn't be read" as the board's heading. The state line keeps the verdict (now "couldn't", as the rest of Ur Score says it) with the reason under it, and the board says what that stops: "Start and Test now are off", "They come back once Ur Score can read your score book. The line above says what stopped it.", Try again. BoardTextTests pins both and fails if the heading says the book couldn't be read again. Seen on screen with a folder Windows won't list. — src/UI/BoardText.cs (BookStateLine, EmptyState) — 2026-09-17 desktop smoke, walk-visible-fixes step 4 screenshot.
- V3-S.22 **FIXED** — you'd notice: with both recipes imported, Setup › Your accounts left "Found in" about as wide as "No clan is in a battle right now" (each recipe takes a 160 px Send column), so the heading ran flush into the first Send box: "right now☑ Send". Shipped in 0.3.4, where the longer true headings arrived. The cell keeps 12 px before the Send columns, and Muted wraps a line into two instead of reaching them. AccountsPageSpacingTests pins the gap and the wrapping style; seen on screen wrapping with two recipes. — src/UI/Setup/AccountsPage.xaml — 0.3.4 how-to screenshot retake, 2026-09-17.
- V3-S.23 **OPEN** — you'd notice (not built): **loadout testing.** Pet Sim 99 play is a loadout question — pet strengths, enchants, books, potions — and the only way anyone knows what works is to try combinations and watch the numbers. Ur Score already keeps every reading of your own accounts, so it is the one thing on the PC that could say which loadout actually earned more per hour. Wants: mark a window as "running loadout X", compare per-hour rates between windows on the same account, and keep the comparison honest about what else changed (a different zone, a different time of day, an event modifier). Nothing about this is designed yet. — owner request, 2026-09-20.
- V3-S.24 **OPEN** — you'd notice (not built): **event loadout testing, as its own session.** Events change the maths: breakables events reward different enchants, books and potions than the ordinary game, so a loadout that wins in normal play can lose in an event, and each event has to be tested on its own. Wants: a session that knows it is an event, holds the loadout being tried, and reports rate per loadout within that event, so a clan can be told what to run. Needs, before any of it is designed: how a breakables event scores, what the API exposes about events and pets, and which of those the recipes can already read. The owner asked for this to be explored by several agents at once, reading the stats Ur Score keeps against what is known about pets and event scoring, rather than one pass. — owner request, 2026-09-20.
- V3-S.25 **FIXED** — you'd notice: **a group list keeps 25 names with no check that a group is a clan.** `GroupRows.Keep` persists the top 25 group names on every read, and "group" is whatever a recipe's `groupName` points at. The two shipped recipes point at clans, so the 2026-09-20 ruling ("these are game stats ... it's just game stuff") covers them — but a third-party recipe whose rows are PLAYERS would write strangers' usernames to disk under exactly the same code path, which no ruling covers. Fix direction: an explicit flag on the recipe (groups are clans, not people), and keep names only when it is set. — src/Book/GroupRows.cs:40 — review, 2026-09-20. **FIXED** — `"groupsAreClans"` is a top-level recipe field, false unless stated, and `GroupRows.Keep` now writes no name at all without it. FieldSummary rides the same line either way, so the leader, top ten, average and bottom ten stay true for a list that keeps nobody. Both shipped group lists make the claim, and ShippedRecipesTests fails any future one that forgets, because the cost of forgetting is an empty race chart mid-battle. The owner ruled on 2026-09-20 that the app says so rather than going quietly empty: the import screen names which of the two is happening, and a race panel with no board says why. Documented in docs/2026-09-13-recipes-design.md §3.1-3.2.
- V3-S.26 **FIXED** — you'd notice: **the import consent screen tells a lie about group lists.** `ImportText` says nothing from this recipe is kept, at the one screen whose job is informed consent. That was true when it was written and is false now: a clans list writes FieldSummary numbers and 25 clan names on every read. Fix it in the same hour V3-S.25 lands, and derive the sentence from the same predicate the writer uses so the two cannot drift again. — src/UI/ImportText.cs:40 — review, 2026-09-20. **FIXED 0.5.3** — the consent screen now names what a clans list keeps (where yours stands, how the field is doing, the top 25 by name) instead of claiming nothing is kept; ImportTextTests pins the true sentence and fails if the old one returns.
- V3-S.27 **FIXED** — docs only: **stale guarantees above the code that broke them.** `LineBuilder` still says a group list writes nothing and still cites the superseded 2026-09-19 ruling about naming no clan; both were overtaken by 0.3.10 and by the owner's 2026-09-20 reversal. Correct them in place with the date they went stale, as ROROROblox does. — src/Book/LineBuilder.cs:15, :31-32 — review, 2026-09-20. **FIXED 0.5.3** — LineBuilder's comment now states what it writes and names the 2026-09-20 reversal instead of the superseded 2026-09-19 ruling.
- V3-S.28 **FIXED** 2026-09-21 — **the history budget has no term for a metric with no subject.** `HistoryBudget.Count` counts accounts x sent stats against RoRoRo's 256 history slots; a clan-and-field metric has no account, so it is counted as nothing, and `OnSaveFieldClick` runs no budget check at all. Six ticked clan numbers across several installed lists can push a heavy setup past the ceiling with no warning on the screen that exists to warn about it. — src/Core/HistoryBudget.cs:21, src/UI/Setup/StatsPage.xaml.cs — review, 2026-09-20. The recipe share carries a third term, the clan numbers a list sends, and the count is accounts times stats PLUS that: each clan number is one series with no account behind it, so under the old count a list's share was exactly zero however much it sent. The field save now runs the same refusal a stat tick meets (`FieldMetricsModel.Budget`), so a clan number cannot take the setup past RoRoRo's ceiling from the one screen that exists to warn about that, and the start-up warning sees lists too — it left them out when they counted as nothing. The line names the new term. Tests: the share and the count with a fixture built so the field term is the ONLY thing that moves the total; the refusal at 254 + 2 allowed, 254 + 3 refused, and lowering an already-too-high count allowed. Both mutations — the field term dropped from the share, and from the sum — fail them by name.
- V3-S.29 **FIXED** — **persisted clan names have no size readout and no way to manage them.** Setup > Score book excludes group lists from what it lists and measures, so the one thing now growing fastest on disk (25 names x every five minutes x a thirteen-day battle) is the one thing the page does not show. The owner's ruling allows the names AND asks that it not store too much; a number on the screen is how that stays honest. — src/UI/ScoreBookModel.cs:42, src/Composition/AppServices.cs — review, 2026-09-20. **FIXED 0.5.3** — Setup › Score book lists a clans list like anything else you keep: readings, first reading and size, and it appears in "not recording" with its reason. No finals count, since it closes no period.
- V3-S.30 **FIXED** — **the board's "your clans" set is still role-blind.** 0.5.2 fixed the two sets the score book and the send use (`MyGroupNames`, `MyClanNames`); `PanelModels.MineNames` was left as it was, so a watched rival can still be drawn as yours on the race chart and in the standings. Decide once what "your clans" means for the board versus for the metrics, write it down, and make the board follow it. — src/Board/PanelModels.cs — review, 2026-09-20. **FIXED** — the board asked two questions under one name. `DrawnNames` now answers only "what does this panel already draw", which is the dedupe and wants watched clans in it; the band's anchor and the standings' bold row and gaps take `SourceRules.MyClanNames`, so the board means what the metrics mean. RaceBoardTests pins a watched rival placed above you: it anchors nothing, is not bolded as yours, and keeps its own gap.
- V3-S.31 **FIXED** — **"your clans" is computed independently in three places.** `MyGroupNames`, `MyClanNames` and `MineNames` are three hand-rolled filters over the same idea, each free to drift — and two of them had drifted, which is what V3-S.30 and the 0.5.2 watch bug both were. Make it one function with one meaning, have every caller take it, and put the consent copy on the same predicate. This is the structural finding the review named as the codebase's biggest weakness: the strongest promises live in prose, and the fixtures are shaped so the invariant cannot fail. — src/Composition/AppServices.cs, src/UI/Setup/FieldMetricsModel.cs, src/Board/PanelModels.cs — review, 2026-09-20. **FIXED** — one `SourceRules.MyClanNames`, taken by all four callers; the copies in `AppServices` and `FieldMetricsModel` are deleted. The two survivors had already drifted again on a source whose recipe is not installed (one dropped it, one kept it); the shared one drops it, because "this clan is mine" is a positive claim and without the recipe there is no telling whether the inputs name a clan at all. SourcesTests pins that case and the watched/switched-off ones, and both were proved to fail against the old behaviour before the delete. The consent-copy half is done too: `ImportText.Kept` lists the kept names only when `Recipe.KeepsGroupNames`, and `KeptNote` may describe a list as holding no players only when the recipe has said so. The sentence and the writer now read the same predicate, so the screen whose job is informed consent cannot drift from what is actually written to disk.
- V3-S.32 **FIXED** — you'd notice: **Ur Score only looks UP.** `clan.standing.gap-above` and `clan.standing.pace-needed` model catching the place above; nothing models the clans BEHIND catching US, which is the direction that actually matters during a battle. The owner's clan already runs a Discord bot that does it, and its wording is the bar to clear (screenshots, 2026-09-20): "Threat Alert — outpaced / H8ER passes us in 5h 56m / 553.59M behind · 748.36M/hr vs our 654.98M/hr (+93.38M/hr) / We're at 9.10B points with 4d 23h left in the battle", and several threats batched into one message (LXCC and R0W together). Wants: how far the clans behind are, and how long until the soonest of them passes us at current paces. Two design notes before building it. (1) Track the RIVAL BY NAME, not the position below: `GroupSeries` already keeps a named clan's series, and a position's points do not reveal a change of occupant on the way UP the way they do on the way down, so the clear-on-fall rule that works for `field-above` does not work for a place below. (2) An Ur Score metric carries no name — RoRoRo words an alert from the rule that fired — so a metric alert can say "a clan behind passes us in under six hours" but not "H8ER does". Decide whether that is good enough, or whether the named sentence belongs on the board and in a report rather than in a metric. — owner, with the clan's existing bot as the reference, 2026-09-20. Positioning, same day: Ur Score is built ALONGSIDE that bot, not to replace it, and its edge is RoRoRo's phone leg — Pushover or ntfy, a real push with no Discord app, one payload of at most a 250-character title and a 992-character body. Reproducing the Discord post is not the win; getting the same intelligence onto a phone, quietly and only when it matters, is. **FIXED** — two numbers, `clan.standing.threat-gap` and `clan.standing.threat-hours`, answer both halves of design note (1) and the owner's want: how far behind the closest threat is, and how long until it passes you, both taken from the SOONEST clan by TIME to pass you rather than the nearest by points (`FieldMetrics.SoonestThreat`), because a near clan gaining slowly can still lose the race to a farther one gaining fast. The threat is tracked by NAME across reads, not by position (`RecipeWatch`'s `_fieldBehind`), so a name leaving the tracked band is dropped rather than folded into a different clan's history, and a fall costs a window of silence rather than a wrong pace. Design note (2) is settled by managing the rule's LABEL instead of putting a name in the metric: `RulesFile.ChangeLabel` rewrites just the label of Ur Score's own Level rule for a managed metric (`FieldMetric.ManagedLabel`), leaving every other field and the backup untouched, and the Alerts card explains why RoRoRo will rename its own alert. The whole feature turns on one ordering rule RoRoRo's capture-then-word design forces (design §1): the label must be written before the number is reported, with nothing between the two, or the alert ships worded under the PREVIOUS chaser's name. `RecipeWatch` reports a managed-label number only when a real write says the label is safely in place; `AppServices.LabelInPlace` maps `RulesFile.ChangeLabel`'s `RuleWrite` to that bool — Done, NotThere and AlreadyThere are safe, CantWrite above all is not — so a write that cannot be trusted refuses the send rather than guess (the owner's ruling of 2026-09-20: a threat number under a stale name is a confident false statement on a phone mid-battle, worse than silence) — src/Core/FieldMetrics.cs, src/Core/RecipeWatch.cs, src/Core/RulesFile.cs, src/UI/Setup/AlertCards.cs, src/Composition/AppServices.cs; tests/FieldMetricsTests.cs, tests/RecipeWatchBookTests.cs, tests/RulesFileTests.cs, tests/AlertCardsTests.cs, tests/AppServicesTests.cs — spec docs/2026-09-20-threat-alerts-design.md, fixed 2026-09-20.
- V3-S.33 **FIXED** — you'd notice: **the Top panel marks watched clans as yours.** Collapsing the other three copies (V3-S.31) turned up a fourth: `PanelModels.Top` builds `yours` as `s.Enabled && !IsGroupList` over `live.Sources`, with no role check, and from `SourceName` rather than the source's inputs. So a WATCHED rival is given `isYours` on the row and is pinned into the list from below the cut like one of your own. The pinning may well be what you want for a clan you are watching; being told it is yours is not. **RULED by the owner, 2026-09-20: a watched clan keeps its place and its estimate row, and loses the tint.** Staying visible is the whole point of watching a clan; the cyan tint says "this is mine" and belongs to your own. So `TopRow.Yours` splits the way `MineNames` did on the race chart: one flag for why a row is shown, a separate one for whether it is yours, the second from `SourceRules.MyClanNames`. Note while doing it that this site derives names from `live.SourceName(s)` rather than the source's inputs, so the two must be reconciled or the shared predicate will miss rows this one matched. — src/Board/PanelModels.cs:954 — found while fixing V3-S.31, 2026-09-20. **FIXED** — `followed` decides why a row is on the list and keeps a watched clan past the cut; `SourceRules.MyClanNames` decides the tint. The two name derivations are reconciled by being a subset: `SourceName` is the recipe's main input, `MyClanNames` takes every input a source carries, so a name this list can show for one of yours is always in the set. An existing test had the bug pinned, asserting `Yours=true` for a `SourceRole.Watch` source.
- V3-S.34 **FIXED** 2026-09-21 — you'd notice: **a mid-table clan's race band is half empty.** The race chart draws `Neighbours = 3` either side of yours (`PanelModels.cs:477`), but `GroupRows.Keep` keeps the top 25, your clans, and only `at - 1` to `at + 1`. Inside the top 25 every neighbour is kept anyway and the band is whole, which is why nobody has seen this: the owner's clan has been placed high. Placed below about 25th, four of the six neighbours have no series and simply do not draw, and the chart gives no sign that it is showing less than it was asked for. Found while planning V3-S.32, 2026-09-20. **FIXED 2026-09-21** as the row directed: `GroupRows.Neighbours` is the one number and `PanelModels` derives from it, in that direction — the reader cannot draw what the writer never kept, so the writer owns the size. `StandingsShown` had the identical shape against `GroupRows.Top` and is joined the same way. Both existing tests that named their own copy of the size now state it against the constant, because a test with its own copy is the same drift one layer up. Costs about a third more names on disk in exactly the case that was broken and nothing at all in the case that was not, which stays inside the 2026-09-20 ruling's bound. Mutation-tested both directions per V3-S.37: the keeping put back to one either side fails the mid-table test BY NAME ("C37, -3 from yours, was not kept"), and a chart asking for five against a book keeping three fails the agreement test. The mid-table fixture deliberately places the clan at 40th, well below `Top`, because the same test written at 24th passes under the old rule and proves nothing. — src/Book/GroupRows.cs, src/Board/PanelModels.cs
- V3-S.35 **FIXED** 2026-09-21 — you'd notice: **a suppressed threat number leaves no trace at all.** V3-S.32's no-label-no-send rule (design §1, `AppServices.LabelInPlace`) is right to refuse a send when `RulesFile.ChangeLabel` cannot be trusted — a threat under a stale name is worse than silence — but the refusal itself is invisible. `ReportPolicy.Sent` and `Dropped` do not move for it (the send is skipped in `RecipeWatch.SendFieldAsync` before either policy method is ever called), and no UI line says why. A rules file whose Ur Score-owned Level row carries a duplicate key (`RulesFileTests.ChangeLabelOnARowWithADuplicateKeyRefusesRatherThanCrashing`'s case) means `clan.standing.threat-gap` and `clan.standing.threat-hours` never fire again, forever, with nothing on screen or in Diagnostics to say so. Silence over a wrong name is the correct ruling; silence with no diagnostic is the gap. **FIXED 2026-09-21.** `ReportPolicy.Held` counts them, apart from `Dropped` because the cause and the cure differ: a drop is the user's own ticks saying no, a hold is a rules file that could not be edited safely, so the remedy is in the file rather than in the settings. It rises only for a write that was TRIED and refused — `writeLabel` absent, or no threat in the read at all, hold nothing back, because a counter that also counted quiet would put a number on every first read of every battle and teach everyone to ignore it. Three traces where there were none: the counter, the board's own read line (`RecipeWatch.HeldBackDetail`, which names the cause and says what the alternative would have been, since "held back" alone reads like a fault rather than a decision), and the Alerts card, the last only when there is something held. `SendFieldAsync` returns sent AND held; `PolicyCounts` and `ISetupServices` widened to carry it. The send-refusal itself is unchanged and stays. — src/Core/RecipeWatch.cs, src/Core/ReportPolicy.cs, src/UI/Setup/AlertsModel.cs, src/Composition/AppServices.cs — task 8 review, threat-alerts plan, 2026-09-20.
- V3-S.36 **OPEN** — you'd notice: **the clan numbers only ever describe ONE of your clans, so alts in two clans cannot both raise alerts.** Every clan-and-field number is computed from the BEST PLACED of your clans: `FieldSummary.Of` finds that row and measures points, place, gap above, free slots, idle members and both threat numbers from it. The cause is not the clans list — one list already sees the whole field, all of your clans included — it is the ID. Each number has a single fixed id (`clan.standing.points`), one read produces one value for it, and two of your clans would both want to be that id, so RoRoRo would see one number flipping between two clans' values. Raised by the owner on 2026-09-20 from the case that actually happens: his accounts are spread across K0i2 and CCGP. **Fix direction, and it needs its own design cycle:** per-clan ids, `clan.standing.points.k0i2`. That keeps the property the fixed ids exist for — a leader names one id in Discord and forty members set the same alert, because a K0i2 id is identical on every machine — while letting one person hold alerts for two clans. Costs to weigh: the id space grows with clan names, an alert does not follow a clan rename, RoRoRo sees ids it has no list of, and the threat label's "which clan is ours" stops being a guess. **This supersedes the row's original framing**, which was that a second sending clans list makes the ids ambiguous and should perhaps be forbidden. That is real but it is the symptom: `AppServices.CreateWatch` builds a watch per source and the ids are fixed rather than per source, so two enabled lists report under the same ids, and the two threat numbers put the rule's LABEL in contention as well. The label half is fenced WITHIN one process (`RecipeWatch.ManagedLabelGate`) so no watch's write lands inside another's write-then-report pair. Per-clan ids would dissolve most of the rest. Restricting to one sending list stays available as a cheap stopgap, and `rororoblox-69` argued for it on 2026-09-20 with the point that the user must then be told WHICH list is the sending one or the second one just looks broken. **Two hazards to design against before anyone builds per-clan ids, raised by `rororoblox-69` 2026-09-20.** (1) MIGRATION: every `clan.standing.*` rule that exists today stops matching, and stops SILENTLY — a rule whose metric id nobody reports is not an error, it is a rule that never fires, which is indistinguishable from "nothing has crossed the line yet". The owner has such rules on his own board and the clan's Discord post for RoRoRo 1.30 names that shape. Cheapest honest migration is to emit the bare id for the best-placed clan AND the per-clan ids together for at least one release, so nobody's phone goes quiet without being told; that doubles the sends, which is a trade for the owner rather than a decision to make quietly. (2) NAME MUTABILITY: an id built from a clan's display name is stable only while the clan keeps that name, and a rename would break every member's alerts at once with no error anywhere. CHECKED, and it softens this rather than settling it: the shipped recipes take no id field at all — the top-clans list reads `name`, `points`, `members`, `memberCapacity` and `contributorCount` from `data.topClans`, and the clan-battle recipe addresses a clan BY NAME in its own URL, `/api/clan/{clan}`, where `{clan}` is what the owner typed in Setup. So the app already keys everything on the display name, and a rename already breaks the source itself, visibly, forcing the owner to act. A name-derived metric id would be no less stable than the configuration it comes from. What is NOT known: whether a `data.topClans` row carries a stable id the recipe simply does not take. Only a live response answers that, and nothing here automates against the real API, so it wants checking by hand against one capture before the design settles. If such an id exists the metric id should be built from it; if it does not, a rename is a breaking event that must be said on the card rather than discovered. **Also multiplies two ceilings:** V3-S.28 (the history budget has no term for a subjectless metric at all — verified, `HistoryBudget.Count` counts accounts x stats and `HistoryBudget.cs` names neither clan nor field) gets worse when six ids become six per clan; and RoRoRo's own `MetricHistory` is bounded at 256 distinct (account, metric) pairs and REFUSES new series at the bound rather than evicting, deliberately, so junk cannot push out a series a rule is watching. Six ids across two clans is nowhere near it; the ceiling being a refusal rather than an eviction is the part worth knowing. — src/Book/FieldSummary.cs, src/Core/FieldMetrics.cs, src/Composition/AppServices.cs — found while closing V3-S.32, reframed by the owner 2026-09-20.
- V3-S.37 **FIXED** 2026-09-21 — **a test is worth only what it discriminates, and seven on this branch discriminated nothing.** V3-S.31 named this as the codebase's biggest weakness — "the strongest promises live in prose, and the fixtures are shaped so the invariant cannot fail" — and recorded it as a finding about code already written. It is not historical. Seven tests written on 2026-09-20 and 2026-09-21, by implementers carrying that row's warning in their briefs, initially PASSED under the exact regression they existed to catch: the Gap sign-flip (`Value` and `Gap` were both 500, so the assertion held either way); the two case-fold call sites (one test covered for the other); the best-placed anchor (the decoy clan sat ADJACENT to the true one, so a wrong anchor produced an identical list); the `ends` cap's positive path (an implementation rejecting every non-null `ends` passed the whole suite, which would have disabled threat alerts for every live battle); `ManagedLabel` on the wrong entry; and the race note's noun ("No sources are named here" shipped through eight task reviews, a whole-branch review and a fix wave, because the test pinned the explanatory clause and never the subject of the sentence). Every one was found by deliberately breaking the implementation and watching; not one by reading the test. **The rules, which are the point of this row:** (1) before trusting a new test, BREAK the code it covers in the way it exists to catch, watch it go red, and put that capture in the record — a test never seen to fail has not been shown to work. (2) Build the fixture so the decoy is distinguishable: a wrong anchor must change the output, a `Gap` must differ from a `Value`, a cap must have a positive case as well as a negative one. (3) When a sentence is assembled from a noun and a clause, assert the NOUN — the clause is the part that reads as the interesting half and is usually the part that is already right (`rororoblox-77`, 2026-09-21). (4) Some of these are only reachable by running the app; the noun bug was invisible to every form of reading. — docs/backlog.md V3-S.31, tests across the threat-alerts work — found by the owner's control-folder walk, 2026-09-21. The four rules now live in `docs/testing.md`, written for whoever writes the next test here rather than as a case file, with the repo's own examples of each. This row stays as the record of the seven tests that produced them. Ratified 2026-09-21.
- V3-S.38 **FIXED** 2026-09-21 — **the full suite stalls sometimes, and now says which kind of stall it is.** A run hung for over ten minutes on 2026-09-21 with the test host alive and idle — 3 seconds of CPU across thirteen minutes, so waiting on a handle rather than spinning — and a re-run passed in 26 seconds. The Counts block has recorded this since 2026-09-17 as "not reproduced or diagnosed". It has now reproduced once, and five deliberate attempts to catch it again under `--blame-hang` all passed clean in 27-28s, so the cause is still unknown. **What changed is that the next occurrence diagnoses itself.** Run the suite as `dotnet test tests/Ur-Score.Tests.csproj -c Release --blame-hang --blame-hang-timeout 120s --blame-hang-dump-type mini`. Every healthy run prints "All tests finished running, Sequence file will not be generated", which gives a decisive fork: that line present with no sequence file means NO test was in flight and the hang is at process exit — for a WPF test assembly that means a Dispatcher or a window left alive, which fits the idle-CPU symptom; a sequence file instead NAMES the test that was running and the exit theory is wrong. One caution learned the hard way while chasing this: do not pipe the run through `tail` or `head`, because the pipe buffers everything until the process exits and an apparently silent run is the pipe, not the program. **DIAGNOSED 2026-09-21, and it is not what the symptom suggested.** A hang dump and blame SEQUENCE file from the original 2026-09-17 stall were still on disk under `tests/TestResults/` (gitignored, so they survived unnoticed). The sequence file settles it: of 1,265 tests recorded, 1,259 completed and SIX did not — `RowListAutomationTests`, `ThemeFenceTests`, `MyAccountsAccessibilityTests`, `AlertResultThemeTests`, `DragHandleTests` and `BoardMenuTests`. Exactly eight test files in the suite create an STA thread and `Join()` it with no timeout; six of those eight are the six that hung, all at once, which is parallel collections each stuck on its own `Join`. The remaining two of the eight, `LegacyRecipeUpdateTests` and `PopOutLifecycleTests`, happened not to be in flight. So it is not a hang at process exit — that was my guess from the idle CPU and it was wrong — and it is not one bad test: it is a CLASS of test that can block forever, because a `Join()` with no timeout cannot fail, only wait. Fix direction, and it is small: give every one of those eight a `Join(timeout)` and fail the test when it expires. That turns an indefinite stall that takes a human to notice into a named failing test, which is the whole difference. Whether the WPF work inside those threads can be made not to block is a separate and larger question; the timeout is worth doing either way, because a test that cannot fail cannot tell you anything. **FIXED 2026-09-21.** All eight now `Assert.True(thread.Join(UiThread.Longest), ...)`. Thirty seconds, deliberately generous and written down as such: this is not a performance budget — these tests finish in milliseconds — it is the line past which waiting has stopped being waiting, and a number tight enough to trip on a loaded machine would trade a rare hang for a common false failure, which is the worse bargain. Proven by mutation per V3-S.37: against a thread made not to finish, the wait failed AT its limit, named the test, and printed the window it gave up on. **And then it earned itself the same afternoon.** A full run stalled again within the hour and, instead of hanging, came back in thirty seconds having named the tests. The remaining question is now V3-S.40, which that run turned from a guess into evidence. — tests/UiThread.cs, tests/ — found 2026-09-21, diagnosed and fixed the same day.
- V3-S.39 **OPEN**, re-opened a second time 2026-09-21 — **`dotnet build Ur-Score.csproj -c Release` intermittently fails with CS5001, "no static Main".** Four times on 2026-09-20 and 2026-09-21. It is the WPF XAML-generated entry point going missing on a build that should have produced it; a plain rebuild with no other change succeeds every time, which is why it reads as noise rather than a fault. It costs a cycle each time and, worse, it teaches whoever sees it to re-run a red build without reading it — which is exactly how a real CS5001 would be missed. First seen with `--no-incremental`, since seen without it. **DIAGNOSED 2026-09-21.** A build finally failed with the error underneath it rather than the symptom: "The process cannot access the file 'Ur-Score_<hash>_wpftmp.csproj' because it is being used by another process", followed by a run of `CS2001: Source file '...RecordsPanel.g.cs' could not be found`. The WPF build generates each XAML file's `.g.cs` through a TEMPORARY project; when that temp project is locked, generation fails and the generated sources never appear — and for `App.xaml` the missing one is the entry point, which is CS5001 exactly. So it is not the entry point going missing mysteriously, it is XAML generation losing a race. The lock comes from overlapping builds: `dotnet build` runs MSBuild nodes with `nodeReuse:true`, so they outlive the build that made them and were measured live at 3 on this machine while nothing was building. Fix direction: put `-nodeReuse:false` on the documented build command, or stop builds overlapping. Also seen once on 2026-09-21, and probably the same family: a full run reported "Test Run Aborted" after 1,431 of 1,455 tests, with an immediate re-run clean — a crash rather than the V3-S.38 hang, and worth catching the next time it happens rather than re-running past it. Worth knowing before it is chased: building the TEST project alone fails the same way every time and for a understood reason (it drags in the WPF app project and the generated entry point is not produced in that context), so the flake is the same error arriving from a different place and the two must not be conflated. **Narrowed 2026-09-21**, and not by putting the flag on a documented command, which was the row's own direction and is the weaker half of it. A documented command only reaches whoever reads the documentation, and this failure is met by whoever types `dotnet build`. `Directory.Build.rsp` at the repo root is read by every msbuild started from this tree — checked by hand, not assumed: a deliberately invalid switch in it failed the root project, the tests project from its subdirectory, AND `dotnet test`, all three. `tests/BuildFenceTests` asserts the line is still there, because deleting it would break no build today and only make a future one lie; mutation-tested by commenting the line out and watching the guard go red. Worth recording that the flake reproduced ONCE more while this was being fixed, on a command that already carried `-nodeReuse:false`, with stale nodes from an earlier build still alive — the flag governs the nodes a build leaves behind, so it protects the NEXT build rather than its own. **RE-OPENED the same day, because it came back.** A `dotnet test` run failed with CS5001 again with the rsp in force AND zero MSBuild nodes alive — checked, not assumed — immediately after a `git checkout master` plus `git reset --hard` had rewritten the source files under a warm `obj/`. An immediate retry succeeded, as it always does. Five deliberate attempts to reproduce it (three touching `src/App.xaml`, two rewriting its contents, each against a warm `obj/`) all passed clean, so **the rewrite-under-warm-obj idea is a guess and is recorded as one, not as a finding.** What IS settled: node reuse was a real cause and removing it removed that path, but it was not the only path, so the earlier FIXED claim was too strong and this row should not have been closed on one clean afternoon. The rsp and its fence stay — they are right, just not sufficient. Next occurrence, capture the FULL build log rather than grepping it for `error`: the lock message that diagnosed this the first time ("the process cannot access the file ... `_wpftmp.csproj`") appears ABOVE the CS5001 and is lost by a grep that only keeps the error lines — which is how four occurrences got recorded as "no static Main" and nothing more. **ROOT-CAUSED 2026-09-21, and the earlier guesses were all wrong.** What finally caught it was following this row's own instruction — capture the FULL build log instead of grepping it for `error` — together with a reproduction that had been missing one detail. Twelve earlier attempts failed because they rewrote files with BYTE-IDENTICAL content; every real occurrence had changed a file's content. Rewriting a source file with genuinely different bytes and then running `dotnet test` reproduces it at about **one build in six**.
  **The actual error, which CS5001 was hiding:** `Microsoft.WinFX.targets(468,8): error MSB3061: Unable to delete file "obj\Ur-Score_<hash>_wpftmp.csproj.nuget.g.props" ... because it is being used by another process.` WPF compiles a XAML-bearing project a second time through a generated temp project with its own copy of the NuGet props, and deletes both at the end of the pass. When that delete loses a race it fails with MSB3061 — which names itself perfectly well. **The failure people actually see is the NEXT build**, which finds those files left behind, does not run markup generation, never produces `App.g.cs`, and reports CS5001. So the bug was never the race; it was the confusing error the race leaves behind for the following build, which is why four occurrences got recorded as "no static Main" and nothing more.
  **The fix breaks the chain rather than winning the race:** a `ClearStaleWpfTempFiles` target deletes leftover `*_wpftmp.*` before every build, with `ContinueOnError` because a file still held is exactly the case it exists for. A lost race now costs one legible MSB3061 instead of a mystery on the next build. **Measured, not asserted:** 24 consecutive runs of the reproduction clean against a measured 1-in-6 baseline — about a 1.3% chance of that by luck, so strong evidence and not proof. If CS5001 is ever seen again this row reopens.
  **Ruled out, checked rather than assumed:** `IncludePackageReferencesDuringMarkupCompilation=false` would remove the `.nuget.g.props` from the dance entirely, and it breaks this project — it also drops source generators from the markup pass, so the `[GeneratedRegex]` partials fail with CS8795. Tried, watched fail, reverted.
  **What the row does NOT claim:** that the race is gone. The holder is most likely on-access virus scanning, which fits every symptom — intermittent, only after a file's CONTENT changes, gone on an immediate retry, and untouched by `-nodeReuse:false` because the holder is not MSBuild. Real-time protection is on and this machine's exclusion list needs an administrator to read. Excluding the repo's `obj` folder is the likely real cure and is the owner's call, not a change to make on his behalf. The `Directory.Build.rsp` and its fence stay: node reuse was a genuine second path to a locked temp project.
  **RE-OPENED THE SAME EVENING, keeping the deal the row made.** CS5001 came back once more with the cleanup target in place — and this time with NO MSB3061 in the full log, on a build right after a change to a TEST file. So the race has at least two faces: the delete at the end of the markup pass (caught, MSB3061, mitigated by the cleanup target) and something earlier in the same pass that produces CS5001 directly with no leftover to clear. 24 clean reproduction runs were strong evidence for the first face only. The root cause stands, the mitigation stands and demonstrably narrows it, and it is not sufficient. **The owner has agreed to the `obj` exclusion** (2026-09-21, "exclude that if it helps and doesn't hurt"); it needs an elevated shell, which this session is not, so the one-line command is his to run: `Add-MpPreference -ExclusionPath 'C:\Users\estev\Projects\Ur-Score\obj'`. If CS5001 is seen after that, the on-access-scanning theory is wrong too and the full log of that occurrence is the next evidence.
  **It was seen after that, and the scanning theory was wrong. THE ACTUAL CAUSE, found from the process tree the same evening:** three MSBuild worker nodes alive with `/nodeReuse:true /noautoresponse` — nodes that read no response file, so nothing in `Directory.Build.rsp` ever reached them — one of them a child of **C# Dev Kit's project-system build host** (`visualstudio-projectsystem-buildhost`, under VS Code's ServiceHost, alive since 10:53), the other two orphaned by its earlier design-time builds. Dev Kit runs a design-time build on every save, in the SAME `obj\` as a `dotnet build` typed a moment after the save, and the two collided on the WPF temp project's files there. That is every symptom at once: only after a file's content changes (a save is what triggers the design-time build), intermittent (it depends on which of the two reaches the temp files first), gone on retry (the design-time build has finished by then), untouched by `-nodeReuse:false` (not this build's nodes) and untouched by the AV exclusion (not the scanner).
  **The fix separates the two trees.** `Directory.Build.props` sets `BaseIntermediateOutputPath` to `obj/designtime/` when `DesignTimeBuild` is true — the global property design-time builds carry — and excludes `obj/**` from the default compile glob, because with two trees the SDK excludes only the one a given build uses and the other's generated `AssemblyInfo.cs` becomes source (CS0579, seen and fixed). Confirmed live: Dev Kit re-evaluated the moment the props file changed and its next design-time build landed in `obj/designtime/Debug`. **Two routes tried and rejected first:** `-p:BaseIntermediateOutputPath` in the response file moves the command-line side instead, and it stops `dotnet test` after its restore step with no tests run — tried, watched, reverted. And the `obj` exclusion, which the owner ran, is harmless and can stay but does nothing for this. The cleanup target and the rsp stay too; each removes a real path. `BuildFenceTests` now pins the props file the way it pins the rsp. **Still not closed on the strength of an afternoon** — the deal stands: days of ordinary work with no CS5001. But for the first time the cause is a process that was watched, not a theory that fit.
  **Seen twice more the same evening, with the split in place** — both right after an edit to a TEST file. Checked: the split holds for the app project (Dev Kit's 47 design-time files are under `obj/designtime/`, `obj/Debug` untouched for half an hour), but `tests/obj/` has no `designtime` folder, and the WPF temp `*_wpftmp.csproj` is written to the PROJECT ROOT by every build regardless of where its intermediates go — where a project-system file watcher can see a new `.csproj` appear and try to load it. That is the remaining suspect and it is a suspect, not a watched process. The cheap mitigation is a workspace file-watcher exclusion for `**/*_wpftmp.csproj`, and `.vscode/` is gitignored here, so it is a local setting for the owner rather than a commit. — Ur-Score.csproj, Directory.Build.props, Directory.Build.rsp, tests/BuildFenceTests.cs — found 2026-09-20, root-caused twice 2026-09-21, the second time correctly, and still not quiet.

- V3-S.45 **OPEN** — performance: **the reader holds five times the book's size in memory.** The season's book from S1-F.3's instrument is 60.5 MB on disk and 326 MB resident once loaded: 100,846 `BookLine`s at about 3.3 KB each, every one carrying its inputs, headline, stats and per-account dictionaries as the JSON gave them. Charts read four things from a line — its time, its period, a headline value and an account's stat value — and `Remembered`/`LastReading` want the whole line only for the newest one per source. A compact reading record for the kept five weeks (shared input and period strings, arrays not dictionaries), with the full `BookLine` kept only where a whole line is asked for, would bring that down by most of it. Design first, since the reader's shape is what every panel builds from; the benchmark reports the number on every run so the change is measurable — src/Book/ScoreBookReader.cs; tests/BookBenchmarks.cs — Sweep D measurement, 2026-09-22
- V3-S.44 **FIXED** 2026-09-21 — code tidiness, but it keeps generating wrong backlog rows: **a namespace shadows a type, and 25 files carry an alias to work around it.** `Labs626.UrScore.Source` is a namespace (6 files under `src/Source/`: AvatarBook, IconClient, JsonNav, NameClient, SourceIcons, UrScoreIdentity) and `Labs626.UrScore.Core.Source` is a type. Outside `Core`, the namespace wins, so `Source` alone does not compile and 25 files carry `using Source = Labs626.UrScore.Core.Source;`. **The reason this is worth a row rather than a shrug:** it reads as clutter, so reviewers keep filing it as removable clutter — S2-2.1, S1-7.1 and S1-9.5 all did, and all three were wrong. Each one costs somebody the same deletion, the same CS0118, and the same re-derivation. A comment now sits on the alias in `BoardEdits.cs`, but comments do not reach the next reviewer reading a different file. **Fix direction:** rename the namespace — `Labs626.UrScore.Sources` or `Labs626.UrScore.Fetch` — which deletes all 25 aliases and the whole class of finding. Mechanical and compiler-verified: 6 namespace declarations, 25 alias lines, and whatever `using` lines follow. Deliberately NOT done inside a tidiness sweep, because a 31-file rename is a bigger diff than any row it closes and wants to be its own change against a quiet tree. **Check before doing it:** whether any namespace string is used for reflection or serialization type names, which a compiler will not catch. — src/Source/, and the 25 files listed by `grep -rl "using Source = Labs626.UrScore.Core.Source"` — found 2026-09-21 by the third row to get it wrong. **FIXED 2026-09-21 on the owner's go-ahead** ("yes as its own PR after A or before if better" — before, because it deletes alias lines from files every later sweep touches). `Labs626.UrScore.Source` is now `Labs626.UrScore.Fetch`, folder and namespace both, which is what the six files are: the avatar, icon and username clients, the JSON helper they share, and the User-Agent. Checked first that no XAML, config or string literal named the old namespace (only gitignored SDD briefs did). All 28 alias lines and the three comments written today to explain them are gone, and so are the fully qualified names in `LineBuilder` and `DiagnosticsModel` that only existed because of the collision. The compiler found every site the rewrite missed — seven files that had reached `Source` only through the alias and now needed the `Core` using — and five fence tests that pin file locations by path failed on the moved folder and were repointed, which is those fences doing their job. 1,492 tests, no behaviour change.
- V3-S.43 **FIXED** 2026-09-21 — **a smoke walk seeded a data folder that could report to RoRoRo, and did not know it.** The control-folder walk for V3-S.42 copied `626labs.ur-score.control` into the live path and launched the app. That folder's `settings.json` has `"startOnOpen": true` and its clan recipe has `"send": true` on `clan.battle.points` — the one metric the owner has a rule on, pointing at his real phone and Discord. Nobody pressed Start; `startOnOpen` is what starting means. RoRoRo was up throughout (1.29.0.0, since 15:28), and 1.29 alerts repeatedly on a state rather than once on a crossing, because the crossing rule shipped in 1.30. Caught by `rororoblox-77`, whose warning arrived after the walk had already run.
  **Two beliefs made it look safe and both were false.** (1) That a walk only reads when it is told to. It reads when the seeded settings say to, and nobody had read the seeded settings. (2) That a build out of `bin\Release` cannot reach the host anyway. It can: the host's `Handshake` resolves a plugin with `_registry.FindById(request.PluginId)` — the id the caller CLAIMS — with no path check, no signature check and no binding to the installed exe, so a dev build is indistinguishable from the installed plugin (checked in the host's source by `rororoblox-77`, not assumed). **Every walk must be written as though it connects, because it does.**
  **Fixed at the source rather than at the host.** `Copy-UrControlData` forces `startOnOpen` false and every stat's `send` false, sets `sentFieldMetrics` empty, then RE-READS the folder off disk and throws if anything could still report — a separate `Assert-UrDataSendsNothing`, because a check that trusts the variable it just wrote is not a check. A folder with nothing set to send cannot send, whichever host version is up, which is why this beats any test of the host's state. Proven by mutation: putting one `send` back and putting `startOnOpen` back each make the assert throw and name the file. Quitting RoRoRo for the duration is a real second layer and the README says so, but it closes the owner's notification host, so it stays a decision taken each time rather than something a script does on its own.
  **What actually went out is NOT knowable from this machine, and that is its own finding.** The host writes nothing on the metric path — `PluginHostService.ReportMetric` logs nothing, `Handshake` logs nothing on accept or reject, and `AlertRouter` has no logger at all — so a day's log showing zero metric lines is the absence of logging, not the absence of events, and `MetricHistory` is in-memory with nothing persisting it. `Restore-UrData` deletes the seeded folder, so Ur Score's own trail and book for those seconds are gone too. The only instrument holding the answer is the owner's phone and Discord channel. **What narrows it:** the control folder's `sources.json` is byte-identical to the live one, so nothing synthetic was ever in play — K0i2 is `main` and sends, CCGP is `watch` and sends nothing, and the values read were the real API's. The recorder was stopped for the walk, so a report from the seeded instance SUBSTITUTES for the one the recorder would have made rather than adding a spurious sample. The single rule is Rate, threshold 100/min over 10 minutes, `alertWhenBelow` true, and a ~16-second instance against a 180-second reporter (the interval is on the recipe and visible in the walk's own screenshot, "Asks every 180 s", rather than taken from memory) almost certainly delivered one observation, where `RatePerMinute` needs two.
  **The conclusion those facts support, stated in its strongest correct form** (agreed with `rororoblox-77`, who raised the hazard and then accepted the narrowing): **if an alert fired, it was a TRUE alert.** The dangerous case needed a synthetic value close to the real one, producing a near-zero rate the real series would not have produced. There was no synthetic value; every number that could have reached the host was the real current one for K0i2, taken the way the recorder takes it, and the recorder was stopped so the observation substitutes rather than sits beside one. The series the host evaluated is the series it would have had anyway, give or take a few seconds of timestamp. **The walk could not manufacture a breach; at most it let a genuine one arrive slightly early.** That is a different claim from "we do not know whether the phone buzzed spuriously", and it is the one the evidence actually supports. It is still not proof that nothing fired — only the owner's phone and Discord hold that — and narrowing is not recorded here as proof. — tools/smoke/uia.ps1, tools/smoke/README.md — caused and fixed 2026-09-21.
- V3-S.42 **FIXED** 2026-09-21 — you'd notice: **an installed recipe could never be updated, so every user was pinned to whatever they first imported.** An installed recipe is a SNAPSHOT of the text imported (`{slug}.recipe.json` holds the exact text). `RecipesModel.BuiltIn` hides a built-in the moment it is installed — right in itself, since offering it again would read as a second copy — and the only other route was to find the recipe's JSON somewhere and import it by hand, which for a built-in means extracting an embedded resource out of the exe. So a fix to a shipped recipe could never reach anybody who already had it, including the owner. Raised as a release blocker; the owner ruled on 2026-09-21: "I don't know that there's any existing users except for me, but I don't want to get stuck with anything, so let's fix that."
  **The machinery was never missing, only a way to reach it.** `ImportFlow.RunTextAsync` already decides install versus update, compares the two, re-asks about hosts only when they actually changed (`ImportReview.AsksAgain`), and carries the user's choices across. So the fix adds no update logic at all: `RecipesModel.CanUpdate` says when Ur Score carries a newer copy, the row grows an Update button and a line saying why it appeared, and the handler is literally the add handler — the flow already knows the difference.
  **The name-and-author clause is the part that is not belt-and-braces.** A slug is DERIVED from name and author (`Recipe.Slug`), so two different recipes can share one: "Pet Sim 99 clan battle points" and "Pet Sim 99 Clan Battle Points" slugify identically. `ImportFlow` refuses that case outright ("rename one of them before importing"), so without the clause the button would appear on somebody else's recipe and be guaranteed to fail. The predicate asks the same question the flow asks, which is what stops the two drifting. Mutation-tested per V3-S.37 both ways: dropping the clause fails the same-slug test, dropping the text comparison fails the identical-text test and the row test.
  **Not yet walked.** This puts a new button on Setup › Recipes and a new line on the row, and the standing rule is that built UI is screenshot-compared before it is called done. That needs a launch, which takes the recording lock, so it waits for an owner-watched walk. — src/UI/Setup/RecipesModel.cs, src/UI/Setup/RecipesPage.xaml(.cs) — raised as a release blocker, ruled and fixed 2026-09-21.
- V3-S.41 **OPEN** — you'd notice: **V3-S.35's trace stops at Ur Score's window, and the phone is the whole premise.** A held-back threat number now leaves three marks — `ReportPolicy.Held`, the board's read line and the Alerts card — and every one of them is on this machine's screen. Somebody watching the board sees it; somebody relying on a push does not, and relying on a push is the reason any of this exists. **Raised by `rororoblox-77`, 2026-09-21, from the host side and checked there rather than assumed:** RoRoRo's `AlertKind` (`src/ROROROblox.Core/Discord/AlertTrigger.cs`) is exactly `AccountDroppedOut`, `MemoryWarning`, `Recycled`, `UptimeMark`, `MetricBreached`, `MetricRecovered` — there is no kind for a metric that STOPPED ARRIVING, and nothing in `MetricHistory` or `AlertRouter` fires on absence. So three states are indistinguishable to the host, and all three read as fine: Ur Score is down; Ur Score is up and holding a number back; Ur Score is up, reporting, and nothing has crossed. Their own `UptimeMark` doc comment already writes the limit down — "its ABSENCE is the only dead-PC signal a PC-fired alert system can give" — which was written about the PC dying and applies unchanged to a metric dying while the PC is fine.
  **What is ours and what is theirs, because the split is the useful part.** An absence check is host-side and `rororoblox-77` owns it. But ONE of the three faces is reachable from here with no host change at all: report the held count as a number of its own, and the user sets an ordinary `Level` rule above zero on it — `AlertKind` already has `Level` (`src/Core/RulesFile.cs:10`), so a rule that fires when Ur Score is holding something back needs nothing RoRoRo does not already do. **And the reason that is not simply the fix:** a number only carries while Ur Score is running. A reporter that is DOWN sends no held count either, so this covers "up and holding back" and cannot touch "down" or "quiet" — exactly the two faces an absence check is for. Worth knowing before anyone builds it and discovers the gap afterwards.
  **Costs to weigh first.** A ninth fixed metric id lands straight in V3-S.36's lap (every id is fixed and shared, and per-clan ids would multiply this one too) and in V3-S.28's (the history budget has no term for a subjectless metric at all). A held count is subjectless like the other eight. Neither is a blocker; both mean this wants deciding alongside V3-S.36 rather than before it.
  **Sequencing, agreed with `rororoblox-77` 2026-09-21 and the practical conclusion of the whole exchange.** Number the faces: (1) the reporter is down, (2) it is up and holding back, (3) it is up and nothing crossed. The held metric buys face 2 and structurally cannot touch 1 or 3; the absence check has to cover 1 and 3 whether or not the held metric exists. They are not alternatives and neither subsumes the other — so if only one is ever built, the absence check is the one that covers the case that actually cost ten hours (2026-09-20 overnight). It is also the cheaper of the two to start: entirely host-side, no plugin change, no new destination and no new settings row, the same shape `TellMeWhenItRecovers` took in v1.30. So RoRoRo can move without waiting on Ur Score's metric-id decision, and probably should, rather than this side paying an id cost for the smaller face first. **Which makes the recommendation here: do nothing yet.** Not blocked, not hard — just second in line behind work that is free of our constraints and worth more. — src/Core/ReportPolicy.cs, src/Core/FieldMetrics.cs, src/Core/RecipeWatch.cs — raised by `rororoblox-77` 2026-09-21 from the host side, recorded here for the half that is ours.
- V3-S.40 **FIXED** 2026-09-22 — **six STA tests block together, and it is contention on something process-wide rather than one bad test.** V3-S.38 called this "a separate and larger question" and left it, correctly: the timeout was worth doing whatever the cause. The bound then paid for itself within the hour. A full run on 2026-09-21 stalled and, instead of hanging, came back in thirty seconds naming at least eight failing tests across six classes — `RowListAutomationTests`, `ThemeFenceTests`, `BoardMenuTests`, `AlertResultThemeTests`, `MyAccountsAccessibilityTests` and `DragHandleTests`. **Those are the same six classes the 2026-09-17 blame sequence file named**, out of the eight files in the suite that use an STA thread, and an immediate re-run passed 1,440 clean. Two readings of that match, and which one is right decides the fix: either these six share something the other two do not, or all eight are equally exposed and the two spared were simply not in flight (which is what the 2026-09-17 note concluded, and the same reasoning applies again). What it is NOT is one bad test — each of these creates its own thread and its own dispatcher, so six blocking at once means they are queueing on something the process shares, and xUnit running collections in parallel is what brings them together. Next step is to find the shared thing rather than to guess at it: capture a full hang dump on the next occurrence (`--blame-hang --blame-hang-timeout 120s --blame-hang-dump-type full`) and read the stacks of the six STA threads, which will name the lock directly. Do not pipe that run through `tail` or `head`; the pipe buffers until exit and an apparently silent run is the pipe, not the program. — tests/, V3-S.38 — found 2026-09-21 by the fix for V3-S.38. **Stacks taken, cause named, precondition removed.** It hung again on 2026-09-22 — the suite now four seconds long (S1-5.6), nine WPF classes starting cold in the same instant — and this time `dotnet-stack report` was run against the stuck host before it was killed: 28 threads, kept in `docs/2026-09-22-sta-stall-stacks.txt`. Every stuck STA thread is inside WPF's first-touch static initialisation: `ContentPresenter..cctor` under `ItemsControl..cctor` under a XAML parse on one thread, `WpfSharedBamlSchemaContext.GetKnownXamlType` / `GetKnownBamlMember` on others, `Application`'s constructor on a third — nine classes, the same six this row named from the 17th and the 21st among them. A type's static constructor runs under a per-type lock the runtime holds, WPF's schema context takes locks of its own inside those constructors, and two threads initialising overlapping sets of WPF types at once can each hold what the other needs; the runtime does not break that cycle. Not one test's fault, and not something a test can be written to avoid: it is what starting WPF on several threads in one instant does. **The fix removes the concurrency:** every WPF-touching test class is in `WpfCollection` (non-parallel), a fence fails the build for one that is not, and `docs/testing.md` §5 records the rule and the diagnosis recipe. Ten consecutive full runs clean at 5 s. **Honestly stated:** it is a race — three runs with the collection removed also passed — so this closes on the stacks and the mechanism, not on a reproduction. The row said the stall could not be forced; it was right, and it did not need to be.

Owner actions noted in the smoke, not Ur Score defects:
- RoRoRo has no alert rules file yet ("nothing can alert until a rule is added"): add a rule for `clan.battle.points` in RoRoRo before the battle.
- CCGP isn't in the current battle (ArcadeBattle2026 ended 2026-09-11); live numbers for it start when the next battle begins.

---

# 0.3.2: the Alerts page and avatars

Every Minor from the reviews of `feat/alerts-card` (`cec5987..4ce08bb`, the full cycle), checked against the tree at `4ce08bb` on 2026-09-15. The execution record, with the rulings and the three lessons, is in `docs/plans/2026-09-15-alerts-card.md`. Reviews: `task-1`, `task-1-rereview`, `task-2`, `task-2-rereview`, `task-3`, `task-4`, `task-6`, `task-7`, `task-8`, `task-9`, `branch-review`, `branch-rereview`, `branch-review-2`.

**52 OPEN, 37 FIXED, 2 GONE — 91 distinct items** (recounted from the rows 2026-09-17, after visible Wave 2 implementation) from the thirteen reviews' raw findings; five are restatements across reviews, merged onto one line each with both sources named. Tasks 1-6's Critical and three Importants, and Task 8's Critical and two Importants, were fixed in their own rounds and are in the execution record, not here. The second whole-branch review's Critical and four Importants are kept below (AC-B2.1-AC-B2.5) instead: when this section was extended to close the gap Important 4 named, only Important 4 had a completed fix (this update) and the Critical's fix round was still in flight in another agent's hands — `git log` now shows it landed at `4ce08bb`, so AC-B2.1 is recorded FIXED; the three README Importants (AC-B2.2-AC-B2.4) are still open.

## Task 1: rules file operations

- AC-1.1 **FIXED** — code tidiness: `Remove` repeated `Guard`'s blank-metric-id check and its message word for word — src/Core/RulesFile.cs:164-166; now the shared `GuardId` (aed8848) — T1 review
- AC-1.2 **FIXED** — a root-only override path such as `C:\` threw unhandled out of `Path.GetDirectoryName(path)!` — src/Core/RulesFile.cs:497; `NamesAFile` now refuses it before any IO (aed8848) — T1 review
- AC-1.3 **OPEN** — code tidiness: `Build` omits `label` entirely when `spec.Label` is blank rather than falling back to the metric id, so "every rule Ur Score writes carries owner and label" isn't self-enforced there — src/Core/RulesFile.cs:533-544 (:568 at aed8848) — T1 review, T1 re-review (left as ruled)
- AC-1.4 **FIXED** — test gap: an infinite threshold (`1e400`) was never exercised end to end, only claimed in the report — tests/RulesFileParityTests.cs `AThresholdTooBigForANumberReadsAsInfinity` (aed8848) — T1 review
- AC-1.5 **OPEN** — a numeric kind (`"kind": "7"`) is skipped here but loaded by RoRoRo, which parses without an `IsDefined` check; no writer emits one — src/Core/RulesFile.cs:404 — T1 review, T1 re-review (left as ruled)
- AC-1.6 **OPEN** — in the rare `ERROR_UNABLE_TO_MOVE_REPLACEMENT_2` partial-swap state the cleanup deletes the temp file unconditionally, destroying the only surviving copy of the new content and leaving the rules file missing rather than unchanged — src/Core/RulesFile.cs:529-536 — T1 re-review (new; parked)

## Task 2: the Alerts model

- AC-2.1 **FIXED** — you'd notice: a number one step below the refusal's own limit displayed rounded up to it ("999999999999999.9" showed as "1,000,000,000,000,000"), and Change then Save rewrote the rule rounded — src/UI/Setup/AlertCards.cs:87, 197-203, 308; `TooManyDigits`/`MaxDigits` now refuse more than 15 significant digits (f9fd6f7) — T2 review Minor 1
- AC-2.2 **FIXED** — AlertCards uses compact round-trip formatting for extreme finite values and opens Change with the existing editing-limit refusal. Unsupported exponents remain unsaveable; an explicit valid replacement is required. Non-finite rules keep their existing remove-only behavior. Tests cover 1e15, 1e300, double.MaxValue, exponent/non-finite input, exact value preservation, replacement and ordinary round-trips; RulesFile regressions pass. Narrow-width desktop smoke pending. Original finding: T2 review Minor 2, T2 report.
- AC-2.3 **OPEN** — focus after Remove with a hand-made duplicate lands on the copy's Change instead of "+ Add an alert" as A15 says; the target is valid and visible — src/UI/Setup/AlertCards.cs:418 — T2 review Minor 3
- AC-2.4 **OPEN** — accessibility: focus drops to the window when `FocusName` returns "" (Remove on a locked file, or removing a stale card's last rule), and with no live region a Narrator user hears nothing — src/UI/Setup/AlertCards.cs:405-422 — T2 review Minor 4 (plan-mandated)
- AC-2.5 **FIXED** — `Same` compared whole `AlertRule` records, `Index` included, so a row someone else inserted higher in the file redrew the cards and dropped your keyboard focus — src/UI/Setup/AlertCards.cs:134-138; now `Placeless` (f9fd6f7). Residue: AC-B.12 — T2 review Minor 5
- AC-2.6 **FIXED** — `Check` accepted any positive minutes string, "Infinity" included, held back only by Task 3's non-editable ComboBox — src/UI/Setup/AlertCards.cs:323; the Rate branch now matches exactly 10, 15 or 30 (f9fd6f7) — T2 review Minor 6
- AC-2.7 **GONE** — a rule's own window was offered as an extra minutes choice and lost after a refused Save — src/UI/Setup/AlertCards.cs:395; ruling 3 removed the extra choice entirely (f9fd6f7), so the case can no longer occur. The ledger counts it among the nine parked; the cost that replaced it is AC-2.13 — T2 review Minor 7 (preflight 4.13)
- AC-2.8 **OPEN** — a hand-tuned 0.125 threshold or 7.333 window shows and saves rounded to two decimals, 0.001 reads "goes below 0", and a window of 0 or less opens as 10 and saves as 10 — src/UI/Setup/AlertCards.cs:197-203, 270-275 — T2 review Minor 8 (preflight 4.12, plan-mandated by A12)
- AC-2.9 **OPEN** — the plural test compares the raw window while printing the rounded one, so 1.004 reads "for 1 minutes" — src/UI/Setup/AlertCards.cs:208 — T2 review Minor 9
- AC-2.10 **OPEN** — copy: `Failed(NotAList)` drops "Fix it by hand, then come back.", `Failed(AlreadyThere)` says "an alert of this kind" though the kind words exist, and `Failed(CantWrite)` joins with a semicolon where its siblings start a new sentence — src/UI/Setup/AlertCards.cs:226, 229, 231 — T2 review Minor 10
- AC-2.11 **OPEN** — the refusal hints mislead a writer from another locale: "1.000" gets "Use at most two decimal places", and "-5" gets "Type a number" although it is one; both refusals are safe — src/UI/Setup/AlertCards.cs:304, 305 — T2 review Minor 11
- AC-2.12 **OPEN** — test gaps: a theory over every `Failed` outcome's copy, `Managed()`, an Ur Score-owned Event rule showing Remove only, `AfterWrite` with `AlreadyThere`/`NotJson`, `Same` on `Note` or `CanAdd` alone, focus after Remove with a duplicate, three number rows, and a Level `OpenChange` row — tests/AlertCardsTests.cs (:151-169 for the number theory) — T2 review Minor 12 (preflight 4.16)
- AC-2.13 **FIXED** — you'd notice: a stored rule whose window isn't 10, 15 or 30 opened Change with an empty minutes box and nothing on screen saying why, and Save said the generic "Choose how many minutes." Change now opens saying "Ur Score offers 10, 15 or 30 minutes, and this alert uses 20. Choose one to save a change, or Cancel to leave the alert as it is.", and Save says the same until one is chosen; the choices stay 10, 15 and 30 (ruling 3) — src/UI/Setup/AlertCards.cs (`OpenChange`, `Check`, `OtherMinutes`) — T2 re-review, branch review Minor 5, fixed 2026-09-16

## Task 3: the themed Alerts page

- AC-3.1 **FIXED** — Change and Remove started from the last drawn view, so after a hand edit Change opened on old values and Remove's result sentence could describe the old rule (the write itself was always correct) — src/UI/Setup/AlertsPage.xaml.cs:87, 127-129; both now re-read the file at the click (953ee44) — T3 review Minor 1 (preflight 4.14)
- AC-3.2 **OPEN** — an unexpected exception in Turn on, Save or Remove goes only to the trail with nothing on the card, so the click looks like it did nothing; no known input triggers it — src/UI/Setup/AlertsPage.xaml.cs:105-138 — T3 review Minor 2 (preflight 3.27)
- AC-3.3 **FIXED** — test gap: Enter and Escape on a *closed* ComboBox were proven by nothing (a closed box doesn't handle either key itself) — the walk's check 2c confirms with Enter in the closed Minutes box and check 4c sends Escape from the closed Direction box (7c5b93e) — T3 review Minor 3
- AC-3.4 **FIXED** — AlertsResultLine receives the current AlertsUi and selects dynamic MagentaBrush for ResultIsProblem, WhiteBrush otherwise. AlertResultThemeTests verifies failure/success transitions and live palette replacement; desktop smoke remains pending. Original finding: T3 review Minor 4, branch review.
- AC-3.5 **OPEN** — accessibility: focus is left nowhere when Remove takes a stale stat's card away; A15 doesn't cover the case — src/UI/Setup/AlertCards.cs:423 with src/UI/Setup/AlertsPage.xaml.cs:136-137 — T3 review Minor 5
- AC-3.6 **OPEN** — the fence bans the bare substring `Preview`, so it would also block `PreviewKeyDown`/`PreviewTextInput`, the natural fix if the combo boxes ever need tunnelled key handling — tests/AlertsPageFenceTests.cs:20 (:21 at 8177b8d) — T3 review Minor 6, branch review Minor 11
- AC-3.7 **OPEN** — performance: `File.Exists` and the read run on the UI thread at every coalesced refresh; a `UR_SCORE_RULES_FILE` pointing at an unreachable share would stall the window each time — src/UI/Setup/AlertsPage.xaml.cs:51; src/Core/RulesFile.cs:106 — T3 review Minor 7
- AC-3.8 **OPEN** — code tidiness: an eighth verbatim copy of `RepoRoot()`, and the `Show` helper written out again — tests/AlertsPageFenceTests.cs:23-33; src/UI/Setup/AlertsPage.xaml.cs:186 (repeats StatsPage.xaml.cs:124) — T3 review Minor 8 (preflight 4.11)

## Task 4: the Setup › Alerts smoke walk

- AC-4.1 **OPEN** — test gap: no plain Rate "+ Add an alert" is exercised through the page; a new Rate alert is only ever reached via Change on the pre-seeded rule — tools/smoke/walk-alerts.ps1 (whole script) — T4 review Minor 1 (preflight 4.17)
- AC-4.2 **OPEN** — code tidiness: `Get-Shown`, `Get-ComboValue` and `Get-FocusedName` are walk-local, though the collapsed-twin hazard they guard is general to any page built from repeated `RowList` card templates — tools/smoke/walk-alerts.ps1:28-37 — T4 review Minor 2

## Task 6: avatars beside your own accounts

- AC-6.1 **FIXED** — you'd notice: the "Account" column header stopped lining up with the names on four surfaces once the rows gained the 28 px avatar slot — src/UI/Panels/MyAccountsPanel.xaml:17, src/UI/Panels/PromotionCheckPanel.xaml:20, src/UI/Setup/AccountsPage.xaml:20, src/UI/Panels/AccountsTablePanel.xaml:10-13; headings indented (8177b8d) and My accounts' conditional "sent" dot given a permanent slot (34569ae) — T6 review Minor 1
- AC-6.2 **FIXED** — nothing pinned A26's `Hidden`, the single point where `Collapsed` would reflow every row on five surfaces — src/App.xaml:143-146; fence added (8177b8d) and given teeth on the setter's value (ddfafc8) — T6 review Minor 2
- AC-6.3 **FIXED** — a cache file deleted mid-session took the corrupt-file path (`File.GetLastWriteTimeUtc` returns 1601 rather than throwing) and left an empty themed ring for the rest of the session — src/UI/Controls/AvatarFill.cs:26-35; the trigger now binds the converted value (8177b8d) — T6 review Minor 3
- AC-6.4 **OPEN** — nothing ever prunes the icon cache, so an account removed from RoRoRo leaves its `avatar-<id>.png` on disk; harmless (it is your own id) but it makes Step 11 item 5's count check read as a failure for anyone who has removed one — src/Source/IconClient.cs:166 — T6 review Minor 4, branch review, branch re-review (ruled: no code this cycle; a sentence went into Step 11 at 0666172)
- AC-6.5 **FIXED** — `AskForAvatars` returned before `Keep` on an empty account list, so removing every account left the whole map in memory and the forgetting promise was conditional — src/Composition/AppServices.cs:752; `Keep` now runs first (8177b8d) — T6 review Minor 5
- AC-6.6 **OPEN** — bookkeeping: two overlapping asks can re-add a just-forgotten id; the read gates cover it and the next `Keep` clears it — src/Source/AvatarBook.cs:88 — T6 review Minor 6 (parked)
- AC-6.7 **FIXED** — a doc comment claimed the brush map "cannot grow past your account list" though its key is `path|writeTicks`, so each seven-day refresh adds an entry — src/UI/Controls/AvatarFill.cs:14 (8177b8d) — T6 review Minor 7
- AC-6.8 **OPEN** — performance: a filesystem stat per row per redraw on the UI thread, inside a converter — src/UI/Controls/AvatarFill.cs:28 — T6 review Minor 8 (parked)
- AC-6.9 **FIXED** — the fence's "asked with your own ids" case only asserted the file *contained* the expression, which `AvatarFileFor` alone satisfies, so passing some other set would still have passed — tests/AvatarFenceTests.cs:65; it now pins the argument (8177b8d) — T6 review Minor 9
- AC-6.10 **FIXED** — a totals-row assertion that would pass for any pictureless row — tests/PanelModelsTests.cs:777; now asserts `IsTotal` alongside (8177b8d) — T6 review Minor 10
- AC-6.11 **FIXED** — the explanatory notes on AccountsPage use a 4-pixel gap, retaining 12 pixels before the table. AccountsPageSpacingTests guards the sequence, margins, and shared wrapping style; desktop appearance and increased-DPI smoke remain pending. Original finding: T6 review Minor 11, T6 report.
- AC-6.12 **FIXED** — the hostname fence swept `.cs` only, so a host written in prose in a `.xaml` file would have passed — tests/NoHostnameFenceTests.cs:25; `.xaml` is now swept with `xmlns` attribute values stripped and a vacuity floor (8177b8d) — T6 review Minor 12
- AC-6.13 **FIXED** — a stale inventory row said `icon-cache\` holds recipe icons, when it now holds your own accounts' avatars too — docs/plans/2026-09-15-backlog-remediation.md:4151 (8177b8d) — T6 review Minor 13

## Whole-branch review

Its Minor 5 is AC-2.13 and its Minor 11 is AC-3.6; both are listed under the task that raised them first.

- AC-B.1 **FIXED (0.3.2 release run)** — docs: three rows in this file that the branch invalidated. V3-S.10's count and S1-12.5's citation were corrected in the release commit; S1-12.11 stays OPEN on its own row because its other gaps remain. Original finding: V3-S.10 still lists `AlertsPage.xaml.cs:74` and `:90` among "nine stock `MessageBox.Show` calls" (both are gone, so the count is seven); S1-12.5 cites `AlertsPage.xaml.cs:62` where the read is now at `:51`; S1-12.11 cites `tests/AlertsModelTests.cs:18` for tests that moved out of that file — docs/backlog.md:155, :161, :348 — branch review Minor 1 (Task 5's release step already instructs the edits)
- AC-B.2 **OPEN** — docs: three earlier design docs still describe the Alerts page this branch replaced, with no banner — docs/2026-09-14-score-book-design.md:381-384; docs/2026-09-13-recipes-design.md:348; docs/2026-09-12-ur-score-design.md:229-237 — branch review Minor 2
- AC-B.3 **OPEN** — docs: the approved design is now wrong in two places — it says the "Next, in RoRoRo" line is always shown while any alert is on (the code needs a *sent* stat's alert), and that the file is backed up *before* each write (the backup is now made as part of a replace that succeeds) — docs/2026-09-15-alerts-card-design.md:28, :33 — branch review Minor 3
- AC-B.4 **OPEN** — code tidiness: `MinuteChoices(string? current)` ignores its parameter, kept so the contract's signature does, and `Row()` still computes a value to feed it; a reader who misses the comment will think A12's extra choice is live — src/UI/Setup/AlertCards.cs:293, 416 — branch review Minor 4
- AC-B.6 **FIXED** — five untracked build transcripts sat in the repo root uncovered by `.gitignore`, where one stray `git add` could commit build logs to a public repo — `*-output.txt` now ignored (8e1bdd9) — branch review Minor 6
- AC-B.7 **FIXED** — process: this cycle's plan was modified and uncommitted in the working tree at review time — committed at 0666172; the tree is clean at ddfafc8 — branch review Minor 7
- AC-B.8 **OPEN** — test gap: `HeadshotsAsync`'s 100-id batching loop has no test, so the `asked` set — the middle privacy gate — is only ever exercised in its single-batch form — src/Source/IconClient.cs:129-154 — branch review Minor 8
- AC-B.9 **OPEN** — performance: `AvatarFileFor` rebuilds the id set on every call, one HashSet allocation per row per redraw, where the board's equivalent reuses a snapshot — src/Composition/AppServices.cs:216-217 — branch review Minor 9
- AC-B.10 **OPEN** — `AvatarFill._brushes` is an unsynchronized `Dictionary` on a converter shared by every window through `App.xaml`; correct today, pinned by nothing — src/UI/Controls/AvatarFill.cs — branch review Minor 10
- AC-B.12 **OPEN** — `Placeless` drops `Index` but keeps the rule's own `Label`, so a hand edit to a label alone still fails `Same`, redraws and drops keyboard focus, though no card text changes — src/UI/Setup/AlertCards.cs:149 — branch review Minor 12 (the residue of AC-2.5)

## Branch fix re-review

Its second Minor is a restatement of AC-6.4 (no icon-cache pruning), not a new item.

- AC-BR.1 **FIXED** — `TheSentDotIsLaidOutWhetherOrNotAnAccountIsSent` asserted the trigger's shape and the old converter's absence but never `Value="Hidden"`, so a regression to `Collapsed` inside that very setter — the one defect the test exists to catch — would still have passed — tests/AvatarFenceTests.cs:97-103; both A26 fences now read the setter through a shared check (ddfafc8) — branch re-review Minor 1
- AC-BR.2 **OPEN** — `walk-starter-board.ps1` check 3 ("My accounts groups your accounts by clan") has no needs-RoRoRo skip, so with RoRoRo closed it fails on an empty panel and reads as a defect in Ur Score. `walk-alts.ps1` handles the same condition by reporting "needs RoRoRo" and skipping — give check 3 the same treatment, and sweep the other walks for checks that silently depend on accounts existing — tools/smoke/walk-starter-board.ps1:92-94 — found by the controller running the walks, 2026-09-15

## Task 7: start reading when Ur Score opens

- AC-7.1 **FIXED** — "Ur Score" landed at both ends of a sentence boundary in the start-on-open explainer ("...you open Ur Score. Ur Score reads only...") — src/UI/Setup/RecipesPage.xaml:51 — reworded, daa2f59 — T7 review Minor 1
- AC-7.2 **FIXED** — the rewritten "Run itself in the background" README bullet wrapped wider (~110-113 cols) than its neighbours (~80-97) — README.md:159-161 — rewrapped, daa2f59 — T7 review Minor 2

## Task 8: the window opens on the last numbers it saw

Task 8's Critical (a remembered snapshot fabricated a clan rank) and Important 1-2 (the board-wide "remembered" sentence dropping on the fault branch; the first draw filtering against the disk cache, not RoRoRo's current list) were fixed in their own round at `f922ab3` and are in the execution record, not repeated here.

- AC-8.1 **OPEN** — `MyAccounts` sets `remembered |=` before its `if (snapshot?.Rows is not { } rows) continue;`, so a source that contributes no rows still marks the panel remembered (over-marks; safe direction) — src/Board/PanelModels.cs:309 — T8 review Minor 1
- AC-8.2 **FIXED** — `BoardText.StateLine`'s `enabled.Count == 0` return also dropped the board-wide "remembered" sentence — src/UI/BoardText.cs:37 — closed as a side effect of the Important-1 fix (the sentence now rides every branch), f922ab3 — T8 review Minor 2
- AC-8.3 **OPEN** — `StatHistory.Span` rounds to the nearest unit, so 2d11h reads "from 2d ago" and 3h20m reads "3h" — the age can sound fresher than it is by up to half a unit — src/Core/StatHistory.cs:79-81 — T8 review Minor 3
- AC-8.4 **OPEN** — test gap: no test for a mixed board (one source read, one remembered) — that only the remembered panel is marked and the sentence stays, dated from the remembered source — tests/PanelModelsTests.cs; tests/BoardTextTests.cs — T8 review Minor 4
- AC-8.5 **OPEN** — test gap: the "stops marking once read" half only asserts Standing and AccountsTable; Race, MyAccounts, AccountCard and ProfileStat are never asserted to stop marking (same gap on the both-maps case) — tests/PanelModelsTests.cs:829-830, :836-840 — T8 review Minor 5
- AC-8.6 **OPEN** — test gap: `AStatTheRecipeNoLongerOffersIsLeftOutAndAnEmptyRowWithIt` only pins the whole-snapshot refusal; a row keeping its other stats while the gone one drops is untested — tests/RememberedTests.cs:65-71 — T8 review Minor 6
- AC-8.7 **FIXED** — the kept `BookAccount.Rank` and row count were discarded by `Remembered.From` with no comment saying why — src/Book/Remembered.cs:66 — closed as a side effect of the Critical's fix (`RememberedRanks` now carries them), f922ab3 — T8 review Minor 7
- AC-8.8 **OPEN** — a remembered Accounts table still copies to the clipboard (`ClipboardCopyMode="IncludeHeader"`) with no sign the numbers were remembered — src/UI/Panels/AccountsTablePanel.xaml:23 — T8 review Minor 8
- AC-8.9 **OPEN** — code tidiness: `Gap` can pair a live gap from the group-list source with a remembered place and total in the same Standing panel; correct in both halves but worth a comment — src/Board/PanelModels.cs:950-955 — T8 review Minor 9
- AC-8.10 **OPEN** — `BoardWindow.ResolveNamesAsync` still reads `SnapshotOf` while the panel it feeds (`LiveLeaderboard`) moved to `LiveOf`; inert only because every remembered row id is already filtered into `MyUserIds` — src/UI/BoardWindow.xaml.cs:457 — T8 review Minor 10
- AC-8.11 **OPEN** — performance: `ScoreBookReader.LastReading` sorts every slug's readings to take one, and `RememberLastNumbers` calls it once per enabled source on every account listing, for a result unused once the source has been read this session — src/Book/ScoreBookReader.cs:121-131 — T8 review Minor 11
- AC-8.12 **OPEN** — the fabricated `HeadlineValue.Text` is an unformatted invariant number ("14020550") where a live one carries the source's own formatting — src/Book/Remembered.cs:71 — T8 review Minor 12
- AC-8.13 **FIXED** — `Record` put every snapshot, failures included, into `_latest`, so the first failed read of a source cleared its remembered numbers and mark, undoing the feature where it was worth most — src/Composition/AppServices.cs:653 — fixed (a failed read now replaces nothing), 7ee4afd — T8 review Minor 13

## Task 9: the reason an account's numbers are empty, beside the empty numbers

- AC-9.1 **OPEN** (parked for 0.3.3) — `PanelText.CannotRead` has no id-ownership guard of its own; it trusts its two callers to only ever pass the owner's own ids. No path reaches it with a foreign id today, but nothing in the type system or a fence would catch a future caller that does — the same hardening avatars got at three layers — src/Board/PanelText.cs:141-158 — T9 review Minor 1
- AC-9.2 **OPEN** — `CannotRead`'s source filter is `.Where(s => s.Enabled)` only; `AccountsModel.FoundIn`, three lines away, also excludes `SourceRole.Watch`. Harmless today (a Watch source's account map is always empty) but the two read as though they should agree — src/Board/PanelText.cs:148 vs src/UI/Setup/AccountsModel.cs:83 — T9 review Minor 2
- AC-9.3 **OPEN** — test gap: the duplicate-line guard (`lines.Contains(line, StringComparer.Ordinal)`) has no test that ever produces two identical lines to dedupe — src/Board/PanelText.cs:154 — T9 review Minor 3
- AC-9.4 **GONE** — the report's own count of new tests ("the brief specifies six, I added a seventh") is a documentation-only miscount; the brief lists five and the diff/suite delta both show six added, not seven. No code affected — task-9-report.md, "Deviation from the brief" — T9 review Minor 4

## Whole-branch review 2 (`8177b8d..7ee4afd`, Tasks 7-9 and their fix rounds)

- AC-B2.1 **FIXED** — `Race` plotted a remembered total at the current instant after any read that brought nothing back, drawing a flat line to "now" for a reading that never happened; fired on every unreachable/rate-limited read and routinely on `SourceIdle`, the between-battles state the owner's main clan sits in — src/Board/PanelModels.cs:285-289, src/Composition/AppServices.cs:691-694 — fixed (the synthetic chart point now reads `live.LiveOf` rather than `SnapshotOf`, so a remembered snapshot can no longer supply it; `AccountCard`'s "Last read" fact given the same fix), 4ce08bb — branch-review-2 Critical 1
- AC-B2.2 **FIXED (fa3897a, the icon.png contradiction is gone)** — README.md:205-209 and :220-221 contradict each other about whether `icon.png` exists, in the section this branch rewrote — README.md:205-209, :220-221 — branch-review-2 Important 1
- AC-B2.3 **FIXED (fa3897a, "What it does" is recipe-first and the three-minute claim is gone)** — README.md's "What it does" (17-34) still describes the pre-recipes build and contradicts the recipe-terms paragraph six lines above it; the three-minute poll interval it cites was replaced by the recipe's own `EffectiveEverySeconds`, and `pollSeconds` no longer exists in Settings — README.md:17-34 — branch-review-2 Important 2
- AC-B2.4 **FIXED (fa3897a, the Troubleshooting table now lists only strings the app produces)** — the README Troubleshooting table (169-178) lists state-line strings the app doesn't produce, including one this branch wrote ("Idle — nothing to watch"); four of the listed rows are states `BoardText.Healthy` treats as fine and `StateLine` never prints — README.md:169-178 — branch-review-2 Important 3
- AC-B2.5 **FIXED** — this backlog's 0.3.2 section was scoped to `cec5987..ddfafc8`, predating the Task 7/8/9 reviews and this review, so their findings had no durable home outside the gitignored SDD workspace Task 5 deletes — docs/backlog.md — fixed by this update (AC-7.x/AC-8.x/AC-9.x/AC-B2.x rows added, range corrected to `cec5987..4ce08bb`) — branch-review-2 Important 4
- AC-B2.6 **OPEN** — two paths now answer "why can't this account be read": `AccountCard`, `ProfileStat` and `AccountsTable` read `Unavailable` off `SnapshotOf` (which can be the remembered snapshot, whose `Unavailable` is always empty); Task 9's two new surfaces read it off `live.Snapshots`. Harmless today, unstated anywhere — src/Board/PanelModels.cs:483, 732, 816 vs :381; src/UI/Setup/AccountsModel.cs:70 — branch-review-2 Minor 1
- AC-B2.7 **OPEN** (a ruling to revisit in 0.3.3, plan-mandated by A39 today) — `BookLine.Unavail` is kept by `LineBuilder` and dropped by `Remembered.From`, so on window open an account whose profile is private shows a dash with no reason beside rows that do have remembered numbers, until the first read lands — src/Book/Remembered.cs:55-65 — branch-review-2 Minor 2
- AC-B2.8 **FIXED** — this section's own stated counts ("53 distinct items ... 32 OPEN, 20 FIXED, 1 GONE") were off by one row even before this update (`AC-BR.2` was added after the counts were written) — docs/backlog.md:342, :371 — recount folded into this update's totals — branch-review-2 Minor 3
- AC-B2.9 **OPEN** — `MyAccountsPanel.xaml`'s name now sits in a StackPanel above the note while `Value`/`InGroup`/`Change` stay vertically centred, so a row with a note loses its shared baseline with its own numbers — src/UI/Panels/MyAccountsPanel.xaml:41-59 — branch-review-2 Minor 4
- AC-B2.10 **OPEN** — `Assert-OneLine` in the Alerts walk runs immediately after the click and before `Wait-Line`, so it can only catch a duplicate already on screen — tools/smoke/walk-alerts.ps1:93, 115, 130, 141, 148 — branch-review-2 Minor 5
- AC-B2.11 **OPEN** — `BoardText.Sentences` appends "." to any credit fragment not already ending in one, so a third-party recipe credit ending "Thanks!" would render as "Thanks!." on the board footer — src/UI/BoardText.cs:107-109 — branch-review-2 Minor 6
- AC-B2.12 **OPEN** — three README wording inaccuracies: "exactly two outbound calls per poll" is a clan-recipe-only fact; `clan.battle.points` is called "Ur Score's metric id ... by default" when it's the recipe's; the rules file is said to be backed up "before every write" though the backup is now made only as part of a successful replace — README.md:47-51, :109, :119 — branch-review-2 Minor 7
- AC-B2.13 **OPEN** — the trail line at BoardWindow.xaml.cs:133 writes "Setup > Recipes" with an ASCII `>` where every other surface uses "Setup › Recipes" — src/UI/BoardWindow.xaml.cs:133 — branch-review-2 Minor 8
- AC-B2.14 **OPEN** — the `catch (Exception ex)` in `OpenOnLastNumbersAsync` is unreachable (`RefreshAccountsAsync` swallows everything except the caller's own cancellation, already caught above it); harmless, worth a word saying so — src/Composition/AppServices.cs:317-321 — branch-review-2 Minor 9
- AC-B2.15 **OPEN** — test gap: Task 7's wiring (`BoardWindow.OnLoaded` calling `StartsOnOpen`; the checkbox writing the settings file) has no automated cover — nothing but the plan's Step 9 live look proves it — src/UI/BoardWindow.xaml.cs; src/UI/Setup/RecipesPage.xaml.cs — branch-review-2 Minor 10
- AC-B2.16 **OPEN** — `activeRecipe` is declared on the `Settings` record and read or written by nothing in `src/`: it is serialised into `settings.json` and never used. Either wire it or drop it; a setting a user can see and edit that does nothing is worse than no setting — src/Core/Settings.cs:16 — found by the README audit, 2026-09-15 (the controller had documented it as "set by Setup › Recipes", which was never true)
- AC-B2.17 **OPEN** — `IsOverdue` measures the last read ATTEMPT, not the last successful one, so a source failing every cycle never shows as overdue — and since 4ce08bb that can pair with "remembered, from 3h ago" and no mark. Pre-existing and defensible (the state line names the failure on the same screen); redefining overdue reaches past this release — src/Board/PanelModels.cs — raised by Task 8's implementer, ruled to 0.3.3 by the controller
- AC-B2.18 **FIXED** — the account-name TextBlock's automation name includes "sent to RoRoRo in the last read" only when its row is Sent, making the dot's meaning accessible with the account rather than naming a peerless Ellipse. MyAccountsAccessibilityTests exercises sent/unsent and reused-row transitions; existing last-read and dot-spacing tests still pass. Live Narrator verification remains pending. Original finding: true-numbers wave, 2026-09-16.
