<Application x:Class="Labs626.UrScore.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="clr-namespace:Labs626.UrScore.UI"
             xmlns:sys="clr-namespace:System;assembly=System.Runtime">
    <Application.Resources>
        <ResourceDictionary>
            <!-- RoRoRo's Brand palette, painted before any connection exists. ThemeService replaces
                 every one of these with RoRoRo's live palette once it answers, so these are the only
                 literal colours in the app (ThemeFenceTests). Reference them with DynamicResource. -->
            <SolidColorBrush x:Key="BgBrush" Color="#0F1F31" />
            <SolidColorBrush x:Key="CyanBrush" Color="#17D4FA" />
            <SolidColorBrush x:Key="MagentaBrush" Color="#F22F89" />
            <SolidColorBrush x:Key="WhiteBrush" Color="#FFFFFF" />
            <SolidColorBrush x:Key="MutedTextBrush" Color="#9AA8B8" />
            <SolidColorBrush x:Key="DividerBrush" Color="#1F3149" />
            <SolidColorBrush x:Key="RowBgBrush" Color="#15263A" />
            <SolidColorBrush x:Key="RowHoverBrush" Color="#1D2E41" />
            <SolidColorBrush x:Key="EdgeBrush" Color="#6B7F92" />

            <!-- Chart series colours (ChartPalette). Deliberately NOT repainted by ThemeService: a line's
                 colour is what says which clan it is, and a theme change must not move a clan from green to
                 grey while its legend still reads green. The first two are the brand's own. -->
            <SolidColorBrush x:Key="Series1Brush" Color="#17D4FA" />
            <SolidColorBrush x:Key="Series2Brush" Color="#F22F89" />
            <SolidColorBrush x:Key="Series3Brush" Color="#FFD23F" />
            <SolidColorBrush x:Key="Series4Brush" Color="#5BE37D" />
            <SolidColorBrush x:Key="Series5Brush" Color="#C77DFF" />
            <SolidColorBrush x:Key="Series6Brush" Color="#FF7A45" />
            <SolidColorBrush x:Key="Series7Brush" Color="#6C8BFF" />
            <SolidColorBrush x:Key="Series8Brush" Color="#E8E8F0" />

            <FontFamily x:Key="DisplayFont">Segoe UI Variable Display, Segoe UI</FontFamily>
            <FontFamily x:Key="BodyFont">Segoe UI Variable Text, Segoe UI</FontFamily>
            <FontFamily x:Key="MonoFont">Cascadia Code, Consolas</FontFamily>

            <!-- Keyboard focus is always visible: a cyan ring outside the control. -->
            <Style x:Key="FocusRing">
                <Setter Property="Control.Template">
                    <Setter.Value>
                        <ControlTemplate>
                            <Rectangle Margin="-3" RadiusX="5" RadiusY="5" StrokeThickness="1.5"
                                       Stroke="{DynamicResource CyanBrush}" SnapsToDevicePixels="True" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Text roles. Windows set Foreground to WhiteBrush; these cover the rest. -->
            <Style x:Key="Heading" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource DisplayFont}" />
                <Setter Property="FontSize" Value="20" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>
            <Style x:Key="SectionLabel" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource MonoFont}" />
                <Setter Property="FontSize" Value="11" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="{DynamicResource CyanBrush}" />
                <Setter Property="Margin" Value="0,0,0,6" />
            </Style>
            <!-- A panel's title (the mock's .p-title): mono, small, cyan, drawn upper case; its accessible name stays the title. -->
            <Style x:Key="PanelTitleText" TargetType="TextBlock" BasedOn="{StaticResource SectionLabel}">
                <Setter Property="Margin" Value="0" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
            <!-- A value on a key-value line (the mock's .kv b): white, medium, tabular figures. -->
            <Style x:Key="KeyValue" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12" />
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="Typography.NumeralAlignment" Value="Tabular" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
            <Style x:Key="Muted" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>
            <Style x:Key="Refusal" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource MagentaBrush}" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>
            <Style x:Key="Card" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource RowBgBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="6" />
                <Setter Property="Padding" Value="12" />
            </Style>

            <Style TargetType="ToolTip">
                <Setter Property="Background" Value="{DynamicResource RowBgBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource EdgeBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="8,5" />
                <Setter Property="FontFamily" Value="{StaticResource BodyFont}" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="MaxWidth" Value="320" />
                <Setter Property="HasDropShadow" Value="False" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToolTip">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}"
                                    CornerRadius="4" SnapsToDevicePixels="True">
                                <ContentPresenter ContentSource="Content">
                                    <ContentPresenter.ContentTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" TextWrapping="Wrap" />
                                        </DataTemplate>
                                    </ContentPresenter.ContentTemplate>
                                </ContentPresenter>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Buttons. The fill and text never change on hover or press; a sheen in the text colour
                 lies over them instead, so a button reads the same in every theme. -->
            <Style TargetType="Button">
                <Setter Property="FocusVisualStyle" Value="{StaticResource FocusRing}" />
                <Setter Property="Background" Value="{DynamicResource RowBgBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource EdgeBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="14,7" />
                <Setter Property="FontFamily" Value="{StaticResource BodyFont}" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <Border x:Name="Fill" Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4" />
                                <Border x:Name="Sheen" Background="{DynamicResource WhiteBrush}" CornerRadius="4"
                                        Opacity="0" IsHitTestVisible="False" />
                                <ContentPresenter Margin="{TemplateBinding Padding}" RecognizesAccessKey="True"
                                                  HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Sheen" Property="Opacity" Value="0.08" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Sheen" Property="Opacity" Value="0.16" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Fill" Property="Background" Value="{DynamicResource BgBrush}" />
                                    <Setter TargetName="Fill" Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
                                    <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
                                    <Setter Property="Cursor" Value="Arrow" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Background" Value="{DynamicResource CyanBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource CyanBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource BgBrush}" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </Style>

            <!-- Bool to Visibility for bindings. Panels and Setup rows use it. -->
            <BooleanToVisibilityConverter x:Key="BoolToVisible" />
            <ui:UpperCase x:Key="Upper" />
            <ui:AvatarFill x:Key="Avatar" />
            <!-- A clan's icon as an image to draw: the Standing panel's own clan's (backlog V3-S.7). -->
            <ui:PictureFile x:Key="Picture" />

            <!-- One of your own accounts' pictures: round, sized to its row, the same on every surface. The slot is laid
                 out whether or not a picture is there (Hidden, never Collapsed), so a picture that arrives late, fails or
                 never comes never moves a row. Only your own accounts ever have one. -->
            <Style x:Key="AccountAvatar" TargetType="Ellipse">
                <Setter Property="Width" Value="20" />
                <Setter Property="Height" Value="20" />
                <Setter Property="Margin" Value="0,0,8,0" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Stroke" Value="{DynamicResource DividerBrush}" />
                <Setter Property="StrokeThickness" Value="1" />
                <Setter Property="Fill" Value="{Binding Avatar, Converter={StaticResource Avatar}}" />
                <Style.Triggers>
                    <!-- On the converted brush, not the path: a file that is gone by the time it is drawn, or that doesn't
                         decode, hides the slot too instead of leaving an empty ring for the session (review Minor 3). -->
                    <DataTrigger Binding="{Binding Avatar, Converter={StaticResource Avatar}}" Value="{x:Null}">
                        <Setter Property="Visibility" Value="Hidden" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <!-- A button that reads as a row: no fill or edge until hovered, content on the left. Search
                 matches and panel tools use it. -->
            <Style x:Key="FlatButton" TargetType="Button">
                <Setter Property="FocusVisualStyle" Value="{StaticResource FocusRing}" />
                <Setter Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Padding" Value="8,5" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="FontFamily" Value="{StaticResource BodyFont}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <Border x:Name="Fill" Background="{TemplateBinding Background}" CornerRadius="3" />
                                <Border x:Name="Sheen" Background="{DynamicResource WhiteBrush}" CornerRadius="3"
                                        Opacity="0" IsHitTestVisible="False" />
                                <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Left" VerticalAlignment="Center" />
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Sheen" Property="Opacity" Value="0.08" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Sheen" Property="Opacity" Value="0.16" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Board panels (spec §9.4 and the mock): a card, big numbers, key-value lines, column headers, notes. -->
            <Style x:Key="PanelCard" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource RowBgBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="Padding" Value="14,12,14,14" />
            </Style>
            <!-- The mock's .big (30 px, line height 1). WPF gives the line a third more height than CSS does, split evenly
                 here by the negative margins, so the figures sit in a 30 px box as the mock's do and a 12 px word beside
                 them, bottom-aligned, shares their baseline. -->
            <Style x:Key="BigNumber" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource DisplayFont}" />
                <Setter Property="FontSize" Value="30" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Typography.NumeralAlignment" Value="Tabular" />
                <Setter Property="Margin" Value="0,-5,0,-5" />
            </Style>
            <Style x:Key="KeyLabel" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
            <!-- A table's heading text (the mock's th): its row, PanelTableHeadingRow, gives the padding and the line under it. -->
            <Style x:Key="ColumnHeader" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
                <Setter Property="FontFamily" Value="{StaticResource MonoFont}" />
                <Setter Property="FontSize" Value="10.5" />
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
            </Style>
            <!-- A panel table's heading row and body rows, for tables drawn as RowLists (the mock's th and td): padding and a
                 divider line under each. The mock's cells are 6 px padded at CSS line heights (1 for headings, 1.55 for rows);
                 WPF's lines are 1.16 and 1.33, so the vertical padding here is what gives the mock's 23 px and 33 px rows. -->
            <Style x:Key="PanelTableHeadingRow" TargetType="Border">
                <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
                <Setter Property="BorderThickness" Value="0,0,0,1" />
                <Setter Property="Padding" Value="6,5" />
            </Style>
            <Style x:Key="PanelTableRowLine" TargetType="Border">
                <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
                <Setter Property="BorderThickness" Value="0,0,0,1" />
                <Setter Property="Padding" Value="6,7" />
                <Setter Property="Typography.NumeralAlignment" Value="Tabular" />
            </Style>
            <!-- A group's heading inside a table (My accounts' clans): semibold, 10 px above, no line under. -->
            <Style x:Key="PanelTableGroupHeading" TargetType="TextBlock" BasedOn="{StaticResource ColumnHeader}">
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Margin" Value="6,10,6,6" />
            </Style>
            <Style x:Key="PanelNoteText" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource EdgeBrush}" />
                <Setter Property="FontFamily" Value="{StaticResource MonoFont}" />
                <Setter Property="FontSize" Value="11" />
                <Setter Property="TextWrapping" Value="Wrap" />
                <Setter Property="Margin" Value="0,10,0,0" />
            </Style>
            <!-- A cell that greys out when its row has no value. -->
            <Style x:Key="RowCell" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Missing}" Value="True">
                        <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="NumberCell" TargetType="TextBlock" BasedOn="{StaticResource RowCell}">
                <Setter Property="TextAlignment" Value="Right" />
                <Setter Property="Typography.NumeralAlignment" Value="Tabular" />
            </Style>

            <Style TargetType="TextBox">
                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                <Setter Property="Background" Value="{DynamicResource RowBgBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource EdgeBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CaretBrush" Value="{DynamicResource CyanBrush}" />
                <Setter Property="SelectionBrush" Value="{DynamicResource CyanBrush}" />
                <Setter Property="Padding" Value="6,5" />
                <Setter Property="FontFamily" Value="{StaticResource BodyFont}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="Frame" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"
                                              Focusable="False" HorizontalScrollBarVisibility="Hidden"
                                              VerticalScrollBarVisibility="Hidden" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="Frame" Property="BorderBrush" Value="{DynamicResource CyanBrush}" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="CheckBox">
                <Setter Property="FocusVisualStyle" Value="{StaticResource FocusRing}" />
                <Setter Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CheckBox">
                            <StackPanel Orientation="Horizontal" Background="Transparent">
                                <Border x:Name="Box" Width="16" Height="16" CornerRadius="3" BorderThickness="1"
                                        Background="{DynamicResource RowBgBrush}" BorderBrush="{DynamicResource EdgeBrush}"
                                        VerticalAlignment="Center">
                                    <Path x:Name="Tick" Data="M 3,8 L 6.5,11.5 L 13,4.5" StrokeThickness="2"
                                          Stroke="{DynamicResource BgBrush}" Visibility="Collapsed" />
                                </Border>
                                <ContentPresenter Margin="6,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True" />
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Box" Property="Background" Value="{DynamicResource CyanBrush}" />
                                    <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource CyanBrush}" />
                                    <Setter TargetName="Tick" Property="Visibility" Value="Visible" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource CyanBrush}" />
                                </Trigger>
                                <!-- Last, so it wins over IsChecked for a box that is both: a checked-but-disabled
                                     box (a greyed setup-import row, spec §3) must read as inert, not as an ordinary
                                     tick. Brushes already named above and in the Button style's own disabled trigger. -->
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Box" Property="Background" Value="{DynamicResource BgBrush}" />
                                    <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
                                    <Setter TargetName="Tick" Property="Stroke" Value="{DynamicResource MutedTextBrush}" />
                                    <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
                                    <Setter Property="Cursor" Value="Arrow" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ComboBox">
                <Setter Property="FocusVisualStyle" Value="{StaticResource FocusRing}" />
                <Setter Property="Background" Value="{DynamicResource RowBgBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource EdgeBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="8,5" />
                <Setter Property="MinHeight" Value="30" />
                <Setter Property="FontFamily" Value="{StaticResource BodyFont}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <ToggleButton x:Name="Toggle" Focusable="False" ClickMode="Press"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border x:Name="Frame" CornerRadius="4" BorderThickness="1"
                                                    Background="{DynamicResource RowBgBrush}" BorderBrush="{DynamicResource EdgeBrush}">
                                                <Path x:Name="Arrow" HorizontalAlignment="Right" VerticalAlignment="Center"
                                                      Margin="0,0,11,0" Data="M0,0 L4,4 L8,0 Z" Fill="{DynamicResource MutedTextBrush}" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Frame" Property="BorderBrush" Value="{DynamicResource CyanBrush}" />
                                                    <Setter TargetName="Arrow" Property="Fill" Value="{DynamicResource CyanBrush}" />
                                                </Trigger>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="Frame" Property="BorderBrush" Value="{DynamicResource CyanBrush}" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter IsHitTestVisible="False" Margin="10,0,28,0"
                                                  HorizontalAlignment="Left" VerticalAlignment="Center"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  TextElement.Foreground="{TemplateBinding Foreground}" />
                                <Popup x:Name="PART_Popup" Placement="Bottom" Focusable="False" AllowsTransparency="True"
                                       IsOpen="{TemplateBinding IsDropDownOpen}" PopupAnimation="Fade">
                                    <Border Background="{DynamicResource BgBrush}" BorderBrush="{DynamicResource EdgeBrush}"
                                            BorderThickness="1" CornerRadius="4" Margin="0,3,0,0"
                                            MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                            MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <ScrollViewer>
                                            <ItemsPresenter />
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="ComboBoxItem">
                <Setter Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Padding" Value="8,5" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="Item" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}"
                                    CornerRadius="3" Margin="2,1">
                                <ContentPresenter />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="Item" Property="Background" Value="{DynamicResource RowHoverBrush}" />
                                    <Setter Property="Foreground" Value="{DynamicResource CyanBrush}" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Item" Property="Background" Value="{DynamicResource RowBgBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Menus (the board's tab menu). The stock templates paint a system-coloured icon gutter down the left
                 edge and a system highlight; both are replaced, so the menu reads like a combo box list in any theme. -->
            <Style TargetType="ContextMenu">
                <Setter Property="Background" Value="{DynamicResource RowBgBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource EdgeBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="3" />
                <Setter Property="FontFamily" Value="{StaticResource BodyFont}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ContextMenu">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="MenuItem">
                <Setter Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Padding" Value="12,6" />
                <Setter Property="MinWidth" Value="140" />
                <Setter Property="Cursor" Value="Hand" />
                <!-- The highlight is the focus mark, as in a combo box list. -->
                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="MenuItem">
                            <Border x:Name="Item" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="3">
                                <ContentPresenter ContentSource="Header" RecognizesAccessKey="True" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="Item" Property="Background" Value="{DynamicResource RowHoverBrush}" />
                                    <Setter Property="Foreground" Value="{DynamicResource CyanBrush}" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Item" Property="Background" Value="Transparent" />
                                    <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
                                    <Setter Property="Cursor" Value="Arrow" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Tables. The stock DataGrid paints black cell text, white headers and a system-blue
                 selection; every one of those is replaced so rows read in any theme. -->
            <Style TargetType="DataGrid">
                <Setter Property="Background" Value="{DynamicResource BgBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                <Setter Property="RowBackground" Value="{DynamicResource RowBgBrush}" />
                <Setter Property="AlternatingRowBackground" Value="{DynamicResource BgBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="HeadersVisibility" Value="Column" />
                <Setter Property="GridLinesVisibility" Value="None" />
                <Setter Property="RowHeaderWidth" Value="0" />
                <Setter Property="SelectionMode" Value="Single" />
                <Setter Property="CanUserAddRows" Value="False" />
                <Setter Property="CanUserDeleteRows" Value="False" />
                <Setter Property="FontFamily" Value="{StaticResource BodyFont}" />
                <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource DividerBrush}" />
                <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource DividerBrush}" />
            </Style>
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="{DynamicResource BgBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
                <Setter Property="BorderThickness" Value="0,0,0,1" />
                <Setter Property="FontFamily" Value="{StaticResource MonoFont}" />
                <Setter Property="FontSize" Value="11" />
                <Setter Property="Padding" Value="8,6" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DataGridColumnHeader">
                            <Grid>
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="Center" />
                                </Border>
                                <Thumb x:Name="PART_LeftHeaderGripper" HorizontalAlignment="Left" Width="6"
                                       Cursor="SizeWE" Opacity="0" />
                                <Thumb x:Name="PART_RightHeaderGripper" HorizontalAlignment="Right" Width="6"
                                       Cursor="SizeWE" Opacity="0" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="DataGridRow">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource RowHoverBrush}" />
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{DynamicResource RowHoverBrush}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="DataGridCell">
                <Setter Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                <Setter Property="Padding" Value="8,4" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DataGridCell">
                            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                <ContentPresenter VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            <!-- The Send column's checkbox, shown and edited in the theme's style. -->
            <Style x:Key="GridCheckBox" TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
            <Style x:Key="GridCheckBoxDisplay" TargetType="CheckBox" BasedOn="{StaticResource GridCheckBox}">
                <Setter Property="IsHitTestVisible" Value="False" />
                <Setter Property="Focusable" Value="False" />
            </Style>

            <!-- A table that sits on a panel card (D14): no fill of its own, dividers between rows, headings like the panels' column headers. -->
            <Style x:Key="PanelTableHeader" TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="FontSize" Value="10.5" />
                <Setter Property="FontWeight" Value="Medium" />
                <!-- As PanelTableHeadingRow: the mock's 6 px heading padding at WPF's taller line. -->
                <Setter Property="Padding" Value="6,5" />
            </Style>
            <!-- A number column's heading lines up with its numbers. -->
            <Style x:Key="PanelTableNumberHeader" TargetType="DataGridColumnHeader" BasedOn="{StaticResource PanelTableHeader}">
                <Setter Property="HorizontalContentAlignment" Value="Right" />
            </Style>
            <!-- The account column's heading lines up with the names, which the avatar slot (AccountAvatar: 20 px wide with
                 8 px after it) indents by a constant 28 px whether or not a picture is there. -->
            <Style x:Key="PanelTableNameHeader" TargetType="DataGridColumnHeader" BasedOn="{StaticResource PanelTableHeader}">
                <Setter Property="Padding" Value="34,5" />
            </Style>
            <!-- The cell the arrow keys are on: a thin cyan ring inside it, shown for the keyboard only. -->
            <Style x:Key="PanelTableCellFocus">
                <Setter Property="Control.Template">
                    <Setter.Value>
                        <ControlTemplate>
                            <Rectangle Margin="1" StrokeThickness="1.5" Stroke="{DynamicResource CyanBrush}" SnapsToDevicePixels="True" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="PanelTableCell" TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
                <Setter Property="FocusVisualStyle" Value="{StaticResource PanelTableCellFocus}" />
                <!-- As PanelTableRowLine: the mock's 6 px cell padding at WPF's shorter line. -->
                <Setter Property="Padding" Value="6,7" />
            </Style>
            <!-- The cyan tint across a marked row: the Accounts table's picked row, Top of the battle's own clan. Lay it under
                 the row's content and set its Opacity to PanelRowTintOpacity when the row is marked. -->
            <sys:Double x:Key="PanelRowTintOpacity">0.14</sys:Double>
            <Style x:Key="PanelRowTint" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource CyanBrush}" />
                <Setter Property="Opacity" Value="0" />
                <Setter Property="IsHitTestVisible" Value="False" />
            </Style>
            <!-- A row: the hover fill, and the picked row tinted cyan across the whole width. -->
            <Style x:Key="PanelTableRow" TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DataGridRow">
                            <Grid>
                                <Border Background="{TemplateBinding Background}" SnapsToDevicePixels="True" />
                                <Border x:Name="Tint" Style="{StaticResource PanelRowTint}" />
                                <DataGridCellsPresenter ItemsPanel="{TemplateBinding ItemsPanel}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Tint" Property="Opacity" Value="{StaticResource PanelRowTintOpacity}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="Transparent" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="PanelTable" TargetType="DataGrid" BasedOn="{StaticResource {x:Type DataGrid}}">
                <Style.Resources>
                    <!-- Every heading in the table, the grid's filler heading too, which lies under the real ones across the
                         whole width; so there is no ColumnHeaderStyle setter, which would reach the real headings only. -->
                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource PanelTableHeader}" />
                    <!-- Every cell too: the application's implicit DataGridCell style outranks the grid's CellStyle, so without
                         this nearer implicit style the cells kept the stock 8 × 4 padding and no focus ring. -->
                    <Style TargetType="DataGridCell" BasedOn="{StaticResource PanelTableCell}" />
                </Style.Resources>
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="RowBackground" Value="Transparent" />
                <Setter Property="AlternatingRowBackground" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="GridLinesVisibility" Value="Horizontal" />
                <Setter Property="IsReadOnly" Value="True" />
                <Setter Property="AutoGenerateColumns" Value="False" />
                <Setter Property="CanUserReorderColumns" Value="False" />
                <Setter Property="CanUserResizeRows" Value="False" />
                <Setter Property="SelectionUnit" Value="FullRow" />
                <!-- Copies nothing: a panel table may hold other players, who never reach the clipboard. The Accounts table,
                     your own accounts only, turns copying on itself (ClipboardFenceTests). -->
                <Setter Property="ClipboardCopyMode" Value="None" />
                <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
                <Setter Property="VerticalScrollBarVisibility" Value="Disabled" />
                <Setter Property="Typography.NumeralAlignment" Value="Tabular" />
                <Setter Property="RowStyle" Value="{StaticResource PanelTableRow}" />
            </Style>

            <!-- Slim scrollbars: a thin thumb in the divider colour, no arrows, no track. -->
            <Style x:Key="SlimThumb" TargetType="Thumb">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Thumb">
                            <Border x:Name="Bar" Background="{DynamicResource EdgeBrush}" CornerRadius="3" />
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bar" Property="Background" Value="{DynamicResource MutedTextBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="ScrollBar">
                <Setter Property="Background" Value="Transparent" />
                <Style.Triggers>
                    <Trigger Property="Orientation" Value="Vertical">
                        <Setter Property="Width" Value="8" />
                        <Setter Property="MinWidth" Value="8" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ScrollBar">
                                    <Track x:Name="PART_Track" IsDirectionReversed="True" Margin="1">
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource SlimThumb}" />
                                        </Track.Thumb>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False" />
                                        </Track.IncreaseRepeatButton>
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False" />
                                        </Track.DecreaseRepeatButton>
                                    </Track>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                    <Trigger Property="Orientation" Value="Horizontal">
                        <Setter Property="Height" Value="8" />
                        <Setter Property="MinHeight" Value="8" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ScrollBar">
                                    <Track x:Name="PART_Track" Margin="1">
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource SlimThumb}" />
                                        </Track.Thumb>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Command="ScrollBar.PageRightCommand" Opacity="0" Focusable="False" />
                                        </Track.IncreaseRepeatButton>
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Command="ScrollBar.PageLeftCommand" Opacity="0" Focusable="False" />
                                        </Track.DecreaseRepeatButton>
                                    </Track>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
