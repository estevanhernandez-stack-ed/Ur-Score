<Window x:Class="Labs626.UrScore.UI.BoardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:Labs626.UrScore.UI"
        Title="RoRoRo Ur Score" Height="900" Width="1280" MinHeight="560" MinWidth="720"
        Background="{DynamicResource BgBrush}" Foreground="{DynamicResource WhiteBrush}"
        FontFamily="{StaticResource BodyFont}" FontSize="13" PreviewKeyDown="OnWindowKeyDown">
    <Window.Resources>
        <!-- A tab (the mock's .tab): medium weight, no fill until hovered; the board on screen is a pill in the panel
             colour with a divider edge and a cyan bar inset along its bottom. -->
        <Style x:Key="BoardTab" TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusRing}" />
            <Setter Property="AutomationProperties.Name" Value="{Binding Name}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Item" Background="Transparent" BorderBrush="Transparent" BorderThickness="1" CornerRadius="6">
                            <Grid>
                                <ContentPresenter Margin="{TemplateBinding Padding}" />
                                <Border x:Name="Bar" Height="2" VerticalAlignment="Bottom" Margin="3,0" CornerRadius="1"
                                        Background="{DynamicResource CyanBrush}" Visibility="Collapsed" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Item" Property="Background" Value="{DynamicResource RowHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Item" Property="Background" Value="{DynamicResource RowBgBrush}" />
                                <Setter TargetName="Item" Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
                                <Setter TargetName="Bar" Property="Visibility" Value="Visible" />
                                <Setter Property="Foreground" Value="{DynamicResource WhiteBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- + Board (the mock's .tab.add): cyan, medium, lined up with the tabs' text; muted when BoardButtons turns it off. -->
        <Style x:Key="AddBoardButtonStyle" TargetType="Button" BasedOn="{StaticResource FlatButton}">
            <Setter Property="Foreground" Value="{DynamicResource CyanBrush}" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Padding" Value="12,7" />
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
                    <Setter Property="Cursor" Value="Arrow" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <DockPanel>
        <Border DockPanel.Dock="Top" BorderBrush="{DynamicResource DividerBrush}" BorderThickness="0,0,0,1" Padding="16,8">
            <!-- The top bar (spec §3.1): icon, tabs, ⋯, + Board, the status chip, ⟳, the period line; Arrange and Setup on the
                 right. A Grid in the order it is seen, so Tab and a screen reader go left to right. The period line is the
                 star column: it trims first, then the tabs scroll (OnTopBarSizeChanged); the chip and ⟳ never give way. -->
            <Grid x:Name="TopBar" SizeChanged="OnTopBarSizeChanged">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <!-- The main clan's icon (backlog V3-S.7), from the moment the window opens (V3-S.6). Its name says whose it is, set
                     with the picture in ApplyIcon; its space is kept while the main clan's picture is on its way (A26). -->
                <Image x:Name="BoardIcon" Grid.Column="0" Width="22" Height="22" Margin="0,0,10,0" Visibility="Collapsed"
                       VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality" />
                <!-- Past its share of the bar (OnTopBarSizeChanged) the tabs scroll, so + Board and the period line always show. -->
                <ListBox x:Name="BoardTabs" Grid.Column="1" ItemContainerStyle="{StaticResource BoardTab}" DisplayMemberPath="Name"
                         Background="Transparent" BorderThickness="0" VerticalAlignment="Center" SelectionChanged="OnTabChanged"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Disabled"
                         AutomationProperties.Name="Boards">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ContextMenu>
                        <ContextMenu x:Name="TabMenu" Opened="OnTabMenuOpened" Closed="OnTabMenuClosed"
                                     Background="{DynamicResource RowBgBrush}"
                                     Foreground="{DynamicResource WhiteBrush}" BorderBrush="{DynamicResource EdgeBrush}">
                            <MenuItem x:Name="RenameBoardItem" Header="Rename…" Click="OnRenameBoardClick" />
                            <MenuItem x:Name="DuplicateBoardItem" Header="Duplicate" Click="OnDuplicateBoardClick" />
                            <MenuItem x:Name="DeleteBoardItem" Header="Delete…" Click="OnDeleteBoardClick" />
                        </ContextMenu>
                    </ListBox.ContextMenu>
                </ListBox>
                <!-- The board on screen: ⋯ opens the tab's own menu (Rename…, Duplicate, Delete…), the same one the
                     right-click opens, so the commands are on screen without taking the right-click away. Same ⋯ the
                     panels carry, one step up: it acts on the board, so it sits with its tabs. -->
                <Button x:Name="BoardMenuButton" Grid.Column="2" Content="⋯" Style="{StaticResource FlatButton}" Padding="6,2"
                        HorizontalAlignment="Left" VerticalAlignment="Center" Margin="2,0,10,0"
                        Foreground="{DynamicResource MutedTextBrush}" Click="OnBoardMenuClick"
                        AutomationProperties.Name="Rename, duplicate or delete this board" />
                <Button x:Name="AddBoardButton" Grid.Column="3" Content="+ Board" Style="{StaticResource AddBoardButtonStyle}"
                        HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,14,0" Click="OnAddBoardClick"
                        AutomationProperties.Name="Add a board" />
                <!-- BC2: the chip keeps StartStopButton's id so the walks' "ready" check still finds it. A click opens the
                     status card and never pauses; only "Start reading" acts, because there is no status yet to show. -->
                <Button x:Name="StartStopButton" Grid.Column="4" Style="{StaticResource ChipButton}" VerticalAlignment="Center"
                        Margin="0,0,6,0" Click="OnStatusChipClick" SizeChanged="OnTopBarSizeChanged">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="ChipGlyph" VerticalAlignment="Center" Margin="0,0,6,0" />
                        <TextBlock x:Name="ChipWord" VerticalAlignment="Center" FontWeight="Medium" />
                    </StackPanel>
                </Button>
                <Button x:Name="TestNowButton" Grid.Column="5" Content="⟳" Style="{StaticResource FlatButton}" Padding="6,2"
                        VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{DynamicResource MutedTextBrush}"
                        ToolTip="Read every source now (F5)" AutomationProperties.Name="Read every source now" Click="OnTestNowClick" />
                <!-- Ticks between reads (WithClock off: this line is one line wide, and the panel gives the end as a time). -->
                <ui:CountdownText x:Name="PeriodLine" Grid.Column="6" WithClock="False" Style="{StaticResource Muted}"
                                  VerticalAlignment="Center" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                <StackPanel x:Name="TopButtons" Grid.Column="7" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0"
                            SizeChanged="OnTopBarSizeChanged">
                    <!-- BC4: the gate stays, as Arrange. It stays in place while arranging, disabled; the banner holds the rest. -->
                    <Button x:Name="EditBoardButton" Content="Arrange" Click="OnEditBoardClick" AutomationProperties.Name="Arrange" />
                    <Button x:Name="SetupButton" Margin="8,0,0,0" Click="OnSetupClick" AutomationProperties.Name="Setup">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE713;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" VerticalAlignment="Center" Margin="0,0,6,0" />
                            <TextBlock Text="Setup" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>
                <!-- The status card (spec §3.3): opening it changes nothing (BC2). Themed (V3-S.13). Esc and a click away close it. -->
                <Popup x:Name="StatusCard" PlacementTarget="{Binding ElementName=StartStopButton}" Placement="Bottom" VerticalOffset="6"
                       StaysOpen="False" AllowsTransparency="True" Closed="OnStatusCardClosed">
                    <!-- R6d: Focusable, with the same focus ring a control gets, so Esc still closes the card when Pause is
                         disabled (e.g. a Test now read in flight) and nothing else inside it can take focus. -->
                    <Border x:Name="CardBorder" Focusable="True" FocusVisualStyle="{StaticResource FocusRing}"
                            Background="{DynamicResource RowBgBrush}" BorderBrush="{DynamicResource EdgeBrush}" BorderThickness="1"
                            CornerRadius="8" Padding="14" MinWidth="320" MaxWidth="460" PreviewKeyDown="OnStatusCardKeyDown"
                            KeyboardNavigation.TabNavigation="Cycle" AutomationProperties.AutomationId="StatusCardBody"
                            AutomationProperties.Name="Reading status">
                        <StackPanel>
                            <TextBlock x:Name="CardStateLine" FontWeight="SemiBold" TextWrapping="Wrap" Foreground="{DynamicResource WhiteBrush}" />
                            <!-- A RowList, not a bare ItemsControl (RowListFenceTests): each row reaches a screen reader as its text, not StatusRow's ToString. -->
                            <ui:RowList x:Name="CardSources" Margin="0,10,0,0" AutomationProperties.Name="Sources">
                                <ui:RowList.ItemTemplate>
                                    <DataTemplate>
                                        <DockPanel Margin="0,2">
                                            <TextBlock DockPanel.Dock="Right" Text="{Binding Line}" Style="{StaticResource Muted}" Margin="16,0,0,0" />
                                            <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis" Foreground="{DynamicResource WhiteBrush}" />
                                        </DockPanel>
                                    </DataTemplate>
                                </ui:RowList.ItemTemplate>
                            </ui:RowList>
                            <TextBlock x:Name="CardAlertsLine" Style="{StaticResource Muted}" Margin="0,10,0,0" />
                            <TextBlock x:Name="CardDetailLine" Style="{StaticResource Muted}" TextWrapping="Wrap" Margin="0,4,0,0" />
                            <Button x:Name="PauseResumeButton" HorizontalAlignment="Right" Margin="0,12,0,0" Click="OnPauseResumeClick" />
                        </StackPanel>
                    </Border>
                </Popup>
            </Grid>
        </Border>

        <!-- The lines under the bar, and while arranging the banner in their place (spec §4.2). Known limit: when the board
             was healthy and its lines hidden (BC3), the banner adds its own height once on the way in; no panel changes size.
             R8 (task 14): the margin lives on StateLines and ArrangeBanner themselves, not this slot, so a healthy,
             non-arranging board — both children Collapsed — draws no empty strip under the bar (TopBarFenceTests). -->
        <Grid x:Name="StatusSlot" DockPanel.Dock="Top">
            <StackPanel x:Name="StateLines" Margin="16,8,16,0">
                <TextBlock x:Name="StateLine" FontWeight="SemiBold" TextWrapping="Wrap" AutomationProperties.HelpText="What Ur Score is doing" />
                <TextBlock x:Name="DetailLine" Style="{StaticResource Muted}" Margin="0,2,0,0" />
            </StackPanel>
            <!-- BC5: Cancel is IsCancel, so Esc leaves arranging wherever focus is in the window; ApplyButtons disables it
                 outside arranging, and a disabled IsCancel button ignores Esc. The status card's own Esc is handled inside it. -->
            <Border x:Name="ArrangeBanner" Visibility="Collapsed" Margin="16,8,16,0" Background="{DynamicResource RowBgBrush}"
                    BorderBrush="{DynamicResource CyanBrush}" BorderThickness="0,0,0,2" CornerRadius="4" Padding="12,6">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="AddPanelButton" Content="+ Add panel" Click="OnAddPanelClick" AutomationProperties.Name="Add panel" />
                        <Button x:Name="CancelArrangeButton" Content="Cancel" Margin="8,0,0,0" IsCancel="True" Click="OnCancelArrangeClick"
                                AutomationProperties.Name="Cancel arranging" />
                        <Button x:Name="DoneButton" Content="Done" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0"
                                Click="OnDoneClick" AutomationProperties.Name="Done" />
                    </StackPanel>
                    <TextBlock x:Name="ArrangeLine" VerticalAlignment="Center" TextWrapping="Wrap" Foreground="{DynamicResource WhiteBrush}" />
                </DockPanel>
            </Border>
        </Grid>

        <TextBlock x:Name="AttributionLine" DockPanel.Dock="Bottom" Style="{StaticResource Muted}" FontSize="11" Margin="16,4,16,8" />

        <Grid>
            <!-- The drop caret is an adorner on the grid, so the scrolling area needs a layer to put it in. -->
            <AdornerDecorator>
            <ScrollViewer x:Name="BoardScroll" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="14">
                <!-- Transparent so a drop between panels still lands on the grid. -->
                <ui:PanelGrid x:Name="BoardPanels" Background="Transparent" AllowDrop="True" DragOver="OnBoardDragOver" DragLeave="OnBoardDragLeave" Drop="OnBoardDrop"
                              PreviewMouseLeftButtonDown="OnBoardMouseDown" PreviewMouseMove="OnBoardMouseMove"
                              PreviewMouseLeftButtonUp="OnBoardMouseUp" PreviewKeyDown="OnBoardKeyDown"
                              AutomationProperties.Name="Board" />
            </ScrollViewer>
            </AdornerDecorator>
            <StackPanel x:Name="EmptyState" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="480">
                <TextBlock x:Name="EmptyStateLine" Style="{StaticResource Heading}" TextAlignment="Center" />
                <TextBlock x:Name="EmptyStateDetail" Style="{StaticResource Muted}" TextAlignment="Center" Margin="0,8,0,0" />
                <Button x:Name="EmptyStateButton" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Center" Margin="0,16,0,0"
                        Click="OnEmptyStateClick" />
            </StackPanel>
            <!-- The undo toast (spec §5.3): themed, bottom centre, one at a time, about six seconds. A polite live region, so a
                 screen reader hears it; its Undo is a Tab stop while it shows, and Ctrl+Z works with or without it. -->
            <Border x:Name="UndoToast" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,16"
                    Background="{DynamicResource RowBgBrush}" BorderBrush="{DynamicResource EdgeBrush}" BorderThickness="1" CornerRadius="8" Padding="14,8">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="UndoToastText" VerticalAlignment="Center" Foreground="{DynamicResource WhiteBrush}"
                               AutomationProperties.LiveSetting="Polite" AutomationProperties.AutomationId="UndoToastText" />
                    <Button x:Name="UndoToastButton" Content="Undo" Style="{StaticResource FlatButton}" Margin="12,0,0,0"
                            Foreground="{DynamicResource CyanBrush}" Click="OnUndoToastClick" AutomationProperties.Name="Undo" />
                </StackPanel>
            </Border>
        </Grid>
    </DockPanel>
</Window>
