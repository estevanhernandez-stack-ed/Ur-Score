<UserControl x:Class="Labs626.UrScore.UI.PastPeriodsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="clr-namespace:Labs626.UrScore.UI">
    <Border Style="{StaticResource PanelCard}">
        <StackPanel>
            <ui:PanelFrame DataContext="{Binding Head}" />
            <StackPanel Margin="0,10,0,0" Visibility="{Binding Head.HasBody, Converter={StaticResource BoolToVisible}}">
                <!-- Four columns, and every one of them can be full: a long period name, a place, a total, and a
                     value with an account name after it. So each column past the first opens with a 12 px gutter,
                     and no two cells can run together the way Total and Your best did (32 beside 32 read as 3232).
                     The numbers take what they need and no more (an ordinal, and an abbreviated total), which
                     leaves the two columns that carry names the rest to share; Your best gets the larger share,
                     since it carries a number and a name where the period column carries one word. -->
                <Border Style="{StaticResource PanelTableHeadingRow}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="64" />
                            <ColumnDefinition Width="76" />
                            <ColumnDefinition Width="1.4*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding PeriodColumn}" Style="{StaticResource ColumnHeader}" />
                        <TextBlock Grid.Column="1" Text="Place" Style="{StaticResource ColumnHeader}" TextAlignment="Right" Margin="12,0,0,0" />
                        <TextBlock Grid.Column="2" Text="Total" Style="{StaticResource ColumnHeader}" TextAlignment="Right" Margin="12,0,0,0" />
                        <TextBlock Grid.Column="3" Text="Your best" Style="{StaticResource ColumnHeader}" TextAlignment="Right" Margin="12,0,0,0" />
                    </Grid>
                </Border>
                <ui:RowList ItemsSource="{Binding Rows}" AutomationProperties.Name="Finished periods, newest first">
                    <ui:RowList.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource PanelTableRowLine}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="64" />
                                        <ColumnDefinition Width="76" />
                                        <ColumnDefinition Width="1.4*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Period}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1" Text="{Binding Place}" TextAlignment="Right" VerticalAlignment="Center" Margin="12,0,0,0" />
                                    <TextBlock Grid.Column="2" Text="{Binding Total}" TextAlignment="Right" VerticalAlignment="Center" Margin="12,0,0,0" />
                                    <TextBlock Grid.Column="3" Text="{Binding YourBest}" TextAlignment="Right" TextTrimming="CharacterEllipsis"
                                               VerticalAlignment="Center" Margin="12,0,0,0" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ui:RowList.ItemTemplate>
                </ui:RowList>
            </StackPanel>
            <TextBlock x:Name="PanelNote" Text="{Binding Head.Note}" Style="{StaticResource PanelNoteText}"
                       Visibility="{Binding Head.HasNote, Converter={StaticResource BoolToVisible}}" />
        </StackPanel>
    </Border>
</UserControl>
