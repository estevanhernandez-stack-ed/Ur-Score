<UserControl x:Class="Labs626.UrScore.UI.MyAccountsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="clr-namespace:Labs626.UrScore.UI">
    <Border Style="{StaticResource PanelCard}">
        <StackPanel>
            <ui:PanelFrame DataContext="{Binding Head}" />
            <StackPanel Margin="0,10,0,0" Visibility="{Binding Head.HasBody, Converter={StaticResource BoolToVisible}}">
                <Border Style="{StaticResource PanelTableHeadingRow}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="110" />
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="120" />
                        </Grid.ColumnDefinitions>
                        <!-- Indented by everything the column lays out before a name: the avatar slot's 28 px and the sent
                             dot's 12 px, both unconditional, so this sits over every row's name. -->
                        <TextBlock Grid.Column="0" Text="Account" Style="{StaticResource ColumnHeader}" Margin="40,0,0,0" />
                        <TextBlock Grid.Column="1" Text="{Binding ValueColumn}" Style="{StaticResource ColumnHeader}" TextAlignment="Right" />
                        <TextBlock Grid.Column="2" Text="{Binding GroupColumn}" Style="{StaticResource ColumnHeader}" TextAlignment="Right" />
                        <TextBlock Grid.Column="3" Text="Change" Style="{StaticResource ColumnHeader}" TextAlignment="Right" />
                    </Grid>
                </Border>
                <ui:RowList ItemsSource="{Binding Groups}" AutomationProperties.Name="Your accounts by this stat">
                    <ui:RowList.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Heading}" Style="{StaticResource PanelTableGroupHeading}" />
                                <ui:RowList ItemsSource="{Binding Rows}">
                                    <ui:RowList.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource PanelTableRowLine}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="110" />
                                                        <ColumnDefinition Width="80" />
                                                        <ColumnDefinition Width="120" />
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0">
                                                        <DockPanel>
                                                            <Ellipse DockPanel.Dock="Left" Style="{StaticResource AccountAvatar}" />
                                                            <!-- Hidden, never Collapsed, for the same reason the avatar slot is (A26): the
                                                                 dot's 12 px is laid out whether or not the account is sent, so every name in
                                                                 the column starts at the same x, the heading sits over all of them, and a row
                                                                 doesn't move when its sent state changes. -->
                                                            <Ellipse DockPanel.Dock="Left" Width="6" Height="6" Margin="0,0,6,0" VerticalAlignment="Center"
                                                                     Fill="{DynamicResource CyanBrush}">
                                                                <Ellipse.Style>
                                                                    <Style TargetType="Ellipse">
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding Sent}" Value="False">
                                                                                <Setter Property="Visibility" Value="Hidden" />
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Ellipse.Style>
                                                            </Ellipse>
                                                            <TextBlock Text="{Binding Name}" Style="{StaticResource RowCell}" />
                                                        </DockPanel>
                                                        <!-- Indented past the avatar slot and the sent dot (28 + 12 px, A26), so the
                                                             reason lines up under the name it is about. -->
                                                        <TextBlock Text="{Binding Note}" Style="{StaticResource Muted}" FontSize="11"
                                                                   Margin="40,3,0,0"
                                                                   Visibility="{Binding HasNote, Converter={StaticResource BoolToVisible}}" />
                                                    </StackPanel>
                                                    <TextBlock Grid.Column="1" Text="{Binding Value}" Style="{StaticResource NumberCell}" />
                                                    <TextBlock Grid.Column="2" Text="{Binding InGroup}" Style="{StaticResource NumberCell}" />
                                                    <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right">
                                                        <TextBlock Text="{Binding Change}" Style="{StaticResource NumberCell}" />
                                                        <TextBlock Text=" · stalled" FontSize="11" VerticalAlignment="Center"
                                                                   Foreground="{DynamicResource MagentaBrush}"
                                                                   Visibility="{Binding Stalled, Converter={StaticResource BoolToVisible}}" />
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ui:RowList.ItemTemplate>
                                </ui:RowList>
                            </StackPanel>
                        </DataTemplate>
                    </ui:RowList.ItemTemplate>
                </ui:RowList>
            </StackPanel>
            <TextBlock x:Name="PanelNote" Text="{Binding Head.Note}" Style="{StaticResource PanelNoteText}"
                       Visibility="{Binding Head.HasNote, Converter={StaticResource BoolToVisible}}" />
        </StackPanel>
    </Border>
</UserControl>
